# 챌린지 상세 5종 이미지 고급화 — 설계 (2026-09-16 오너 확정)

오너 "나노바나나를 활용해서 챌린지 상세페이지를 고급화하고 싶다. 지금도 마음에 들지만 이미지를
더 많이 쓰고 싶다" → 목업(보신각)으로 배치 확정. 범위는 **상세 5종만**(허브·신청 페이지는 범위 밖).

2026-09-07 ①안 '결과 먼저' 배치(`pages.md` 같은 절)를 그대로 두고 **이미지만 얹는다.** 순서·문구·
가격 규칙은 건드리지 않는다.

## 왜 — 진단 실측 (보신각 375×812)

| 구간 | 길이 | 이미지 |
|---|---|---|
| 히어로 | 697px | 사진 1 |
| 비포/애프터 | 673px | 0 |
| 이런 학생에게 | 489px | 0 |
| 2주 진행 방식 | 949px | 코치 64px |
| 후기 | 771px | 0 |
| 참가비 | 555px | 0 |
| FAQ | 437px | 0 |
| 마무리 | 378px | 로고 |

전체 5,252px 중 **3,303px(63%)가 글자·상자만.** 특히 참가비→FAQ→마무리 1,370px(1.7화면)은
이미지 없이 끝난다. 2026-09-07 B안(아이콘·옅은 판·선그림)은 같은 증상의 부분 처방이었다.

## 결정 1 — 이미지 종류: 사람 없는 장면·사물·질감

오너 확정. **사람·얼굴·손이 나오는 AI 이미지는 만들지 않는다.**

- 승무원 지망생은 유니폼·기내 디테일에 민감하다. AI가 그린 승무원은 항공사 유니폼이 아니라
  '유니폼 비슷한 것'이 되고 바로 티가 난다 — 신뢰가 깎인다.
- 히어로·연구진 인물 사진이 이미 있다. AI가 사람을 또 그리면 그 사진들과 경쟁만 한다.
  (⚠️ `hero-spinning.webp` 는 AI 생성으로 보인다 — 세 명 좌우 대칭·같은 자세·균일한 얼굴 질감.
  2026-09-16 오너 확인 전. 맞다면 이 원칙과 어긋나니 교체 여부를 따로 정한다.)
- 손을 빼면 AI 손가락 문제가 원천 차단된다.

## 결정 2 — 자리마다 역할이 다르다 (C안)

'많이'가 아니라 '왜 여기 있는지'. 섹션마다 헤더 이미지를 까는 B안은 기각했다 — 클래스101·인프런의
그 이미지들은 전부 **결과물 스크린샷**이고, 우리에겐 그 소재가 없어서 흉내 내면 의미 없는 질감
사진 30장이 된다(길이도 6.5→8화면으로 되돌아간다).

| 역할 | 자리 | 장수 | 만드는 법 |
|---|---|---|---|
| ① 보정 | 보신각 히어로 | 1 | **AI 없이** 크롭·직접 보정 |
| ② 3단계 장면 | 2주 진행 방식 | 6(공용) | Seedream 4.5 |
| ③ 전환 밴드 | 비포/애프터 뒤 | 5 | Seedream 4.5 |
| ④ 마감 | 마무리 CTA 배경 | 1(공용) | Seedream 4.5 |

**새로 뽑는 것 12장 + 보정 1장.**

### ① 히어로 보정은 1장뿐 — AI를 쓰지 않는다

인스타 UI 잔재는 `hero-voice.webp` 한 장에만 있다(2026-09-16 5장 전부 눈으로 확인). 오른쪽 가장자리
캐러셀 화살표와 하단 점 인디케이터 — **하단 점은 상세 4:3 크롭(`object-position: 50% 24%`)에서 이미
잘리고 오른쪽 화살표만 보인다.** 인물 얼굴이 든 실사를 AI 편집에 넣으면 얼굴이 미묘하게 바뀔 수 있어
크롭이나 직접 패치로 처리한다. ⚠️ 같은 파일을 허브 카드(`challenges.html`)도 쓰니 크롭하면 거기서도 본다.

세로형 `hero-culture.webp`(899×1124)도 상세 CSS 가 데스크톱까지 4:3 cover 로 자르므로 가로 확장이 필요 없다.

### AI가 맡으면 안 되는 자리

**비포/애프터·후기·코치·제출물에는 생성 이미지를 넣지 않는다.** 가짜 증거가 된다
(`pages.md` '지어낸 지표 제거' 2026-08-02 원장과 같은 규칙).

### 중간부(후기·참가비)는 비워둔다 — 오너 확정

읽고 결정하는 구간이라 사진이 시선을 빼앗으면 결제 직전 집중이 깨진다. 앞뒤(③ 밴드 ·
④ 마무리)에 이미지가 있어 글 벽으로도 보이지 않는다. **참가비 카드 배경 질감도 넣지 않는다.**

### '이런 학생에게 맞아요'는 이미지로 대체하지 않는다

6줄이 각각 구체적 증상("말끝이 흐려져 자신감이 없어 보이는 학생")이라 이미지로 바꾸면 정보가
사라진다. 글 대체는 ②(3단계) 한 자리뿐이다.

## 아트디렉션 — 5종이 한 세트로 보이게

프롬프트 공통 접미(그대로 붙인다):

    No people, no hands, no faces, no text, no letters, no logos, no brand marks.
    Cool white and deep navy color palette, soft natural window light from the side,
    no warm beige or yellow tones, calm minimal editorial still life photography,
    shallow depth of field, clean uncluttered composition.

| 항목 | 고정값 |
|---|---|
| 사람 | 없음. 손도 없음 |
| 색 | 쿨 화이트 + 딥 네이비 + 자연광. **웜 베이지·아이보리 금지**(2026-08-05 폐기 팔레트) |
| 조명 | 창측 부드러운 측광. 드라마틱 조명·강한 그림자 금지 |
| 글자 | 이미지 안에 텍스트 없음(AI 한글은 깨진다) |
| 상표 | 항공사 CI·로고·유니폼 없음 |

전환 밴드 소재: 보신각=창가 마이크 / 스피닝=메트로놈 / 영합각=흰 벽 앞 빈 의자 /
승자각=인덱스 카드 10장 / 댄특완=이른 아침 빈 탑승구

**한 페이지에서 같은 소품이 밴드와 3단계에 겹치지 않게 골랐다.** 영합각 밴드를 삼각대·링라이트로 두면
'촬영' 단계와 겹치고, 승자각 밴드를 노트로 두면 '답변 쓰기' 단계와 겹친다. 댄특완을 기내 창으로 두면
5종 공용 마무리(기내 창 하늘)와 겹친다. 보신각은 밴드(창가 스탠드 마이크)와 '녹음'(탁상 폰+핸드 마이크)이
둘 다 마이크지만 목소리 챌린지라 자연스럽고 구도가 달라 그대로 뒀다.

## 만드는 법 — Seedream 4.5, 오너가 웹에서 무료로 (2026-09-16 오너 확정)

**12장 전부 Seedream 4.5 한 모델로 뽑는다.** 오너가 웹에서 직접 뽑아 `다운로드\monc-seedream` 에
넣으면, 자리 판별·리사이즈·webp 변환·적용은 작업자가 한다. 프롬프트 12개와 버리는 기준은
**`2026-09-16-challenge-detail-imagery-prompts.md`** 가 정본이다.

- **MCP(Higgsfield 연결)로 뽑지 않는다.** 2026-09-16 실측: Plus 요금제, 무제한 생성 꺼짐
  (`unlim.available: false`), Seedream 4.5 = 1크레딧/장 · `nano_banana_pro` = 2크레딧/장. 오너 방침은
  크레딧 소모 없이.
- **한 모델로 통일한다.** 같은 프롬프트라도 모델마다 색·질감 해석이 달라 한 페이지에 섞이면 어긋난다.
  목업 때 `nano_banana_pro` 로 뽑은 5장(10크레딧 사용)은 쓰지 않는다 — 톤 기준으로만 참고한다.

### 파일 규격·변환

원본 → PIL 로 리사이즈 + webp. 실측 용량은 아래 '받은 12장' 절(Seedream 기준).

| 자리 | 폭 | 품질 |
|---|---|---|
| 전환 밴드 | 1200 | q82 |
| 3단계 | 420×420 | q82 |
| 마무리(1:1 원본) | 1200×1200 | q82 |

파일명: `images/cd-band-<id>.webp` · `images/cd-step-<장면>.webp` · `images/cd-outro.webp`

3단계 공용 6장과 5종 매핑:

| 챌린지 | 1 | 2 | 3 |
|---|---|---|---|
| 보신각·스피닝 | watch | record | review |
| 영합각 | watch | film | review |
| 승자각·댄특완 | watch | write | submit |

## 받은 12장 — 확정과 보정 (2026-09-17)

원본: `다운로드\monc-seedream`(1차 2026-09-16 8장 · 2차 2026-09-17 9장 · 3차 같은 날 1장). 변환본: `images/cd-*.webp`(2026-09-18 적용).

| 자리 | 원본 파일 접두 | 보정 |
|---|---|---|
| 밴드 voice·spinning·expression·answer·culture | `hf_20260916_095745`·`095751`·`095756`·`095925`·`095929` | 위아래 가장자리 |
| 3단계 watch·record·film·write·submit·review | `hf_20260917_075310`·`075313`·`075316`·`075319`·`075322`·`075330` | 흰색 중성화 |
| 마무리 outro | `hf_20260917_082221`(밤하늘 기내 창 · 1:1 → 1200×1200) | 없음(덮개가 씌워진다) |

**버린 것**: 1차 `100108`(영상 보기 — 태블릿 화면에 흐릿한 사람 실루엣) · 1차 `100113`·`100116`(녹음·촬영 —
16:9 크롭본이었는데 3단계 여섯 장을 같은 세션 1:1 로 통일) · 2차 `073431`·`074135`(프롬프트 12개에 없는 세로형 장면,
하나는 사람 — 다른 작업 이미지가 섞인 것으로 보임) · 2차 `075132`(마무리 낮 하늘 — 아래 '대비' 절, 폰에서 사진이 안 보여 교체).

보정 두 가지 — 둘 다 PIL(numpy), 크레딧 없음:

- **밴드 위아래 가장자리**: 높이의 16% 구간을 0.88 → 1.0 으로 곡선 감쇠(지수 1.6). 스피닝 흰 창·보신각 흰 벽이
  페이지 순백에 녹아 사진이 덜 로드된 것처럼 보였다.
- **3단계 흰색 중성화**: 2차 1:1 여섯 장이 파란빛이었다(밝은 영역 B-R **16~34** vs 밴드 **2~10**). 이미지마다 밝기
  상위 60~90% 픽셀 평균을 재서 `(L-2.5, L, L+2.5)` 가 되게 채널 게인 → 보정 후 B-R 1~6.

용량(변환 실측): 밴드 15~57KB(댄특완만 창틀 디테일로 57) · 3단계 5~8KB · 마무리 22KB.
페이지당 밴드 1 + 3단계 3 + 마무리 1 = **보신각 67KB · 댄특완 96KB**.

## 코드

전부 `challenge-detail.css` 한 파일에. **5종 인라인 `<style>` 블록은 손대지 않는다**(다섯 파일
글자 그대로 동일 유지 규칙).

- `.cd-band`(3-1 절) — 전환 밴드. 전체 폭 사진, ≥860px 은 760px 폭 + 라운드. 마크업은 '3. 나한테 맞나' 주석 앞 한 줄
- `.cd-steps`(5-1 절) — 5종 전부 사진이라 수식어 없이 규칙 자체를 교체했다(`<i data-icon>` → `<img>` · 109×109).
  선 아이콘 규칙 7개와 연결선(`::before`)은 dead 라 지웠다
- `body[data-challenge] .cta-box`(8-1 절) — 마무리. 마크업 변경 없이 특이도로 덮는다. 배경 사진 + 세로 그라디언트 덮개

⚠️ **`.cta-box::before` 는 이미 쓰이고 있다**(인라인 141행 — 오른쪽 위 220px 원형 하이라이트).
덮개를 `.cta-box::before` 로 덮어쓸 때 `inset: 0` 만 주면 **원래 규칙의 `width: 220px; height: 220px; border-radius: 50%`
가 그대로 남아 덮개가 왼쪽 위 원 하나로 줄어든다**(2026-09-17 실측 — 상자 343×298 중 220×220 원만 덮임).
반드시 `width: auto; height: auto; border-radius: 0` 을 같이 준다. 나노바나나 목업이 이 상태로 승인됐다
(사진이 선명해 보인 것도 이 버그 때문이었다).

- `.cd-step img` 에 `box-shadow: 0 0 0 1px var(--border-soft)` — 흰 책상 사진이 흰 배경에 녹는다
  (구 아이콘 원 `.cd-step i` 와 같은 문법).

전체 폭 밴드는 금지 규칙의 **승인된 예외**다(홈 ④ 숫자와 같은 문법 — 금지가 막은 건 순백과 옅은
판이 만드는 가로선이고, 사진은 장면 전환으로 읽힌다).

이미지는 전부 `loading="lazy"` + `width`/`height` 명시(CLS 방지). 히어로만 `fetchpriority="high"` 유지.

## 예산 — 못 지키면 실패로 본다

| 항목 | 기존 | 목업 실측 | 상한 |
|---|---|---|---|
| 길이 | 5,252px (6.5화면) | 5,608px (6.9화면) | **7.2화면** |
| 이미지 전송량 | — | 67~96KB/페이지 | **250KB** |
| 375px 넘침 | 0 | 0 | 0 |
| 사진 위 글씨 대비 | — | 최악 5.31:1(320 설명 줄) | 4.5:1 |

(2026-09-17 Seedream 12장 · 보신각 목업 기준)

### ⚠️ 사진 위 글씨 대비 — 글씨를 숨긴 렌더에서 줄마다 잰다

두 방법이 차례로 틀렸다.

1. **글씨가 보이는 스크린샷**: 글씨 픽셀이 배경으로 섞여 1.54:1 이라는 가짜 미달(2026-09-16).
2. **원본 사진 최대 밝기 + 덮개 알파 합성**: 계산은 맞지만 덮개가 상자 전체를 덮는다는 전제였는데 실제로는
   원 하나만 덮고 있었다(위 '코드' 절). 게다가 이 방식은 '글씨 뒤가 순백일 수도 있다'고 가정해서, 버그를 고치고
   전체에 적용하면 덮개가 너무 진해 **사진이 사라진다**.

**정본**: 목업에 `?hidetext=1`(상자 자식 `visibility: hidden`)을 주고 iframe 래퍼로 찍은 뒤, 프리뷰 JS 로 잰
**줄별 좌표**(제목·설명·작은 글씨)의 배경 최대 밝기와 그 줄 글씨색으로 대비를 계산한다. **폭 4개 — 320·375·430·1280.**
폭마다 상자 비율이 달라 사진의 다른 부분이 글씨 뒤에 온다. 사진을 바꾸면 다시 잰다.

**덮개를 정하는 법 — 필요 최소 알파를 먼저 계산한다**(2026-09-17 밤하늘 사진에 적용):
① `?hidetext=1&noveil=1`(덮개도 끔)로 폭 4개를 찍는다 ② 줄마다 배경 최대 밝기 픽셀 P 에 대해
`P×(1-a) + 덮개색×a` 위 글씨가 **5.0:1(기준 4.5 + 여유)** 이 되는 최소 a 를 0.01 단위로 찾는다 ③ 줄의 상자 내 %위치에
폭 4개 최대 a 를 놓아 그라디언트를 짠다 ④ `?hidetext=1` 로 다시 찍어 검증. 한 번에 맞는다 — 감으로 올렸다 내리는 반복을 없앤다.

마무리 사진은 **밤하늘 기내 창**(2차 `075132` 낮 하늘에서 교체 · 2026-09-17 오너 선택). 낮 하늘은 흰 구름·밝은 하늘이라
대비를 지키려면 폰에서 덮개가 .38~.92 로 진해져 사진이 거의 안 보였다. 밤하늘 필요 최소 알파(목표 5.0):

| 폭 | 제목 | 설명 | 작은 글씨 |
|---|---|---|---|
| 320 | .20 | .21 | .60 |
| 375 | .00 | .01 | .66 |
| 430 | .05 | .13 | .66 |
| 1280 | **.55** | .34 | .62 |

폰은 제목·설명 뒤가 짙은 별하늘이라 덮개가 거의 필요 없고 작은 글씨(구름층)만 진해야 한다. **데스크톱은 1:1 사진이
넓게 확대돼 제목 뒤에 밝은 창틀이 와서 따로 둔다**(4폭 최대로 하나만 두면 폰도 .55 가 깔려 사진이 죽는다):

- 폰(기본): `linear-gradient(180deg, .24 0%, .26 45%, .72 62%, .74 100%)` · `background-position: 50% 50%`
- 데스크톱(≥860px): `linear-gradient(180deg, .60 0%, .60 34%, .44 48%, .68 68%, .70 100%)`
- 덮개색 `rgba(16,31,54,*)`

| 폭 | 제목 흰 100% | 설명 흰 88% | 작은 글씨 흰 84% |
|---|---|---|---|
| 320 | 5.37 | 5.31 | 6.81 |
| 375 | 8.17 | 6.38 | 5.91 |
| 430 | 6.48 | 5.79 | 5.94 |
| 1280 | 5.77 | 6.73 | 5.96 |

⚠️ 5종은 제목 문구 길이가 달라 줄 좌표·줄 수가 바뀐다 — 적용 후 5종 × 폭 4개를 같은 방법으로 다시 잰다.

### ⚠️ 375px 스크린샷은 iframe 래퍼로만

헤드리스 크롬 `--window-size=375,…` 는 창 최소 폭 때문에 **약 500px 로 그린 페이지의 왼쪽 375px** 를 찍는다.
모바일처럼 보여서 속는다(2026-09-16 목업 두 장이 이 상태로 오너에게 갔다 — 밴드 282px·오른쪽 잘림).
`mockup-frame.html?w=<폭>` 래퍼(iframe 폭 지정)로 찍고, **전체 폭 요소 높이가 프리뷰 실측과 같은지**로 검증한다
(375px 밴드 211px).

## 순서

1. **보신각 1종** 먼저 → 375px 목업 → 오너 승인 ✅(2026-09-16 · 나노바나나 이미지 — 덮개 버그 상태였다)
2. 오너가 Seedream 4.5 로 12장 생성 ✅(2026-09-17 · 17장 받아 12장 확정)
3. 받은 12장을 보신각 목업에 끼워 톤 재확인 ✅(2026-09-17 — 보정 2가지·덮개 버그 수정·대비 폭 4개 실측)
4. 마무리 사진 밤하늘로 교체 + 덮개 폰/데스크톱 분리 ✅(2026-09-17 오너 선택) → 오너 "해봐"(2026-09-18)
5. 5종 적용 + 보신각 히어로 보정 1장 ✅(2026-09-18 — 덮개색은 홈 사진 덮개 `rgba(18,28,46)` 로 통일)
6. 5종 × 폭 4개 실측 ✅(마무리 대비 최저 4.81:1 · 넘침 0 · 콘솔 0) → 오너 스크린샷 확인 후 `main` 직행
   (현행 규칙 요약은 `docs/notes/pages.md` '이미지 고급화' 절)

## 목업 부산물

목업 파일(`mockup-imagery.html`·`mockup-frame.html`·`__detail-frame.html`)과 `outputs/mockup-img/`·`outputs/seedream/`
은 적용 후 지웠다(비교 목업 관례). 변환본 12장은 `images/cd-*.webp`, 원본은 오너 PC `다운로드\monc-seedream`.
