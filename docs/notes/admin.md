# admin.html — 상세 기록

## 5차 — 전면 진단(2026-08-02 · 오너 "어드민 UXUI 시스템 전체 점검")

1~4차가 **모바일 치수**(활자·표적·겹침)를 잡았다면, 5차는 **동선·상태·데이터 안전**까지 봤다.
정적 분석 7관점 + 393/320px 실측을 같이 돌렸다. 확정 결함 106건 중 아래를 고쳤다.

| 잰 것 | 전 | 후 |
|---|---|---|
| 12px 미만 활자 | 6곳 | 0 |
| 44px 미만 조작요소(393px) | 7 | 0 |
| 경계선 3:1 미달(조작요소) | **429** | 0 (`.tb-stat` 구분선 5개는 의도적 예외 — 아래) |
| 인접 표적 6px 쌍 | 151 | 70(칩·행버튼 8px 로) |
| `:focus-visible` 규칙 | **0개** | nav.css 와 같은 3px 링 |
| `:disabled` 규칙 | **0개** | 흰 면 + 흐린 글씨 |
| 가로 넘침(320·393 · iOS date 폭 주입) | 0 | 0 |

### ⚠️ P1 — 조용히 데이터가 사라지던 것 셋
1. **연구실 자료 '가격 0' 이 저장되지 않았다.** `if (priceVal > 0) baseRow.price = priceVal`
   한 줄이 신규·수정 두 경로를 같이 막아서, **유료 자료를 무료로 되돌리면 옛 가격이 그대로
   남고 화면은 '수정했어요'** 라고 말했다. → 수정(update)에서는 0도 반드시 보낸다.
   (컬럼이 없는 환경은 update 가 에러를 내고 기존 안내 문구가 잡는다.)
2. **'충전 상품 저장'이 `credit_packs` 배열을 통째로 덮어썼다.** 화면은 `ai_killer` 팩 하나만
   읽어 편집하는데 저장은 배열 전체를 갈아끼웠다 — 팩이 둘 이상이면 나머지가 사라지고,
   verify-payment 가 같은 값으로 금액을 검증하므로 **그 상품 결제가 막힌다.**
   → 기존 배열을 읽어 `ai_killer` 만 교체. 그리고 **가격 0원 저장을 막았다**(`price < 1` 거부 —
   전에는 칸을 비우면 `Number('')||0` 이 0원 팩으로 저장돼 충전이 공짜가 됐다).
3. **일차 배치 편집기가 배치를 지웠다.** 후보 목록(`pool`)이 `active && 같은 항공사` 라
   **숨김 처리했거나 다른 항공사 기출이 배치된 일차는 `— 비움 —` 으로 보였고, 그대로
   [배치 저장]하면 실제 배치가 삭제**됐다. → 지금 배치된 기출은 후보 밖이어도
   `[지금 목록 밖]` 항목으로 만들어 selected 로 둔다. 지우기 실패도 이제 실패로 알린다.

### ⚠️ 화면이 '모른다'를 '없다'로 말하던 것
- **조회가 실패해도 '오늘' 브리핑이 "처리할 일이 없습니다"로 단정**했다(원칙 11).
  신청·오픈알림·회원 조회가 막히면 배열이 빈 채로 남아 숫자가 전부 0이 되고, 오류처럼
  보이지 않아 오너는 재시도하지 않는다 — 그날 입금 확인을 통째로 건너뛴다.
  → `_loadErr` 세 칸으로 실패를 기억하고, 하나라도 실패면 숫자를 `–` 로 두고
  **"오늘 현황을 불러오지 못했습니다" + [다시 불러오기]** 를 띄운다.
  ⚠️ 이 짝(로더의 `_loadErr[...] = !!error` ↔ loadToday 앞머리 분기)을 끊지 말 것.
- **`loadToday()` 가 초기화 때 딱 한 번만 돌았다.** 입금 3건을 다 처리하고 '오늘'로 돌아와도
  헤드라인이 3건 그대로였다. → 탭 전환 시 `today` 면 다시 센다(캐시만 읽으므로 비용 없음).
- **회원 조회는 `error` 를 통째로 버렸다** — 실패해도 '회원이 없습니다'로 뜨고,
  신청자 배지가 **전원 '비회원'** 으로 뒤바뀐다. → error 를 받아 목록에 사실대로 적는다.

### 동선 — 숫자를 눌렀으면 그 숫자의 목록이 나와야 한다
- 신청자 현황에 **입금 필터(`#afPay` 전체/미입금만)** 신설. '오늘'의 [입금 확인 필요]와
  [오픈 알림 연락 전]이 `data-focus` 로 그 필터를 켜고 이동한다(`activateTab(name, focus)`).
  ⚠️ **'미입금' 판정식은 `loadToday` 의 `unpaid` 와 같아야 한다**(`isLiveApplication && !appPaid`).
  한쪽만 고치면 카드 숫자와 목록 건수가 어긋나 화면이 거짓말을 한다.
  ⚠️ 메뉴로 apps 를 열면 필터는 초기화된다(initTabs) — 안 그러면 나중에 열었을 때
  3건만 보이고 "신청이 왜 이것뿐이지"가 된다. activateTab 이 그 뒤에 다시 걸므로 순서가 중요하다.
- **탭을 바꾸면 맨 위에서 시작한다.** 전에는 스크롤 위치가 그대로라 연구실 자료를 1200px
  내려 보다가 감점 사전을 고르면 그 탭 중간(745px)에 떨어졌다.
- **회원 목록에 기수를 적는다.** 배정이 이 화면의 주 작업인데 '미배정만'을 켜기 전에는
  누가 배정됐는지 알 수 없었다. 정렬도 **최근 가입 먼저**로 뒤집었다('오늘 새 회원'을 눌러
  들어갔는데 그 사람이 목록 맨 끝이면 지표가 행동으로 안 이어진다).

### 기본기 — admin 만 못 받고 있던 것
admin 은 **nav.css 를 안 읽는다(의도)**. 그래서 2026-08-02 에 공개 페이지가 받은 것이 여기만 빠졌다.
- **포커스 링**: 크로미움 기본 1px 주황(`#E59700` · 흰 면 2.2:1)만 나왔다 → nav.css 와 같은
  `outline: 3px solid var(--action); outline-offset: 2px`.
- **본문 바로가기(`.skip-link`)** + `#main`(=`.smain`, `tabindex="-1"`).
- **`:disabled`**: 회원 '배정' 버튼이 disabled 인 채로 채움 네이비 CTA 그대로였다(눌러도 무반응).
  ⚠️ opacity 로 흐리지 않는다 — 글씨 대비가 무너진다. 흰 면 + `--text-dim`.
- **`accent-color: var(--action)`**: 체크박스·라디오가 브라우저 기본 파랑이라 네이비 안에서 튀었다.
- **경계선 `--border-strong`**: `--border`(흰 면 위 1.38:1)를 입력칸·고스트 버튼·칩이 그대로
  쓰고 있었다(429곳). `--border-strong` 사용 0회였다 — 원칙 4의 신설 조항이 admin 에 한 번도 안 왔다.
  ⚠️ `--border` 자체는 올리지 않는다(카드·구분선까지 진해져 표처럼 보인다).
  ⚠️ `.is-danger` 는 이 규칙에서 제외하고, 대신 테두리를 `#B3261E` 제 색으로(전 `.5` 알파 = 2.47:1).
- **바텀시트 포커스 3종**: 열 때 안으로 · 뒤를 `inert` · 닫을 때 `#mtMenu` 로 복귀.
  ⚠️ 데스크톱(>960px)에선 사이드바가 덮개가 아니므로 포커스를 건드리지 않는다.

### 구조 — '썼다'와 '먹는다'가 또 갈렸던 자리
- **`[hidden] { display: none !important; }` 한 줄로 통일.** `.round-editing`·`#lrEditBar` 는
  2026-08-01 에 짝 규칙으로 하나씩 막았지만 같은 사고가 `#qfRaw`(원문 접기가 안 접힘)·
  `#afSub`(비어 있어도 자리를 먹음)에서 또 났다. **자리마다 짝을 달지 말고 이 한 줄에 맡긴다.**
- **`.ri-del`·`.ri-edit`·`.ri-cancel`·`.ri-actions` 를 `.round-item` 자손에서 떼어냈다.**
  목록 밖에서 쓰던 두 곳(자료 파일 목록·이용권 지급 회원 고르기)이 브라우저 기본 회색 사각
  버튼으로 나오고 있었다.
- **`.empty` 는 38곳이 쓰는데 CSS 정의가 없었다.** 실패·빈 안내가 여백 0의 맨 글자였다
  (그래서 자리마다 인라인 style 로 때웠다). `.empty` / `.empty.err` 신설.
- **`button, input, select, textarea { font-family: inherit }`** — 개별 규칙 17곳이 이걸 빠뜨려
  같은 화면에 SUIT 와 시스템 글꼴이 섞였다.
- **인라인 `min-height:38px/36px` 제거**(일차 배치 select·date, 답변 프로그램 `<summary>` 2곳) —
  모바일 44px 규칙을 인라인이 이기고 있었다. summary 는 `.ap-sum` 부품으로(펴기/접기 표시 포함).
- **`.round-form input { width:100% }` 에서 체크박스·라디오 제외** — 데스크톱에서 '공개'
  체크박스가 폼 폭(752px)만큼 늘어났다.
- **크레딧 회원 행의 인라인 `display:flex` 제거** → `.ri-actions`. 2026-08-01 의 "행 버튼
  컨테이너에 인라인 flex 금지" 규칙을 이 줄만 안 지켜서 모바일 1열 규칙이 안 먹었다.
- **폼 제출 연타 방지 `guardSubmit(formId, handler)`** — 8개 폼 전부. 전에는 두 번 누르면
  특강·프로그램·기수·문제·기출이 두 벌씩 들어갔다. ⚠️ handler 가 async 고 return 경로가
  많아 자리마다 풀어 주면 반드시 하나를 빠뜨린다 — 리스너를 감싸고 `finally` 에서만 푼다.

### 문구
- **챌린지 이름을 학생이 보는 이름으로 통일**: `CH_LABEL` 이 `보이스·표현력·답변` 이라
  후기 탭(`보신각·영합각·승자각`)·공개 페이지와 갈려 있었다 → `보신각·영합각·스피닝·승자각`.
  select 는 `보신각(보이스)` 처럼 옛 이름을 괄호로 남긴다. **DB 값(voice 등)은 그대로다.**
- **이용권 목록의 DB enum**(`admin`/`purchase`, `active`/`expired`)이 화면에 그대로 떴다 →
  `AP_SOURCE`·`AP_ENR_STATUS` 표로 한국어.
- 후기 카드의 기수 칸이 채워지면 `2` 만 남았다(원칙 15) → `기수` 라벨을 붙였다(`.rv-lb`).
- 신청자 **전화 검색이 하이픈까지 같아야** 걸렸다 → 숫자만 남겨 비교(폰에서 `01098765432` 로 침).
- 후기 **노출/숨김이 목록을 통째로 다시 조회**해 스크롤이 맨 위로 튀었다 → `patchRow`.

### '오늘' 숫자 = 2열 타일 (2026-08-02 · 오너가 목업 B안 확정)

5차 진단에서 "숫자 5줄의 구분선 대비·간격은 오너 확정 목업이라 손대지 않는다"고 남겼는데,
오너가 **"1면 목업은 한 번 더 정해야겠네"** 라며 다시 고르기를 요청했다. 네 안(지금 화면 /
A 할 일 먼저 / B 숫자 접기 / C 최소 수정)을 393px 목업으로 만들어 **B안 확정**.

| | 전(세로 5줄) | 후(2열 타일) |
|---|---|---|
| 첫 행동 버튼 위치 | 749px(첫 화면 밖) | **642px**(첫 화면 안) |
| 숫자 블록 높이 | 352px | 277px |
| '오늘' 전체 길이 | 1,605px | 1,498px |
| 같은 숫자 반복 | 3번 | 2번(제목 + 타일) |

- **칸은 6개**: 입금 확인 필요 · 오픈 알림 연락 전 · 검수 대기 · 오늘 신청 · 오늘 새 회원 · **전체 회원**.
  순서는 '돈 먼저' — 손댈 것이 남은 칸이 위, 그냥 보는 숫자가 아래.
- **`data-todo="1"` 칸만 빨강 강조**(`.tb-stat.todo` — 테두리 + 숫자 `#A33D14`).
  ⚠️ **값이 0 이거나 '–'(모름)이면 강조하지 않는다** — 할 일이 없으면 눈에 띌 이유가 없고,
  못 받은 값을 '급한 일'처럼 보이게 하면 원칙 11 위반이다. 판정은 `tdSet()` 안에 있다.
- ⚠️ **`minmax` 를 150px 이상으로 올리지 말 것** — 320px 화면에서 1열로 풀려 도로 길어진다
  (본문 여백 16×2 를 뺀 288px 에 132×2+8 이 들어간다). 실측: 320px → 140px 2열, 데스크톱 우측 단 → 174px 2열.
- **리드 문장(`#tbLede`)은 정상 화면에서 접는다.** 바로 아래 타일·'지금 처리'가 같은 숫자를
  다시 말하는 줄이었다. ⚠️ **요소는 지우지 말 것** — 조회 실패 안내가 이 자리를 쓴다.
- **전체 회원 수는 독립 칸**이다. 하루 전 버전은 '오늘 새 회원'의 보조 문구(`#tdMembersTotal`)에
  얹었는데, B안에서 칸이 생기며 `#tdMembersAll` 로 옮겼다. 구 id 는 사라졌다.
- 새 숫자 칸을 만들면 **`TD_CARDS` 배열에도 넣는다** — 조회 실패 화면에서 같이 '–'로 비운다.
- 구 `.tb-stat.zero { opacity:.55 }` 는 칸 전체를 흐리게 해 글자 대비까지 무너뜨렸다(원칙 4) →
  숫자 색만 흐리게.
- 검증(실측): 320·393·1280px 전 탭 — 12px 미만 활자 0 · 44px 미만 0 · 겹침 0 · 넘침 0 · 콘솔 0.
  0건·'–' 상태에서 강조가 풀리는 것까지 확인.

### 회원 3천 명 대비 — 1000행에서 조용히 잘리던 것 (2026-08-03 · 오너 "2~3천명이 되어도 문제 없나")

**Supabase 는 한 번에 최대 1000행만 돌려주고, 넘으면 아무 말 없이 잘라 준다.** admin 은 그 배열로
숫자를 세기 때문에 잘리는 순간 **화면 전체가 같이 거짓말을 한다.** 실측(목 3,000명·1,500건):

| | 전 | 후 |
|---|---|---|
| 회원 조회 | 3,000명 중 **1,000명** | 3,000명 |
| '전체 회원' 숫자 | 1,000 | 3,000 |
| 1000명 밖 회원의 신청 배지 | 전부 '비회원'(거짓) | 정상 |
| 크레딧 탭 여는 시간 | **968ms**(DOM 32,883) | **32ms**(DOM 3,886) |

- **`fetchAll(build)`** 하나로 모았다 — `range()` 로 1000행씩 끝까지 받는다.
  적용: `members` · `applications` · `challenge_waitlist` · `reviews` · `credit_ledger` · `lab_downloads`.
  ⚠️ **`build` 는 매번 새 쿼리를 만들어 돌려주는 함수**여야 한다(supabase-js 쿼리 객체는 재사용 불가).
  ⚠️ `FETCH_CAP`(20,000)을 넘기면 `truncated:true` 를 돌려준다 — **부르는 쪽이 화면에 사실대로 적는다.**
  회원·신청 목록에 빨간 한 줄로 나온다. 조용히 자르는 코드를 다시 만들지 말 것.
- **목록은 끊어 그린다**: 회원·크레딧 `M_PAGE=100`, 신청 카드 `A_PAGE=50`, 연구실 열람 이력 `LAB_LOG_PAGE=20` + '더 보기'.
  ⚠️ **계산은 전량으로 한다** — 건수·중복 검사·CSV·'오늘' 숫자는 `members`/`_apps` 전체를 그대로 쓴다.
  화면만 끊는 것이지 데이터를 끊는 게 아니다.
  ⚠️ 조건(검색·필터)이 바뀌면 표시 수를 `_mShown`/`_aShown`/`_credShown` 처음으로 되돌린다 —
  안 하면 결과 3건짜리 화면에 '더 보기'가 남는다.
  ⚠️ `_credShown` 은 `renderCredList` **위에서** 선언한다(아래에 두면 초기 로드가 TDZ 로 터진다).
- **'전체 선택'은 화면에 보이는 것이 아니라 조건에 맞는 전원(`visibleIds`)** 이다. 더 보기를 안 눌러도
  일괄 배정은 전부에 걸린다 — 그래서 목록 꼬리에 '몇 명 중 몇 명'을 같이 적는다.
- ⚠️ **다음 단계는 서버 검색이다.** 지금은 '전량 받아 브라우저에서 거르기'라 2만 행이 상한이다.
  그 위로 가면 검색·필터를 `ilike`·`range` 로 서버에 넘기고 `count` 로 총계를 따로 받아야 한다.

### 회원 상태 배지·필터 (2026-08-03 · 오너 "번호 없는 사람·이상한 사람을 갈라 놓자")

회원 목록에 이름·이메일·기수만 있어서 **누가 연락 가능한 사람인지 구분할 방법이 없었다.**
행에 상태 배지를 붙이고 상태 필터 칩 6개를 뒀다(구 '미배정만' 체크박스는 '기수 미배정' 칩이 대신한다).

| 필터 | 뜻 |
|---|---|
| 가입 미완료 | 번호 없음 — 로그인은 했고 이름·번호 넣는 화면에서 나간 사람 |
| 번호 확인 필요 | 번호는 있는데 형식이 안 맞음(유선·자릿수 오타) |
| 기수 미배정 | `cohort_id` 없음 |
| 신청 있음 | 챌린지·특강 신청 이력이 있음 |
| 흔적 없음 | 번호 없음 + 신청 0 + 크레딧 사용 0 + 가입 30일 경과 |

- ⚠️ **번호 판정은 `phone-check.js` 를 그대로 쓴다**(admin 에 스크립트를 추가했다).
  여기서 정규식을 따로 만들면 "가입은 통과했는데 admin 만 이상하다고 하는" 번호가 생긴다.
- ⚠️ **'흔적 없음'은 보수적으로 센다.** 신청·크레딧 원장을 아직 못 읽었으면 **아무도** 흔적 없음으로
  치지 않고 머리글에 '확인 중'을 띄운다. 모르는 것을 '없다'로 말하지 않는다(원칙 11).
  회원 목록은 초기화 순서상 신청·크레딧보다 먼저 그려지므로 **`loadCredits()` 끝에서 다시 그린다.**
- ⚠️ **삭제 버튼을 만들지 않았다.** `members` 행만 지우면 `auth.users` 가 남고, 회원 행은
  **첫 로그인 때 한 번만**(`on_auth_user_created`, after insert) 생기므로 그 사람이 다시 로그인해도
  프로필이 복구되지 않는다. 제대로 지우려면 `auth.users` 를 지워야 하고 그러면 `on delete cascade` 로
  크레딧·답변·세션·이용권까지 함께 사라진다. 접이 안내에 이 사실을 적어 두었다 — **지우는 곳은 콘솔이다.**
- ⚠️ **성능**: 신청 건수를 회원마다 `_apps` 를 훑어 세면 3,000 × 1,500 = 450만 번이라 렌더 한 번에
  **2,333ms** 가 걸렸다(실측). 검색은 글자마다 렌더하므로 그대로는 못 쓴다.
  `buildAppIndex()` 로 렌더 시작에 색인을 한 번 만들어 **16ms** 로 내렸다.
  ⚠️ 색인은 `member_id` 가 있으면 그쪽에만 센다 — 두 map 을 더하면 같은 신청이 두 번 세어진다.
- `credit_ledger` 도 `fetchAll` 로 옮겼다 — 무료 사용 한 번마다 한 줄이라 **가장 빨리 1000행을 넘긴다**
  (잘리면 잔액이 틀린다).

### 조용히 실패하던 자리 정리 (2026-08-03)

5차 진단에서 남겨 뒀던 '오너가 모르고 지나가는 것'들을 마저 고쳤다. 전부 같은 성격이다 —
**실패했는데 실패했다고 말하지 않거나, 되돌릴 수 없는 일을 확인 없이 하거나.**

- **error 를 아예 안 받던 토글 3곳**: 연구원 활성/비활성 · 연구원 지정 해제 · 수정 코드 켜기/끄기.
  실패해도 목록만 다시 그려서 "눌렀는데 그대로"가 됐다. 이제 실패는 alert, 성공은 toast.
- **이용권 지급 · 연구원 지정에 확인을 붙였다.** 회원 검색 결과를 **누르는 순간 바로 실행**되고 있었다.
  이용권은 유료 상품이라 옆 이름을 잘못 누르면 하나가 그냥 나가고, 연구원 지정은
  **검수 데스크에서 학생 이름·경험 카드를 보게 되는** 개인정보 접근 권한 부여다.
  ⚠️ 확인 문구에 **누구에게 무엇을** 을 적는다 — `apMemberHits` 가 `onPick(id, who)` 로 이름을 같이 넘긴다.
- **미션을 '미정'으로 되돌리면 코멘트도 같이 사라진다**(기록 행 자체를 지우므로).
  전에는 말없이 지우고 '저장됨'만 띄웠고 **화면엔 코멘트가 그대로 남아** 있었다.
  이제 코멘트가 있을 때만 확인을 묻고, 지운 뒤 입력칸도 비운다. 삭제 error 도 받는다.
- **크레딧 조회 실패를 전부 '마이그레이션 미적용'으로 단정**하던 것 → 테이블 없음(`PGRST205`)일 때만
  SQL 안내를 하고, 나머지는 실제 오류 메시지를 보여 준다. 네트워크 오류에 "SQL 을 실행하세요"라고
  하면 엉뚱한 곳을 고치게 된다.
- **빈 상태를 `.loading` 으로 그리던 곳** 2곳 → `.empty`. 로딩과 구분이 안 됐다.

### 문구·표기 다듬기 (2026-08-03)

- **'환급' → '환불' 통일.** 같은 `refunded` 값을 신청자 현황은 '환급', 간편결제 버튼은 '환불'로
  부르고 있었다. **'환급'은 2026-07-20 에 폐지된 보증금 제도의 말이라 지금은 뜻이 없다.**
  토글·요약 배지·CSV 머리글·크레딧 원장 사유까지 전부 '환불'.
- **환불계좌 줄은 값이 있을 때만 보여준다.** 지금 어느 신청 폼도 `refund_account` 를 받지 않는다
  (보증금 폐지 잔재). 늘 '미입력'만 뜨는 죽은 줄이었다 — 구 데이터에 값이 있는 건만 나온다.
- **날짜 표기를 `fmtDate()` 하나로.** 같은 성격의 값이 `8/2 18:12` 와 `2026-08-02` 로 갈려 있었다.
  이제 **오늘 18:12 / 어제 18:12 / 8월 1일 / 2025. 8. 1.** 넷 중 하나로만 나온다.
  ⚠️ **CSV·파일명은 그대로 ISO** 를 쓴다(엑셀 정렬·기계가 읽는 값).
  ⚠️ 값이 없으면 빈 칸이 아니라 **'미상'** 이다 — 빈 칸은 '없음'처럼 읽힌다.
- **합격 수기 편집 폼 8칸에 라벨을 달았다.** placeholder 뿐이라 값을 채우면 `2`·`0`·날짜만 남아
  어느 칸이 무엇인지 알 수 없었다(원칙 15). ⚠️ `title` 속성은 아이폰에서 안 뜨므로 라벨 대용 금지.
  '기수'는 **'몬크 기수'** 로 적는다(합격 항공사 옆이라 '입사 기수'로 읽힌다).
- **'날짜별 미션(1~14일)'을 접이로.** 회원 상세 4,416px 중 **2,531px(57%)** 를 먹어 답변노트·음성이
  한참 아래로 밀려 있었다. 출석은 카톡 오픈챗으로 운영해 대부분 비어 있다.
  ⚠️ **기록이 하나라도 있으면 펴진 채로 시작한다** — 쓰는 회원에게는 달라지는 게 없다.
  접힌 상태에서도 머리에 `성공 3 · 아쉬움 1 · 미정 10` / `기록 없음` 이 보인다.
  실측: 기록 없는 회원 상세 4,416px → **1,971px**.

### 후기 검색 · 크레딧 탭 순서 (2026-08-03)

**후기 관리에 검색을 붙였다.** 종류 필터뿐이라 한 건을 고치려면 화면을 계속 내려야 했다.
- 검색 대상: 이름(`reviewer_name`) · 한줄평 · 라벨 · 챌린지 · 기수.
  ⚠️ 이름 컬럼은 **`reviewer_name`** 이다 — `name` 으로 읽어 이름이 한 번도 안 붙었던 전례가 있다.
- 카드는 `R_PAGE=60` 씩 그리고 '더 보기'. **건수·챌린지별 요약은 전량으로 센다.**
- ⚠️ **검색·더 보기·종류 필터는 다시 조회하지 않는다**(`loadReviewsAdmin(true)` — `_rvRows` 캐시).
  검색은 글자마다 부르므로 매번 DB 를 치면 후기 수백 장을 반복해 받는다.
  **저장·삭제·업로드 뒤에는 인자 없이** 불러 새로 받는다 — 이 구분을 지우지 말 것.
- 실측(목 400장): 화면 60장 · 검색 3글자 32ms · **그동안 DB 조회 0회** · DOM 4,821.

**크레딧 탭을 '자주 쓰는 것 먼저'로 재배치했다.** 전에는 *"웬만하면 건드리지 말 것"* 인
단가 설정 7칸이 맨 위였고 매번 쓰는 회원 지급 목록이 그 아래 스크롤에 있었다.
- 새 순서: 짧은 안내 → **회원 검색 + 지급 목록** → 기수 일괄 지급 → 최근 원장 → *설정(접이)*.
- 설정 두 벌(무료 제공량·단가 / 충전 상품)은 맨 아래 `.adm-help` 접이로. 요약에
  '웬만하면 그대로 두세요'를 적었다.
- ⚠️ **접혀 있어도 값은 로드된다**(`loadCreditConfig`·`loadCreditPacks` 가 id 로 접근). 확인함.
- 실측: 검색칸 y=161px·목록 y=227px(첫 화면 안), 설정은 y=1,713px.

### 폐기된 오렌지 잔재 (2026-08-03)
`#f2c9b8`(구 오렌지 팔레트의 살구색)이 3곳에 남아 네이비 틴트 배경 위에 살구 테두리를 그리고 있었다
— 회원 목록 '선택됨' 표시(`.mitem.active`)와 상태 배지 둘. `var(--action)`·`var(--accent-ink)` 로 교체.
실측: 살구 테두리 0곳.

### 진단 잔여분 마무리 (2026-08-03)
확정 106건을 주제 57개로 묶어 대조한 뒤, 남아 있던 것들을 마저 처리했다.

- **`.btn` 기본값을 고스트로.** 변형(`.btn-action`/`.btn-ghost`)을 안 붙이면 배경도 테두리도 없어
  **브라우저 기본 회색 버튼**이 나왔다 — 오픈 알림 툴바의 '새로고침'·'CSV'가 그랬다.
  ⚠️ 자리마다 고치지 말고 기본값으로 막는다. 새 버튼이 또 민짜로 나올 수 없다.
- **오픈 알림 툴바 정리** — 건수를 왼쪽으로 밀고 버튼 둘을 오른쪽에(신청자 툴바와 같은 문법).
  'CSV 내려받기'만 혼자 다음 줄로 떨어지던 것이 사라진다.
- **끝난 행·숨긴 행을 `opacity:.6` 으로 흐리게 하던 것 2곳 → `.is-done`/`.is-off`.**
  ⚠️ opacity 는 **되돌리는 버튼까지** 같이 안 읽히게 만든다(원칙 4). 바탕만 눕히고 글자 대비는 둔다.
- **'오늘 · 최근 신청' 행에 키보드 접근** — `role="button"`+`tabindex`+**Enter/Space 위임**까지 한 벌.
  ⚠️ role·tabindex 만 붙이면 포커스는 가는데 안 열린다.
- **후기 카드 메타 5칸 전부 라벨**(종류·챌린지·기수·이름·한줄평)과 **글 후기 '받은 날짜'** 라벨.
  ⚠️ `title` 은 아이폰에서 안 뜬다 — 라벨 대용으로 쓰지 말 것.
- **행 버튼 간격 데스크톱도 6→8px**(원칙 2). 이 줄엔 '삭제'가 끼어 있다.
- **'삭제됐어요' → '삭제됨'** 통일.

**오탐으로 판정한 것**: `.lf-slot` 6열 그리드가 '삭제' 버튼을 삼킨다는 지적 — 실측 결과
데스크톱 818px 에 6열이 다 들어가고(넘침 0), 393px 에선 2열로 접힌다. 이미 `min-width:0` 정비가
끝난 자리다.

### 진입 조회 33회 → 9회 — 탭을 열 때 받는다 (2026-08-03 · 오너 "조회 28번 착수해라")
회원 3,000 · 신청 1,500 기준 **진입 한 번에 테이블 조회 33회**(측정치가 28→33 으로 는 것은
회원·신청·원장의 `range()` 페이지 수가 회원 수에 비례하기 때문이다). 그중 첫 화면('오늘')에
실제로 필요한 것은 **9회**뿐이고 나머지는 그 세션에서 한 번도 안 열 수 있는 탭 몫이었다.

**한 일**: 부팅은 `loadCohorts → loadMembers → loadApplications → loadWaitlist → loadToday` 만.
나머지는 `TAB_DEPS`/`TAB_LOADERS` 표 + `ensureTab(name)` 로 옮겨 **탭을 처음 열 때** 받는다.
`initTabs` 의 클릭 처리 끝에서 부른다.

```
전체 12탭을 다 열었을 때의 합계는 33회로 같다 — 없애는 게 아니라 **미루는 것**이다.
```

- **묶음 단위는 탭 이름이 아니라 데이터 이름**이다(로더끼리 배열을 공유하므로).
  회원 관리는 `credits` 를 같이 받는다 — 크레딧 원장이 있어야 '흔적 없음'을 판정한다.
- **실패하면 `_tabLoaded` 표시를 되돌린다** — 다시 눌렀을 때 재시도돼야 한다.
- ⚠️ **새 로더를 만들면 부팅 목록이 아니라 `TAB_DEPS`/`TAB_LOADERS` 에 넣는다.**
- ⚠️ 근거가 아직 없을 때의 표시를 자리마다 정했다: '흔적 없음' 칩을 원장 도착 전에 누르면
  전에는 "회원이 없습니다"(=지울 게 없다로 읽힘)였다 → **"판단할 자료를 받는 중입니다…"**.

### 회원 보관(archive) (2026-08-03 · 오너 "회원 보관기능 착수해라")
연락이 안 되는 계정을 **지우지 않고 옆으로 치운다.** 마이그레이션 `20260803120000_member_archive.sql`
(`members.archived_at`). 새 RLS 정책은 필요 없다 — 기존 `members_admin_all` 이 덮는다.

**왜 삭제가 아닌가**: `members` 행만 지우면 `auth.users` 계정이 남는다. `handle_new_user` 는
`after insert on auth.users` 라 **첫 로그인 때 한 번만** 회원 행을 만든다 — 그 사람이 다시
로그인해도 프로필이 안 생겨 사이트가 통째로 고장 나고, 관리자가 되돌릴 방법도 없다.

- 정리 동선: **[흔적 없음] → [전체] 체크 → [보관]**. 되돌리기는 [보관함] → [보관 해제].
- 보관한 회원은 **'보관함' 칩에서만** 보인다(전체 포함 다른 칩에서 빠진다). 회원 수(`#mCount`),
  '오늘'의 전체 회원, 크레딧 명단, '흔적 없음' 건수에서 모두 빠진다 — 화면마다 기준이 같아야 한다.
- **오너·관리자는 보관 대상에서 제외**(운영 계정이 목록에서 사라지면 권한 확인이 막힌다).
- 칩을 바꾸면 선택을 비운다 — 안 그러면 '전체'에서 고른 사람이 [보관 해제] 대상에 몰래 섞인다.
- 마이그레이션 미적용이면 조용히 degrade: 아무도 보관 상태가 아니고, 칩 숫자는 비고,
  버튼을 누르면 **실행할 파일명을 그대로 말한다**(`PGRST204` 판정).

### 연구원 검수 건수 (2026-08-03)
마이그레이션 `20260803130000_ap_reviewed_at.sql` — `answer_sessions.reviewed_at` + 도장 트리거.
연구원 목록에 `검수 N건 · 이번 달 M건 · 지금 잡고 있는 것 K건`.

- ⚠️ **상태 전이 심판(`ap_session_guard`)은 건드리지 않는다.** 돈이 걸린 판정이라 다시 쓰다
  한 줄만 어긋나도 유료 상품이 멈춘다. 도장만 찍는 트리거를 따로 달고, 이름을
  `trg_ap_stamp_reviewed_at` 으로 지어 **알파벳 순서상 심판 뒤에** 돌게 했다.
- '결과를 낸 것'만 센다(`approved`/`finalized`/`revision_requested`). 잡아만 둔 것은 따로 표시.
- ⚠️ `updated_at` 으로 이번 달을 세지 않는다 — 학생이 나중에 글을 고치면 엉뚱한 달로 옮겨간다.
  컬럼이 없는 환경에서는 **'이번 달' 줄을 아예 안 쓴다**(원칙 11).
- ⚠️ 컬럼 나열 select 는 미적용 환경에서 400 이라 **`reviewed_at` 빼고 한 번 더** 부른다.
  `select('*')` 로 도망가지 않는다 — 초안·문답 jsonb 까지 딸려와 전송량이 크게 는다.

### 상태색 한 벌로 (2026-08-03 · 오너 "코랄색도 남아있는 게 있던데")
같은 뜻에 색이 여러 개였다 — 경고 글자 `#A33D14`·`#c0392b`·`#B23B2E` 셋, 삭제 `#B3261E`,
성공 `#1a7f37`·`#2E6E42` 둘. 결정적으로 **`var(--danger, …)` 를 두 곳에서 쓰는데 `--danger` 가
어디에도 정의돼 있지 않아** 폴백 색(`#C0392B` / `#B3261E`)이 그대로 칠해졌다.

admin `<style>` 맨 위 `:root` 에 세 가지만 남겼다 — `--st-alert`(경고·오류·손댈 것 남음) ·
`--st-danger`(되돌릴 수 없는 조작) · `--st-ok`(끝남). 배경 틴트 셋도 같이 변수화.

- `--st-ok` 는 `#1A7F37`→`#17703A`. 옛 값은 구 웜 페이퍼 위 **4.50**, 연녹색 배경 위 **4.52** 로
  기준선(4.5)에 딱 붙어 있었다. 지금 5.45/5.47. (alert 5.75 · danger 5.79)
- ⚠️ 이 색들은 팔레트 통일 대상이 아니다(의미색). 네이비로 바꾸지 말 것 — design-principles.md.
- ⚠️ 변수를 지우면 **에러 없이 글자만 사라진다.** 값은 `:root` 에서만 고친다.

### 흐린 행·경계선·넘침 마저 (2026-08-03)
- **`opacity` 로 목록 행을 통째 흐리게 하던 나머지 6곳 → `.is-off`** (소재 문제·감점 사전·
  연구실 자료·연구실 로그·기출 은행·수정 코드·연구원). 앞 배치에서 2곳만 고쳤었다.
  ⚠️ opacity 는 그 행의 **되돌리는 버튼까지** 같이 안 읽히게 만든다.
- **누를 수 있게 만든 것은 테두리도 3:1**: '오늘' 숫자 타일(`.tb-stat` 1.19:1)과 최근 신청 행
  (role=button, 1.19:1), 모바일 '메뉴' 버튼(`.mt-menu` 1.37:1). 폰에서 '메뉴'는 다른 탭으로
  가는 **유일한 문**이라 여기가 흐리면 길이 막힌다. `.tb-stat.todo` 의 반투명 테두리(2.05:1)도 불투명으로.
- **상단 로고 링크 20px → 44px 표적**(로고 크기는 그대로, 여백으로).
- **후기 카드 375px 넘침 3곳** — 앞 배치에서 칸마다 이름표를 붙인 뒤 한 줄에 안 들어가
  카드 밖으로 글자가 나갔다(실측 231px > 148px).
  ⚠️ 원인은 `min-width`. **`min-width` 는 하한이지 상한이 아니다** — 입력칸에 `min-width:96px` 를
  주면 `<input size=20>` 의 기본 폭(약 177px)이 최소폭으로 그대로 남아 더 크게 삐져나온다.
  정답은 입력칸 `min-width:0` + 라벨 `min-width:0` + 행 `flex-wrap:wrap`, 그리고 두 칸이 한 줄에
  못 들어가는 챌린지+기수는 **아예 세로로**(`.rv-cls { flex-direction: column }`).
- 파일 고르기 칸 3곳에 `aria-label`.

**측정 브라우저 함정**: 이 컨테이너의 크로미움은 `<input type=file>` 의 `::file-selector-button`
을 **16px 빈 회색 사각형**으로 그린다(민짜 input 도 똑같다 — 확인함). 화면에 '파일 선택'이
안 보인다고 admin 버그로 판정하지 말 것.

### 안 고치고 남긴 것(판단)
- **환급/환불 용어 혼용**(같은 `refunded` 값을 탭마다 다르게 부른다), 날짜 표기 4종 —
  전수 치환이라 그 파일을 손볼 때 함께.

### 검사 방법(5차 · node 없는 맥에서)
playwright 가 없어 **브라우저 패널 + `javascript_tool`** 로 쟀다.
스크래치패드에 레포 미러를 만들고 `supabase-config.js` 뒤에 목(`mock-monc.js`)을 끼운
`admin-audit.html` 사본을 서빙한다(**레포에 커밋하지 않는다**). 탭 12개를 눌러 가며
활자·입력칸·표적·간격·겹침·넘침·경계선 대비를 센다. 320/393/1280 × iOS date 폭 주입 모드.
- ⚠️ **목 데이터는 분기를 정확히 만들어야 한다.** 처음에 Proxy 로 "모든 키에 값"을 준 탓에
  `a.payment_id` 가 항상 truthy 가 되어 **입금·환급 토글이 한 번도 안 그려졌다** —
  화면에 삭제 버튼만 남아 엉뚱한 진단을 할 뻔했다. 분기 컬럼은 명시적으로 null 을 넣는다.
- ⚠️ **겹침 검사는 스크롤 조상으로 잘라야 한다.** `.mlist`(max-height:60vh·overflow:auto) 안의
  항목은 잘려서 안 보여도 좌표는 그대로라, 아래 '기수 추가' 칸과 겹친 것으로 잘못 잡혔다.


> 2026-07-30 CLAUDE.md 다이어트로 이관한 기능별 상세 기록·의사결정 원장이다.
> 매 작업 공통 규칙은 CLAUDE.md 에 있고, 이 문서는 해당 기능을 고칠 때 읽는다.
> 본문 속 '위/아래 ○○ 절 참조'는 구 CLAUDE.md 기준 표현이라, 그 절은 docs/notes/ 의 다른 문서에 있을 수 있다.

### 모바일 네비 = 상단 바 + 바텀시트 (2026-08-01 · 오너 "네비를 크게 바꾸는거 해봐")
칩 1줄 가로 스크롤(2026-07-31)을 폐기하고 **상단 바(.mtop: 로고·관리자 배지·현재 화면 이름 + '메뉴' 버튼) + 바텀시트**로 교체했다. 칩 12개 가로 스크롤은 그룹 경계가 안 보이고 뒤쪽 탭이 눈에 안 띄는 문제가 컸다. 시트는 운영/상품/콘텐츠 3그룹 12개를 2열 그리드로 한 화면에 보여준다(뉴스 필터 바텀시트와 같은 문법). 아이콘도 시트에서 유지 — 데스크톱과 같은 지도.
- **전환 규약 불변**: 시트 안 버튼도 데스크톱 사이드바와 같은 `.tabbtn` 그대로 — initTabs·activateTab 규약이 그대로 먹는다. initTabs 가 클릭 시 `#mtCur`(상단 바 제목) 동기화 + `navSheet(false)` 닫기를 같이 한다.
- **열림 제어는 `body.navopen` 하나**: 메뉴 버튼 토글·스크림(.mscrim) 탭·Esc·✕·항목 선택 전부 `navSheet()` 경유. 열려 있는 동안 body overflow:hidden. z-index: mtop 9 · 스크림 39 · 시트 40 · 토스트 50.
- ⚠️ **#tabs 인라인 display 는 '비운다'** — revealAdmin 이 `style.display = ''` 로 지우고, 레이아웃(데스크톱 flex 세로 / 모바일 grid 시트)은 CSS 가 정한다. 인라인에 'flex'/'block' 을 넣으면 모바일 시트 그리드가 죽는다. **2026-07-31 절의 "인라인 'flex'" 규칙은 이 개편으로 폐기.**
- 상단 고정부는 한 줄(약 62px) 유지 — "wrap 금지" 실측 교훈 그대로. 시트에서 `.sn-logo` 숨김(로고는 상단 바가 가짐), `.sn-me`(이름+로그아웃)는 시트 맨 아래 — 구 칩 줄에선 로그아웃이 스크롤 끝에 숨어 있었다.
- 검증(375px 실측): 열기/닫기(버튼·스크림·Esc) · 항목 선택 → 패널 전환+제목 동기화+시트 닫힘 · '오늘' data-goto 경유 activateTab 정상 · 가로 넘침 0 · 콘솔 에러 0 · 데스크톱 사이드바 회귀 없음(mtop·시트머리 display:none).

### 후기 탭 — 챌린지별 건수 요약(`#rvChSum`) (2026-08-02)
챌린지 상세(`challenge-*.html`)의 후기 섹션은 `reviews.challenge` 로 골라 담는데, **'여러 장 한번에' 업로드는 이 값을 안 채운다.** 그러면 상세 페이지는 섹션을 `hidden` 으로 두고 조용히 넘어가서, 어드민만 보고 있으면 "후기를 올렸는데 왜 안 뜨지"가 된다. 그래서 종류 필터 바로 아래에 **노출(visible)·챌린지 지정된 건수를 챌린지별로** 찍는다 — 0이면 그 상세엔 섹션이 없다는 뜻. 미지정 건수는 빨간 글씨로 따로 센다.
- **집계 기준은 `challenge-reviews.js` 의 조회 조건과 같아야 한다**: `kind==='challenge'`(null 은 challenge 로 간주) + `visible` + `challenge` 일치. 한쪽만 고치면 숫자가 거짓말을 한다.
- 챌린지 이름 네 글자는 `RV_CH_OPTS` 상수 하나로 모았다. **이 문자열은 각 상세의 `data-challenge` 와 정확히 같아야 한다**(현재 `보신각`·`영합각`·`스피닝`·`승자각`). 어느 한쪽 이름을 바꾸면 매칭이 조용히 끊긴다.
- 종류·챌린지 드롭다운을 바꾸면 **행만 메모리에서 갱신하고 요약만 다시 계산**한다(`patchRow`). 통째로 `loadReviewsAdmin()` 하면 스크롤이 맨 위로 튄다.
- 검증(375px 실측): 보신각 2·영합각 0·스피닝 0(숨김 1건 제외)·승자각 1·미지정 2 — 상담 후기와 숨김 후기가 안 세어지는 것까지 확인. 챌린지 변경 → 영합각 0→1·미지정 2→1, 종류 변경 → 승자각 1→0, 가로 넘침 0, 스크롤 유지, 콘솔 에러 0.

### 목록 행은 모바일에서 1열로 — 버튼 줄은 `.ri-actions` 로만 (2026-08-01 실사고 · 오너 "이런식으로밖에 못하냐")
`.round-item` 은 '본문 1fr + 버튼 auto' 2열 그리드다. **연구실 자료 행은 버튼이 6개**(수정·이력·가격·비밀번호·숨기기·삭제)라 375px 에서 버튼 열이 폭을 다 먹고 **제목이 한 글자씩 세로로 꺾였다.** 고침 두 가지:
- 모바일 보정 블록에 `.round-item { grid-template-columns: 1fr }` — 버튼 줄(과 상태 칩)이 본문 아래로 내려온다. 버튼 정렬은 데스크톱 오른쪽 / 모바일 왼쪽(`.ri-actions { justify-content }`).
- **행 버튼 묶음은 전부 `class="ri-actions"`** 로 통일(10곳). 인라인 `style="display:flex;…"` 로 만들면 모바일 규칙이 못 잡는다 — **행 버튼 컨테이너에 인라인 flex 금지.**
⚠️ **왜 첫 정비(아래 절)에서 못 잡았나: 스크린샷 탭 목록이 8개뿐이었다**(labfiles·waitlist·terms·questions 누락). 검증은 반드시 **`data-tab` 전부**(2026-08-06 실측 13개 — 탭이 늘면 이 숫자도 같이 고친다) 돌릴 것 — 그리고 목 데이터가 빈 목록이면 행 레이아웃 버그는 안 보인다. **행이 그려질 만큼 목 시드를 넣고 본다**(연구실 자료가 빈 배열이라 이 사고를 놓쳤다 — 후기도 같은 이유로 `reviews: []` 였고, 2026-08-02 요약 작업 때 채웠다).

### 모바일(≤960px) UX 정비 (2026-08-01 · 오너 "모바일 어드민 UXUI 가 웹에 비해 너무 떨어진다")
왜 떨어졌었나: 셸 개편(2026-07-31)이 데스크톱 목업 기반이었고 모바일은 '칩 1줄로 접는' 수준이었다. 검증 기준도 '375px 가로 넘침 0'이라 통과해 버렸다 — 공개 페이지의 375px 우선 규칙이 admin 엔 사실상 적용되지 않았던 것. 이번에 고친 것(스타일 맨 아래 `@media (max-width: 960px)` 보정 블록):
- **입력 활자 16px(모바일만)**: iOS 사파리는 16px 미만 입력을 누르면 화면을 강제 확대하고 되돌리지 않는다. 폼이 많은 admin 에서 가장 큰 손실이라 모바일만 16px. 데스크톱 밀도는 그대로.
- **액션 버튼 44px+(모바일만)**: 수정·삭제·입금·환급·후기 버튼 등. `.af-chip`(38px 합의값)·`.tabbtn`(기본에 이미 44px)은 그대로.
- **본문 좌우 여백 16px 통일**: rounds/apps/reviews-wrap·`.main` — '오늘' 탭(today-wrap)과 같은 기준.
- **신청자 툴바 줄바꿈**: 검색이 한 줄 전체, 건수·새로고침·CSV 가 아랫줄(CSV 만 외따로 떨어지던 것 정리).
- ~~**칩 줄 그룹 라벨 복원**~~ (같은 날 저녁 네비 개편으로 칩 줄 자체가 폐기 — 위 절 참조)
- **'왼쪽에서 회원을 선택하세요' → '목록에서'**: 모바일(≤820px)은 목록이 위에 있다 — 방향을 박는 문구 금지(마크업 주석 있음). 선택 시 상세로 스크롤은 기존 selectMember 에 이미 있다.
- ⚠️ **[hidden] 짝 규칙 — 전 화면 버그 수정**: `.round-editing`(CSS display:flex)·`#lrEditBar`(인라인 display:flex)가 hidden 속성을 이겨 **'취소하고 새로 추가' 빈 바가 데스크톱·모바일 모두 항상 떠 있었다**(챌린지·특강·연구실 자료 3곳). 고침: `.round-editing[hidden], #lrEditBar[hidden] { display:none }` + lrEditBar 인라인에서 display 제거. **display 를 직접 주는 요소에 hidden 을 쓰면 [hidden] 짝 규칙을 같이 두고, 인라인 display 는 금지**(인라인은 CSS 로 못 이긴다). 검증: 수정 클릭→바+라벨 표시, 취소→숨김 실측.
- 검증법: requireAdmin·MONC.sb 목 사본(admin-preview.html — 커밋 금지)으로 375px·1280px 전 탭 스크린샷, 가로 넘침 0·콘솔 에러 0 확인.

### ⚠️ 다른 페이지의 클래스를 admin 에 가져다 쓰지 말 것 (2026-08-01 실사고)
연구실 자료 탭의 서가 필터를 `news.html` 의 `.nw-chip`·`.nw-filter-row` 로 만들었는데 **admin.html 에는 그 CSS 가 없다.** 클래스만 붙고 스타일이 하나도 안 먹어 선택 표시조차 없는 민짜 버튼 6개가 나왔다(오너: "UXUI 뭐냐?"). admin 은 인라인 `<style>` 한 벌로만 그려지므로 **admin 안에 정의된 클래스만 쓴다.**
**필터 칩의 정본은 `.apq-filter > .apq-flabel + .af-row > .af-chip`**(신청자 현황·기출 은행이 쓰는 것). 개수 배지는 칩 안에 `<b>` — `#apqFilter b, #lrShelfBar b` 규칙을 공유한다(칩 그룹을 추가하면 이 선택자에 id 를 더한다). 선택은 `.on`(네이비 배경+흰 글씨), 비선택은 흰 배경. 실측: 높이 38px · 간격 6px.
**새 UI 를 붙이기 전에 `grep -c '\.클래스명' admin.html` 로 정의 유무부터 센다.**

### ⚠️ 탭을 추가할 때 — id 접두사가 이미 쓰이는지 먼저 센다 (2026-08-01 실사고)
admin.html 은 **탭 13개가 한 파일에 있고 id 가 전역이다**(2026-08-06 실측 — 채용 캘린더까지 늘었다). '연구실 자료' 탭을 `lf*`(lab file) 로 만들었다가 **특강 폼의 `lf*`(lecture form) 와 `lfTitle`·`lfAirline` 이 겹쳤다.** `getElementById` 가 먼저 나오는 특강 칸(빈 값)을 잡아, 제목을 멀쩡히 적어도 **"제목을 입력하세요"만 반복**됐다 — 오류도 안 나서 원인이 안 보인다. 자료 탭은 `lr*`(lab resource) 로 바꿔 해결.
**새 탭의 id 접두사를 정하면 `grep -o 'id="<접두사>[A-Za-z]*"' admin.html | sort -u` 로 충돌부터 확인할 것.** 현재 쓰는 접두사(2026-08-06 실측 — **탭을 추가하면 이 줄도 같이 고친다**): 오늘 `td`(숫자 스택)·`tb`(브리핑 문장) · 신청자 현황 `app`·`af`(필터 칩) · 오픈 알림 `wl` · 회원 관리 `m`·`bulk`·`sel`(상세 폼 입력칸만 `f`) · 크레딧 `cf`(무료 한도)·`cb`(일괄 지급)·`cp`(충전팩)·`cred`(원장) · 챌린지 `rf`(회차 폼)·`round`(회차 목록·편집)·`ch`(참가비) · 특강 `lf`·`lec` · 답변 프로그램 `ap`·`apq`(기출 은행)·`app`(프로그램 폼) · 후기 관리 `rv`·`st`(합격 수기) · 감점 사전 `tf`·`term` · 소재 문제 `qf`·`q`(필터) · 연구실 자료 `lr` · `lab`(등록 폼·목록은 `labForm`·`labList`, 다운로드 기록은 `labLog*`) · 채용 캘린더 `rc`·`recForm` · 탭 밖 셸 `mt`(모바일 상단 바)·`sn`(사이드바·바텀시트).

⚠️ **접두사가 없는 단독 id 도 전역이라 같이 피해야 한다**(2026-08-06 실측): `layout`·`search`·`mlist`·`detail`·`cohortName`·`addCohortBtn`·`emptyMain`(회원 관리) · `mscrim`·`tabs`·`main`·`loading`·`logoutBtn`·`toast`(셸). 새 탭에서 `search`·`detail` 같은 흔한 이름을 그냥 쓰면 바로 겹친다 — 접두사를 붙일 것.
⚠️ **`app` 과 `qf` 는 이미 두 탭에 걸쳐 있다** — `app*` 은 신청자 현황(`appSearch`·`appsBody`)과 답변 프로그램 폼(`appForm`·`appTitle`), `qf*` 는 소재 문제 폼(`qfContent`)과 감점 사전 아래 미니 다듬기 제보 집계(`qfAgg`·`qfRaw`). 지금은 뒷글자가 달라 안 겹치지만, **이 둘에 새 id 를 더할 땐 반대쪽 탭도 같이 grep 한다.**

### admin.html — 사이드바 셸 + '오늘' 브리핑 개편 (2026-07-31 · 오너 목업 ⑥ "① 페이퍼 데스크 × ⑤ 데일리 브리핑" 확정)
어드민 목업 6안 중 오너가 합본안을 확정("1안 골격 + 5안 신문 1면"). 2단 네비(아래 2026-07-30 절)를 **좌측 사이드바 셸**로 교체하고 '오늘' 탭을 **브리핑형**으로 재구성했다. 폰트는 사이트와 동일하게 SUIT 하나(오너 확인 — 목업의 명조는 시안용, 실구현은 `var(--serif)`=SUIT 800).
- **셸**: 데스크톱(>960px) = `body.authed .shell` 그리드(240px 사이드바 + 본문). ~~모바일(≤960px) = 셸이 block 으로 풀리고 사이드바가 **스티키 칩 1줄 가로 스크롤**~~ **(2026-08-01 폐기 — 모바일은 상단 바+바텀시트. 맨 위 절 참조.)** 그룹 구성(운영/상품/콘텐츠)과 **"탭 추가 시 그룹부터 정한다"** 규칙은 2026-07-30 확정안 그대로 — 사이드바에선 그룹이 라벨(`.sn-glabel`)이다.
- ~~**⚠️ `#tabs` 인라인 display 는 `'flex'`**~~ **(2026-08-01 폐기 — 지금은 revealAdmin 이 인라인 display 를 `''` 로 비우고 CSS 가 정한다. 맨 위 절 참조.)** `body.authed` 클래스가 데스크톱 2열 그리드를 켠다 — 지우면 사이드바 자리가 안 잡힌다.
- **탭 전환 규약은 그대로**: `.tabbtn[data-tab]` ↔ `#panel-*` + `activateTab()`. 그룹 줄(`.tgbtn`·`.tab-row`) 로직은 삭제됐다. ap 서브탭에 `.tabbtn` 금지 규칙 유지.
- **'오늘' 브리핑**: 헤드라인 문장·리드·'지금 처리' 브리프를 **전부 실측 숫자로만 생성**(오늘 신청·미입금·연락 전·검수 대기·새 회원 — 없는 사실을 지어내지 않는다). 숫자 스택은 우측 단(모바일에선 맨 위 — 오너 "모바일은 간결하게"), '지금 처리'는 입금→연락→검수 순(돈 먼저), 이름은 가장 오래 기다린 사람(`_apps` 최신순 정렬이라 마지막 원소). id(`td*`)·`tdSet()`·`loadToday()` 호출 시점(초기화 맨 끝) 규칙은 구 카드 시절 그대로다.
- **검증법**: 미러 서버에 requireAdmin·MONC.sb 를 목으로 바꾼 `admin-preview.html` 사본(커밋 금지)을 두고 확인했다 — 탭 11개 전환·회원/답변 프로그램 패널·모바일 375px(가로 넘침 0) 실측, 콘솔 에러 0.
- **⚠️ 진입은 '한 번에' 그린다(2026-07-31 후속 · 오너 신고 "깨지듯 뒤틀렸다가 로딩된다").** 개편 직후, 인증이 끝나기 전에 `panel-today` 가 **사이드바 없는 전체 폭**으로 먼저 그려졌다가 껍데기가 붙는 순간 레이아웃이 통째로 뒤틀렸다. 원인은 ① `.tab-panel.active` 가 처음부터 보이는 것 ② 껍데기 켜기(`revealAdmin()`)가 데이터 로드보다 앞에 있던 것. 고친 방식은 **짝 규칙 2개** — CSS `body:not(.authed) .tab-panel{display:none}` + 초기화의 `revealAdmin()` 을 **`loadToday()` 까지 끝난 뒤 `finally` 에서 한 번만** 호출(멱등, 8초 안전망 타이머 · 실패해도 켜서 흰 화면 방지). **둘 중 하나만 되돌려도 뒤틀림이 돌아온다.** 진입 로더는 `#loading`(스피너+중앙 정렬)이고, 내부 목록의 `.loading` 과 다른 규칙이니 섞지 말 것. 실측: 로딩 중 패널 폭 0 → 완성 후 단일 폭(중간 단계 0회), 데스크톱·375px 동일.
  - ⚠️ 대신 첫 화면까지 **조회 5개(기수→회원→신청→오픈알림→검수 count)를 순서대로** 기다린다. 신청 조회는 회원 전화 대조(`memberPhoneSet`)를 쓰므로 **`loadMembers()` 앞으로 병렬화하면 '회원/비회원' 배지가 틀리게 그려진다** — 순서를 바꾸지 말 것.

### admin.html — 2단 네비 개편 (2026-07-30 · 오너 "기능 나열만 있고 효율적인 동선이 아니다")
탭 11개 한 줄 나열을 **그룹 3개 × 2단 네비**로 재편. 1줄 = 그룹(언더라인 탭), 2줄 = 그 그룹의 화면(알약 칩) — 신청자 현황 종류 필터와 같은 문법.
- **그룹 구성(오너 사용 빈도 기준 — 매일 보는 것이 '운영')**: 운영(오늘·신청자 현황·오픈 알림·회원 관리·크레딧) / 상품(챌린지·특강·답변 프로그램) / 콘텐츠(후기 관리·감점 사전·소재 문제). ⚠️ **탭을 추가하면 어느 그룹인지부터 정할 것** — 맨 뒤에 이어붙이면 개편 전으로 돌아간다.
- **탭 이름 변경**: '모집일정'→'챌린지', '기출문제'→'소재 문제'(소재발굴 도구에 나가는 오늘의 문제 — 답변 프로그램의 '기출 은행'과 이름이 겹쳐 혼동되던 것). '홈 커뮤니티' 탭은 삭제(위 커뮤니티/후기 절 참조).
- **⚠️ 기출 은행의 자리는 '상품 > 답변 프로그램' 서브탭이 맞다(2026-07-30 오너 확정).** 개편 직후 오너가 "기출 넣고 세팅하는 게 콘텐츠에서 빠졌다"고 물었다 — 사라진 게 아니라 상품 탭 안에 있는 것. 콘텐츠 그룹 독립 탭으로 승격하는 안을 제시했으나 **오너가 지금 위치 유지를 선택**(기출 등록과 일차 배치가 한 화면이라 조립 작업이 편하다). 같은 질문이 또 나오면 위치 안내가 답이지 구조 변경이 아니다.
- **첫 화면 = '오늘' 대시보드**(`panel-today`): 입금 확인 필요(계좌이체 미입금·환불 제외) · 오픈 알림 연락 전 · 검수 대기(→review-desk.html 링크) · 오늘 신청 · 오늘 새 회원 + 최근 신청 5건. 숫자를 누르면 해당 탭으로(`activateTab()` — 탭 버튼 클릭 핸들러가 그룹 줄까지 동기화하므로 어디서 불러도 일관). ⚠️ **`loadToday()`는 초기화 시퀀스 맨 끝** — 카드가 `_apps`/`_wl`/`members`를 재사용해서 `loadApplications`/`loadWaitlist`를 `await`한 뒤에 돈다(추가 조회는 answer_sessions count 하나 — 미적용 환경은 try/catch로 0 표시).
- ~~**⚠️ `#tabs`를 켤 때 `style.display = 'block'`** — 구 `'flex'`를 그대로 두면 그룹 줄과 알약 줄이 **가로로 눕는다**(실측으로 잡은 회귀 — 인라인 스타일이라 CSS로 못 이긴다).~~ **(2026-07-31 폐기 — 사이드바 셸로 바뀌며 정반대가 됐다. 현행은 `'flex'`가 맞다. 위 2026-07-31 절 참조.)**
- **답변 프로그램 탭 안은 서브탭 6개**(`.ap-st`/`.ap-sec`: 기출 은행·프로그램·이용권·연구원·수정 코드·진행률) — 기출 89개 시점부터 6절 세로 나열이 한 스크롤에 안 담겨 분리. ⚠️ 서브탭에 `.tabbtn` 클래스를 쓰지 말 것(initTabs가 tabbtn 전부에 패널 전환을 건다). `.ap-sec[hidden]{display:none}` 가드 유지.
- **모바일(<760px)**: 알약 줄은 `nowrap + overflow-x` 1줄 스크롤 — wrap으로 두면 스티키 탭바가 150px(화면 1/4)를 먹는다(실측 116px로 축소). 터치는 전 버튼 44px+(9대 원칙 2).
- 검증법: 로그인 게이트라 프리뷰로 바로 못 본다 — `requireAdmin`을 목으로 바꾼 사본을 서버 루트에 두고 확인(이 파일은 커밋 금지). ⚠️ 프리뷰 서버 cwd가 워크트리일 수 있다 — repo 루트 파일을 고쳤는데 반영이 안 보이면 `preview_list`로 서빙 디렉토리부터 확인할 것.

## 채용 캘린더 탭 (2026-08-04 신설 · 콘텐츠 그룹 · `reccal`)

연구실 04 서가의 공채 회차(`recruit_rounds`)를 만드는 화면. **'연구실 자료' 안 서브탭이 아니라 옆 탭**이다 — 표가 다르고(파일이 아니라 날짜), 편집 모양(단계 반복 입력)도 겹치는 코드가 없고, 그 탭은 이미 가장 긴 화면이다.

- **단계 종류(`key`)는 '전형 이름'이 아니라 '역할' 9개**(apply·screen·video·interview·health·result·pass·intake·etc). 항공사마다 전형 방법·횟수가 달라서다(오너 지적 — 배경은 `lab.md` 04 절). **화면에 뜨는 글자는 단계의 '이름' 칸**이라 '2차 실무면접'·'AI역량검사'를 그대로 적는다. ⚠️ `RC_ROLES` 목록은 마이그레이션의 `recruit_stages_ok()` 와 **같은 어휘**다 — 여기만 늘리면 저장이 `23514`(check 위반)로 막힌다.
- **↑↓ 를 자동 정렬로 바꾸지 말 것.** `result`(발표)는 '바로 앞 단계의 결과'라 차례가 곧 뜻이다. 그런데 발표와 다음 전형이 **같은 날인 회차가 많아**(대한항공 3회차 전부) 날짜순 정렬로는 둘의 순서를 정할 수 없다.
- **'지난 회차 틀 가져오기'는 단계 종류·이름만 가져온다.** ⚠️ 날짜는 절대 안 가져온다 — 지난 회차 날짜가 새 회차에 남으면 그게 그대로 공개된다.
- **저장 전 계산 미리보기(`.rc-prev`)** — 총 단계·처음→마지막 날수·'발표 당일 다음 전형 시작' 배지를 저장 버튼 **위에** 보여준다(원칙 13 — 누른 뒤에 보이는 근거는 근거가 아니다). 차례가 어긋나면 경고를 같이 적되 저장은 막지 않는다(막는 것은 이름·시작일·종료<시작 셋뿐).
- ⚠️ **저장 버튼 글자는 `rcSyncLabel()` 로만 바꾼다.** `guardSubmit` 은 핸들러 **시작 시점의** 글자를 기억했다가 finally 에서 되돌린다 — 핸들러 안에서 `textContent` 를 직접 고치면 저장 직후 '수정 저장'이 그대로 남는다. 그래서 저장 성공 뒤 `setTimeout(rcSyncLabel, 0)` 이 필요하다. (연구실 자료 폼에 남아 있던 같은 함정도 2026-08-04 `labSyncLabel()` 로 같이 고쳤다 — 아래 '연구실 자료 탭' 절.)
- ⚠️ **값(가격) 칸을 만들지 말 것** — 무료 공개 확정(오너 2026-08-04).
- 실측(2026-08-04 · 검사대): 393px 가로 넘침 0 · 조작요소 전부 44px · 12px 미만 활자 0 · 1280px 가로 넘침 0. 목록·수정 진입은 실제 Supabase 자료(대한항공 3회차)로 확인했고, 비로그인 저장 시도는 RLS 가 막아 `권한이 없어요` 안내가 떴다(행 3개 그대로).
- **맨 위 제보함(`recruit_tips`)** — 학생 화면 아래 '일정 알려주기'로 들어온 글을 읽고 그 자리에서 회차를 고친다. ⚠️ **자동 반영 코드를 만들지 말 것**(제보는 확인 전 주장, 회차는 확인한 기록). 안 읽은 것이 없으면 조용한 회색 줄, 하나도 없으면 상자 자체를 감춘다. **표가 없어도(`PGRST205`) 조용히 접고 캘린더 탭은 그대로 뜬다.**
  - **'오늘' 브리핑의 '지금 처리'에도 한 줄 뜬다**(`status='new'` count). 이 줄이 없으면 제보함은 캘린더 탭을 열어야만 보이는 상자가 된다.
  - ⚠️ 제보 본문은 **비회원이 보낸 남의 글**이다 — 반드시 `esc()` 로 넣는다(`<img onerror>` 를 넣어도 글자로만 보이는 것을 실측했다).
  - 행 버튼은 `.btn-sm`(35px)이 아니라 **회차 목록과 같은 `.ri-edit`/`.ri-del`(44px)** 을 쓴다.
- **검증법**: admin 은 로그인 게이트라 프리뷰로 못 연다. `admin.html` 에서 **`<style>` + `#panel-reccal` 마크업 + `rc*` JS 블록만 떼어낸 검사대**를 스크래치패드 미러에 만들어 띄운다(공용 헬퍼 `esc`·`toast`·`fmtDay`·`guardSubmit`·`LAB_AIR_LABEL` 은 같은 값으로 복사). 목록 조회는 **anon 으로도 되므로 실자료가 그대로 뜬다**(회차는 공개 자료). 쓰기는 RLS 가 막아 프로덕션이 안 더러워진다. ⚠️ 검사대 `<body>` 에 `authed` 클래스를 줘야 한다 — `body:not(.authed) .tab-panel { display:none }` 때문에 안 주면 빈 화면이다.

## admin 모바일 UX 정비 2차 (2026-08-02 · 오너 "어드민 페이지도 uxui 신경쓰라고했지?")

오너가 아이폰으로 '연구실 자료' 탭을 열고 지적했다. 재보니 **admin 은 공개 페이지가 받은
UX 작업을 사실상 못 받은 상태**였다. 393px 실측(12개 탭 전수).

| | 전 | 후 |
|---|---|---|
| 16px 미만 입력칸(iOS 확대) | **26개** | 0 |
| 44px 미만 조작요소 | **102개** | 0 |
| 24px 미만 체크박스(WCAG 2.5.8 AA) | 6종 | 0 |
| 첫 화면을 막는 안내문(연구실 자료) | 312px | 0(접힘) |

### ⚠️ 제일 뼈아픈 것 — 규칙은 있었는데 한 번도 안 먹었다
`@media(max-width:960px){ .smain input… { font-size:16px } }` 는 **2026-08-01 에 이미 넣었다.**
그런데 admin 의 입력칸 24개가 `style="…font-size:14px"` **인라인**을 달고 있어 클래스 규칙이
전부 졌다. 즉 "고쳤다"고 기록만 남고 화면은 그대로였다.
- 그래서 지금은 **`!important`** 를 붙였다(인라인을 이기는 유일한 길). 지우지 말 것.
- **교훈**: CSS 규칙을 넣었으면 **브라우저로 세어 본다.** '썼다'와 '먹는다'는 다르다.
  검사 스크립트는 탭을 하나씩 눌러 가며 `getComputedStyle` 로 개수를 센다.

### 무엇을 고쳤나
- **모바일 공통(≤960px)**: `.smain .btn`(btn-sm 포함)·`select`·`.af-chip`·`.lf-slot-add`·
  파일/날짜 입력 **44px**, 텍스트·숫자 입력 **44px**, 체크박스·라디오 **24px**, `.tb-more` 44px.
  구 규칙은 `.ri-edit/.ri-del/.ac-tg/.rv-btn` 같은 **행 버튼만** 덮어서 정작 저장·추가·CSV
  같은 주 버튼과 모든 select 가 36~38px 로 남아 있었다.
- **`.adm-help` 접이 안내 신설** — 긴 설명은 이 부품으로 옮긴다(탭마다 새 스타일 만들지 말 것).
  적용: 연구실 자료 312px · 감점 사전 166px · 후기 관리 145px → 전부 44px 요약 줄 하나.
  ⚠️ **후기 탭의 개인정보 경고(스크린샷에 이름·번호가 찍혀 있으면 그대로 보임)는 접지 않았다** —
  올리기 전에 눈에 띄어야 하는 경고다. 접이로 옮기지 말 것.
  ⚠️ 감점 사전은 가장 중요한 한 줄("글에 그대로 나오는 문자열로")을 summary 에 남겼다.

### 연구실 자료 탭 — 저장 버튼 글자는 `labSyncLabel()` 로만 바꾼다 (2026-08-04)
자료를 **수정**하고 저장하면 폼은 '새로 올리기'로 돌아가는데 버튼만 '수정 저장'으로 남아 있었다.
`guardSubmit` 이 핸들러 **시작 시점의** 글자('수정 저장')를 기억했다가 `finally` 에서 되씌우는데,
`labEditCancel()` 은 핸들러 **안에서** 글자를 '자료 올리기'로 바꾸기 때문이다 — 순서상 항상 진다.
- 글자는 `_labEditId` 에서 **다시 계산**한다: `labSyncLabel()`. `labEditStart()`·`labEditCancel()` 은
  `textContent` 를 직접 쓰지 않고 이 함수를 부른다.
- 제출 핸들러는 `labEditCancel()` 뒤에 **`setTimeout(labSyncLabel, 0)`** 을 둔다(finally 가 그 사이에 돈다).
- 채용 캘린더의 `rcSyncLabel()` 과 같은 처리다. **같은 폼 재사용 화면을 새로 만들면 이 한 쌍을 같이 넣는다.**
- 검증(2026-08-04 · 검사대 8건 전부 통과): `admin.html` 에서 `guardSubmit` + 라벨 블록을 **슬라이스해**
  스크래치패드에 띄우고 수정 진입 → 저장 → 취소 → 삭제 경로의 글자를 실측했다. ⚠️ 옛 코드를 재현한
  **대조군을 같이 넣어** 검사대가 실제로 이 버그를 잡는지부터 확인할 것(대조군이 통과하면 검사가 헛돈 것이다).

### 연구실 자료 탭 — 오너 화면에서 깨져 있던 것들
- **'올리자마자 공개' 글자가 '자료 올리기' 버튼 뒤로 깔렸다.** 옵션·주 버튼·진단 셋을 한
  `display:flex` 줄에 몰아넣은 구조였고, 사파리가 라벨을 줄여 버렸다.
  ⚠️ **크로미움은 재현하지 않는다**(체크박스를 28px 로 키워도 안 남). 원인을 좇는 대신
  **줄을 셋으로 나눠 구조적으로 불가능하게** 했다 — 옵션 / 주 버튼 / 진단.
  같은 모양(한 줄에 라벨+버튼 여럿)을 다른 탭에 새로 만들지 말 것.
- **'업로드 진단'이 주 버튼과 같은 무게**였다 → 테두리만 있는 보조 버튼 + 글자를
  '안 올라가나요? 업로드 진단'으로. 데스크톱에선 주 버튼도 `justify-self:start` 로 200px.
  ⚠️ 그리드 칸이라 `width:auto` 만으로는 stretch 가 이긴다 — `justify-self` 가 필요하다.
- **'자료 유형' 칸이 2열 격자의 세 번째**라 모바일에서 반쪽 폭으로 혼자 남아 깨진 칸처럼
  보였다(159/327px) → 격자 밖 전체 폭 행으로.
- **placeholder 잘림**('비우면 회원이면 누구ᅡ') → '비우면 회원 누구나'.
- **행 버튼 6개가 전부 같은 모양**이고 그중 하나가 삭제였다 → `.ri-del.is-danger`
  (빨강 #B3261E · 대비 6.54:1). ⚠️ **되돌릴 수 없는 행동에만** 쓴다.

### 검사 방법(재현)
`admin.html` 사본을 만들지 않는다 — playwright `addInitScript` 로 `MONC.requireAdmin`·`MONC.sb`
를 목으로 갈아끼우고 탭을 하나씩 눌러 잰다. 세는 항목: 16px 미만 입력칸 / 44px 미만 조작요소
(체크박스는 24px) / 12px 미만 활자 / 가로 넘침 / 콘솔 에러.
⚠️ `input[type=file]` 은 타이핑 칸이 아니라 iOS 가 확대하지 않는다 — 세면 거짓 실패가 된다.

### 3차 — 날짜 칸이 격자를 뚫고 옆 요소를 덮던 건 (2026-08-02 · 오너 "아직도 박스가 튀어나오고 정렬이 안 됐는데?")

2차 정비를 하고도 오너 화면(챌린지 탭)에서 **모집 시작·마감 칸이 삐져나오고 '추가/수정' 버튼이
'프로그램 시작' 입력 위에 올라타 있었다.** 내 검사가 놓친 이유가 분명하다.

- **잰 것**: 페이지·패널의 `scrollWidth − clientWidth`(가로 넘침) → **0 이었다.**
- **안 잰 것**: **요소끼리 겹치는지.** 칸이 트랙을 넘쳐도 페이지 폭은 안 늘어난다.
  넘친 칸은 옆 칸 *위로* 올라탈 뿐이라 넘침 검사에 안 걸린다.
- 게다가 **크로미움의 `input[type=date]` 는 min-content 폭이 좁아** 트랙에 들어간다.
  사파리의 날짜 컨트롤은 **약 168px 아래로 안 줄어든다.** 눈검사로도 크로미움 측정으로도 못 잡는다.

**원인**: `.round-form { grid-template-columns: repeat(auto-fit, minmax(120px,1fr)) }` 의 트랙은
393px 화면에서 158px, 데스크톱에서도 127px 다. 그리고 **그리드 칸의 기본 `min-width` 는
`min-content`** 라 안에 든 것이 크면 칸이 트랙을 넘친다.

**고친 것**
- `.round-form > *`·`.field` 에 **`min-width:0`**, 입력에 `min-width:0; max-width:100%`
  → 어떤 컨트롤도 자기 칸 밖으로 못 나간다. **지우지 말 것.**
- **`.field-date { grid-column: span 2; min-width:170px }`** — 날짜 칸은 두 트랙을 쓴다.
  ⚠️ 새 날짜 입력을 만들면 감싸는 `.field` 에 **`field-date` 를 같이 단다**(현재 8곳).
- 모바일에서 `.round-form > button[type=submit]` 은 **한 줄 전체**. 필드 옆 칸에 끼어 있어서
  오너 화면의 '추가/수정'이 옆 입력을 덮었다.
- 삭제 버튼 전부 `.is-danger`(빨강 #B3261E · 흰 배경 대비 6.54:1) — 2차엔 연구실 자료 한 곳만
  했었다. 지금은 12곳(오픈 알림·챌린지·특강·크레딧·후기·수기·소재·감점·기출·이용권·파일·시간대).
- 목록 줄이 `모집 undefined ~ undefined` 를 찍던 것 → `미정`(원칙 11 — 모르는 걸 단정하지 않기).

### 검사에 추가한 것 — 겹침 검사(overlap.mjs)
탭마다 **실제 조작요소끼리** 사각형이 겹치는지, **자기 `.field` 밖으로 나갔는지** 본다.
`12개 탭 × 6폭(320·375·393·430·768·1280) × 2모드`.
- ⚠️ **`.field` 같은 래퍼끼리 비교하면 안 잡힌다** — 입력칸이 래퍼를 넘쳐도 래퍼끼리는 안 겹친다
  (첫 판을 그렇게 짜서 '이상 없음'이 나왔다). **input·select·button 을 직접** 비교할 것.
- ⚠️ **닫힌 `<details>` 안의 요소는 제외**한다. `content-visibility:hidden` 이라 그려지지만
  않을 뿐 좌표는 그대로여서 겹침으로 잘못 잡힌다(답변 프로그램 4건 오탐).
- ⚠️ **iOS 폭 재현 모드**를 같이 돌린다: `input[type=date]{min-width:168px}` 를 주입한다.
  크로미움 기본으로만 돌리면 이 버그가 통과한다.

### 4차 — 날짜 입력이 카드를 뚫던 진짜 원인 (2026-08-02 · 오너 세 번째 지적)

3차에서 `.field-date { grid-column: span 2 }` 로 칸을 한 줄 전체(327px)로 넓혔는데도
**오너 화면에서 날짜 박스가 여전히 카드 밖으로 약 20px 나갔다.** 칸 문제가 아니었다.

**증거가 스크린샷에 있었다**: 날짜 칸만 **회색 민짜 박스**로 보였다. 옆의 '기수'는 흰 배경 +
테두리인데 날짜만 다르다 — **우리가 준 배경·테두리가 안 먹고 있었다는 뜻**이다.
iOS 사파리는 `input[type=date]` 를 **네이티브 컨트롤**로 그린다. 네이티브 위젯은 우리 스타일을
무시하고 **자기 고유 크기로 폭을 정하며, 우리가 준 padding 이 그 밖에 붙는다**(그래서 초과분이
정확히 좌우 padding 20px 였다). `width:100%`·`max-width:100%`·`min-width:0` 를 다 줘도 소용없다.

**고친 것 — 네이티브에게 크기 결정권을 주지 않는다**
```
.smain input[type="date"] { -webkit-appearance:none; appearance:none; box-sizing:border-box;
  max-width:100%; min-width:0; background:#fff; border:1px solid var(--border); text-align:left; }
.round-form input[type="date"], .field input[type="date"] { width:100%; }
```
- ⚠️ **`appearance:none` 을 지우지 말 것.** 이게 이 버그의 실제 수정이다.
- ⚠️ 탭하면 **날짜 피커는 그대로 열린다** — 그리기만 바뀌지 동작은 그대로다.
- ⚠️ `width:100%` 는 **격자 칸 안의 날짜에만** 준다. 후기 탭 `#rvTextDate` 는 flex 줄에 끼어
  있어 100% 를 주면 옆 입력을 밀어내고 혼자 한 줄을 먹는다(한 번 그렇게 만들었다가 되돌렸다).
- 마지막 안전장치로 `.round-form { overflow-x: clip }` — 무엇이 됐든 카드 밖으로는 못 그린다.
  `hidden` 이 아니라 `clip` 이라 스크롤 컨테이너가 안 생기고 세로 포커스 링이 안 잘린다.
  ⚠️ 이건 **backstop 이지 수정이 아니다** — 겹침 검사는 여전히 rect 로 재니 가려지지 않는다.

### ⚠️ 이 컨테이너에는 사파리 엔진이 없다 — 세 번 놓친 진짜 이유
playwright 의 `webkit` 은 설치돼 있지 않고, 프록시가 다운로드를 막는다(403 host not permitted).
크로미움만으로는 **네이티브 폼 컨트롤이 얽힌 버그를 구조적으로 못 잡는다** — date 입력이 그랬고,
하단 고정바 잘림·체크박스 라벨 겹침도 같은 부류였다.
- 그래서 **네이티브 위젯이 관여하는 자리는 '재현해서 고친다'가 아니라 '네이티브에게 결정권을
  주지 않는다'로 간다** — `appearance:none`, 줄 나누기, 명시적 박스 모델.
- 오너 스크린샷에서 **우리 스타일이 안 먹은 흔적**(배경색·테두리가 다름)을 먼저 찾는다.
  그게 '네이티브가 그리고 있다'는 신호이고, 그 순간 폭 계산도 우리 것이 아니다.

### 소재 문제 탭 — 항공사 칸 삭제 · 유형별 보기 추가 (2026-08-05 · 오너 "항공사는 의미가 없는 것 같다")

**항공사 칸(`#qfAirline`)을 폼과 목록에서 없앴다.** 소재 발굴 문제는 전 항공사 공통이라
채울 이유가 없었고, 값이 든 문제만 목록 줄에 항공사가 붙어 기준이 있는 것처럼 보였다.
- ⚠️ **DB `questions.airline` 컬럼은 그대로 둔다**(구 데이터 보존). 화면에서만 뺀 것이다.
  저장 row 에서 빠졌으므로 기존 값은 수정해도 남는다 — 지우려면 콘솔에서 한다.
- ⚠️ 답변 프로그램 **기출 은행(`#apqAirline`)의 항공사는 그대로다** — 거기는 항공사별 기출이라
  의미가 있다. 이름이 비슷하다고 같이 지우지 말 것.

**유형별 보기 필터(`#qCatFilter`) 신설.** 난이도 필터만 있어서 "상황대처형이 몇 개인지"를
목록을 훑어야 알 수 있었다. 칩 문법은 admin 정본(`.apq-filter` + `.af-row` + `.af-chip`).
- ⚠️ **두 필터는 겹쳐 걸린다. 각 칩의 개수는 '상대 필터를 적용한 뒤'의 건수다** — 전량으로 세면
  `3` 이라 적힌 칩을 눌렀는데 목록이 0건이 되어 화면이 거짓말을 한다.
- **5유형은 0건이어도 항상 세운다**(어떤 유형이 있는지가 보여야 한다). `CAT_LABEL` 에 없는 옛
  코드가 데이터에 있으면 뒤에 덧붙인다 — 안 붙이면 그 문제가 '전체' 말고 어느 칩에도 안 잡혀
  조용히 사라진다.
- 빈 안내는 '이 난이도에는 문제가 없습니다' → **'고른 조건에 맞는 문제가 없습니다'**(이제 조건이 둘이다).
- 검증(375px 목 12건): 칩 숫자 = 목록 건수 일치(유형·난이도 교차 5단계) · 가로 넘침 0 ·
  12px 미만 활자 0 · 44px 미만 표적 0 · 콘솔 0.
