# 역량검사 게임 연습(games.html) — 상세 기록

> 2026-08-24 신설. 매 작업 공통 규칙은 CLAUDE.md 에 있고, 이 문서는 이 기능을 고칠 때 읽는다.

## 무엇인가 · 왜 만들었나

항공사 채용 전형에 AI역량검사(게임형)가 퍼지고 있어, 그 **게임 유형을 미리 손에 익히는 무료 연습장**이다.

- 2026-08-24 조사에서 확인한 사실: 제주항공은 자인원 역검 전용 응시 도메인(`jejuair.acca.ai`)이 실존, 파라타항공(구 플라이강원)도 동일(`parataair.acca.ai`). 티웨이(2026-10 트리니티항공으로 사명 변경 예정)는 2026-07 채용 전형에 '서류·역량검사·영상면접' 명시, 에어부산은 2025-07 채용부터 AI역량검사 신규 도입 보도, 진에어는 전형에 AI역량검사(1차 면접 뒤) 포함. **티웨이·에어부산·진에어의 검사 업체는 미확정** — 수강생 응시 안내 링크가 `*.acca.ai` 인지로 확정한다.
- 오너 확정: **무료, 결제·크레딧 없음.** 순수 클라이언트 게임이라 제공 원가가 0이다(서버·AI 호출 없음) — 유료화하려면 오너 결정부터.

## 지켜야 하는 선(법적 — 완화 금지)

1. **전부 자체 제작이다.** 잡다(자인원)의 화면·그래픽·캐릭터·문구·사운드를 가져오지 않는다(저작권). 게임 '유형'(도형 회전·순서 기억 같은 표준 인지과제)은 아이디어라 자유지만, **검사 전체 구성을 통째로 똑같이 재현하는 것도 피한다**(2019 대법원 팜히어로사가 판례 — 규칙 조합의 복제는 침해가 될 수 있다).
2. **"잡다"·"역검"을 서비스 이름처럼 쓰지 않는다.** 화면 이름은 '역량검사 게임 연습'. 개발사 언급은 고지문의 "자인원·잡다와 무관" 한 곳뿐 — 설명적 사용만.
3. **페이지 하단 고지(`.gm-note`) 삭제 금지**: 자체 도구·개발사와 무관·실제 검사와 다를 수 있음·실전은 PC 응시. 이 네 가지가 오인(공식 제휴로 보임)과 과신(이것만 하면 된다)을 막는다.
4. 실제 검사 화면 캡처를 레포·페이지에 싣지 않는다.
5. **이모지 금지**(2026-08-25 오너 "이모지를 사용한 것만 해도 벌써 비슷한 느낌"). 아이콘·게임 그래픽은 전부 `games.html <defs>` 의 자체 심볼이다 — 게임 아이콘 9종(`gi-*`, 가는 선 + currentColor)과 도형 세트(`gs-*`, 면 채움 — 순서 기억·마법약 재료·가위바위보가 공유). 굵기는 심볼이 아니라 바깥 svg CSS 에서 준다(tools.html 의 use 그림자 트리 규칙). JS 는 `sym(id)` 로만 그린다.

## 파일 · 구조

- `games.html` — 허브(카드 9장) + 스테이지(소개 → 플레이 → 결과)가 한 파일. head 는 표준 장착(og 3종 동일 제목·nav.js·inapp.js·scroll-keep.js(defer 없음)·pageshow reload).
- `games.js?v=1` — 엔진(타이머·점수·화면 전환·기록) + 게임 9종 정의(`GAMES` 배열). **게임 추가는 이 배열에만** — 허브 카드·소개 화면은 배열에서 그린다. 파일을 고치면 `?v=` 도 같이 올린다.
- Supabase 를 싣지 않는다(비회원 공개·서버 판정 없음). nav.js 는 supabase 없이도 돈다(nav.js 머리 주석).
- 진입: tools.html 격자 7번 타일 + nav `TOOLS_SUB` 7번 항목. `SECTION_OF` 에 'games.html': 'tools' 등록됨(빼면 현재 위치 표시가 사라진다).
- 게임 화면 히스토리: 게임을 열 때 `pushState(#게임id)` — 폰 뒤로가기가 페이지 이탈이 아니라 목록 복귀가 된다. `#rps` 처럼 해시 딥링크도 열린다(이때는 상태를 push 하지 않는다 — 주석 참조).

## 게임 9종 — 규칙 요약

| id | 이름 | 측정 | 진행 | 점수 |
|---|---|---|---|---|
| rps | 가위바위보 | 순발력·판단 전환 | 지시(이겨라/져라/비겨라)+상대 손, 60초 | 맞힌 수 |
| path | 길 만들기 | 계획·공간 | 타일 90도 회전으로 입구→출구 연결, 90초 | 완성 ×10 |
| sequence | 도형 순서 기억 | 작업 기억 | 불 켜진 순서 재현, 3→+1, 기회 3 | 최고 단계 |
| rotate | 도형 회전 | 공간 회전 | 회전 일치 보기 고르기(거울상 함정), 60초 | 맞힌 수 |
| potion | 마법약 만들기 | 기억·정확성 | 주문서 재료를 솥에 담아 제조, 5점부터 주문서 숨김, 60초 | 맞힌 수 |
| cat | 고양이 술래잡기 | 전략·수읽기 | 벽으로 가두기(어긋난 격자·6방 이웃), 잡을수록 벽↓, 놓치면 종료 | 잡은 판 |
| yaksok | 약속 정하기 | 조건 추론 | 조건 만족하는 유일 시간 찾기(유일성은 구성으로 보장), 90초 | 맞힌 수 |
| numbers | 숫자 누르기 | 주의·탐색 | 1부터 차례로, 판마다 개수↑(5→12), 60초 | 판 개수 합 |
| compare | 개수 비교 | 수 감각 | 점 많은 쪽(크기 무작위 — 면적 단서 제거), 맞힐수록 차이↓, 60초 | 맞힌 수 |

## 구현 메모(함정)

- **점수 기록은 localStorage `monc_games_v1`** — 기기별 최고점·판수뿐, 개인정보 아님. scroll-keep 의 'sessionStorage 고정' 규칙은 위치 기록 얘기라 여기 적용 안 된다.
- **bfcache reload 표준 장착.** 게임 타이머가 `Date.now()` 기준이라 되살아난 화면은 이미 끝난 판이다 — reload 가 맞다. `scroll-keep.js` 는 defer 없이, 페이지 reload 처리보다 앞(CLAUDE.md).
- 연타 UI 라 `button { touch-action: manipulation }` + 보드 `user-select:none` — 빼면 더블탭 줌·텍스트 선택으로 화면이 튄다.
- rotate: 거울상이 회전과 같아지는 대칭 도형은 다시 뽑는다(함정을 못 만든다).
- path: 정답 길을 먼저 깎고 타일을 흩는다. 만들자마자 이어져 있으면 정답 타일 하나를 더 돌린다.
- yaksok: 정답 유일성은 재시도가 아니라 **구성으로 보장**(답 요일+더미 요일만 가능 ∩ 답 시간대만 ∩ 더미 요일 불가).
- cat: BFS 이웃 순서를 섞는다 — 안 섞으면 같은 거리에서 늘 같은 쪽으로 도망가 패턴이 읽힌다.

## 검증

- lint·테스트 없음(레포 공통). **375px 브라우저 실측**이 기본 — 허브 격자, 게임별 시작 화면, 실제 플레이 동작(탭 판정·타이머·결과 화면), 320px 잘림.
- 2026-08-24 초판 실측: Playwright 크로미움 375×740 — 9종 전부 시작·조작·판정 동작, 콘솔 에러 0(상세는 커밋 메시지).

## 남은 일

- **실전 재현도 보정** — 각 게임의 라운드 수·시간·난이도 곡선은 공개 후기 기반 근사치다. 실제 응시한 수강생의 이야기(어떤 게임이 나왔는지·규칙 차이)를 받으면 그 게임부터 맞춘다.
- 티웨이·에어부산·진에어의 검사 업체 확정(수강생 응시 링크 도메인).
- 반응 좋으면: 회원 점수 기록(Supabase)·기록 비교 같은 확장은 오너 결정 후.
