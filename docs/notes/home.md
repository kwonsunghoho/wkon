# 홈(index) · 챌린지 허브 — 상세 기록

## 계측 비콘 `page_events` — 규칙 (2026-08-05 보안 점검)

anon INSERT 는 **열어 둔다.** 측정 대상이 로그아웃 방문자의 히어로 도달률이라 로그인 필수로 만들면 기능이 죽는다. 대신 무엇을·얼마나 넣을 수 있는지를 좁혔다(migration `20260805150000_page_events_guard.sql`).

- **이벤트명은 `^[a-z][a-z0-9_]{2,63}$`** — 소문자·숫자·밑줄만, 3~64자. **새 이벤트를 이 규칙 밖으로 지으면 계측이 조용히 안 쌓인다**(비콘 호출부가 전부 실패를 무시하도록 짜여 있어 화면에 아무 표시가 없다). 경로는 `/` 로 시작, `meta` 는 객체 + 직렬화 500자 이내.
- 규칙은 **RLS 가 아니라 CHECK 제약**(`page_events_shape_ck`)에 있다 — service_role 도 못 지나가고 규칙이 한 곳에만 있다. anon INSERT 정책은 `with check (true)` 그대로.
- 폭주 상한 **분당 전역 2,000건**(`trg_page_events_rate`). 첫 방문 1명당 최대 2건이라 분당 첫 방문 1,000명에 해당 — 인스타가 크게 터져도 안 닿는다.
- 보관 정리는 `select public.page_events_prune(180);`(서버 전용). pg_cron 을 켜면 자동화 가능 — 마이그레이션 5절에 주석으로.
- ⚠️ **한계**: RLS 로는 방문자별 속도 제한이 안 된다(anon 은 신원이 없고 IP 를 정책에서 못 본다). 위 상한은 전역이라 '테이블이 커지는 것'은 막아도 '그동안 정상 계측이 거절되는 것'은 못 막는다. 실제 공격이 오면 답은 **Edge Function 비콘 + IP 제한**이지 DB 가 아니다.
- ⚠️ **컬럼은 `event` 다.** `program-common.js` 가 `name` 으로 보내고 있어 **답변 프로그램 계측(ap_*)이 한 건도 안 쌓이고 있었다**(2026-08-05 발견·수정). 새 비콘을 달 때 컬럼명을 확인할 것 — 실패가 조용해서 눈으로는 안 잡힌다.

### 전환 퍼널 이벤트 — `supabase-config.js` 공용 비콘 (2026-08-17)

방문→가입→apply 도달→결제 시도의 분모를 만드는 비콘. `supabase-config.js` 맨 아래 IIFE 라 **그 파일을 싣는 32개 페이지 전부에 자동으로 붙는다**(페이지별 코드 없음). 표·CHECK 는 기존 그대로 — 마이그레이션 불필요.

- `visit` — 탭 세션의 첫 페이지에서 1회. meta `{ member, ret_days, ref?, src? }`. **`ret_days` 가 재방문율의 원천**: 직전 방문일과의 날짜 차(기기 localStorage `monc_seen_v1` 에 마지막 방문 '날짜' 문자열 하나만 남긴다 — 개인정보 없음. 공용 기기 잔존 규칙과 다른 자리). null=이 기기 첫 방문 · 0=같은 날 재세션 · 1 이상=다른 날 재방문. `ref` 는 외부 유입 도메인(내부 이동 제외), `src` 는 `utm_source`.
- `page_view` — 세션에서 처음 밟는 경로마다 1회(sessionStorage `monc_pv_v1` 가드). apply·lecture 도달률 등 페이지별 분모. bfcache 통째 reload 가 다시 실행돼도 가드가 남아 이중 집계 없음.
- `pay_open` — 결제창을 연 순간. 여섯 결제 지점(apply `k:challenge` · lecture `k:lecture` · ai-killer/mypage/polish `k:credit` · program `k:program`)이 `moncBeacon('pay_open',{k})` 로 직접 쏜다. **복귀 복원·재기록(drop 후 add·URL 복원) 자리에는 심지 않았다 — 이중 집계 방지. 새 결제 흐름을 만들면 결제창 열기 직전 한 곳에만 심는다.**
- 공용 함수 `window.moncBeacon(event, meta)` — 앞으로 페이지 인라인 계측도 이걸 쓴다(insert 코드 복사 금지. 기존 moncTrack·개별 insert 는 잔존 — 통합은 선택).
- **admin·review-desk 는 계측 제외**(운영자 방문이 분모를 흐린다). supabase-config 를 안 싣는 **privacy·researchers·lab-* 안내판 5개는 비콘이 없다**(알려진 공백 — researchers 를 재려면 supabase 로드부터).
- 가입·결제 '완료'는 비콘이 아니라 **DB 가 원장**(members.created_at·applications·credit_ledger·program_enrollments) — 퍼널은 비콘 분모와 DB 분자를 나란히 읽는다.

## 히어로 정체 한 줄 — `.hc-tag` (2026-08-02 오너 "스크롤도 승무원 넣어보고")

- 첫 화면에 '승무원'이 한 글자도 없어 인스타 유입이 여기가 뭐 하는 곳인지 못 읽던 것 보완. 스크롤 신호(#heroCue) 맨 위에 **"승무원 준비 온라인 플랫폼"** 한 줄(13.5px·800). cue 안이라 등장(1.8s)·스크롤 숨김(is-gone)을 그대로 따르고, 창 개구부 아래 자리라 로고 실측 상수(fitWindow/fitKor)와 무관하다. `.hc-label`(아래로 스크롤)은 opacity .78 로 한 단계 눌렀다(대비 여전히 4.5:1 이상).

> 2026-07-30 CLAUDE.md 다이어트로 이관한 기능별 상세 기록·의사결정 원장이다.
> 매 작업 공통 규칙은 CLAUDE.md 에 있고, 이 문서는 해당 기능을 고칠 때 읽는다.
> 본문 속 '위/아래 ○○ 절 참조'는 구 CLAUDE.md 기준 표현이라, 그 절은 docs/notes/ 의 다른 문서에 있을 수 있다.

## index.html 개요(구 CLAUDE.md Pages 항목)

- `index.html` — the landing page. **서사**(2026-08-19 현행: 인트로 → 막막함 → 네 개의 문 → 열다섯 가지→세 가지 → 숫자 → 마무리 CTA — 아래 '랜딩 섹션 순서' 참조). **CSS는 `index.css`로 분리** — 캐스케이드 `tokens.css`→`index.css` 순서 보존이 핵심(링크 순서 바꾸면 `!important` 싸움이 깨짐). ⚠️ index의 CSS는 `index.css`에서 찾을 것(index.html 안엔 `<style>` 없음). JS는 여전히 index.html 인라인.

### 랜딩 섹션 순서 — 2026-07-30 C안 서사(오너 확정 · `outputs/home-story-v2-mockup.html`의 C안 + B안 칩 장치가 결정 기록, 기각안 A·B도 그 파일에 보존) · **2026-08-19 오너 지시로 ②③ 자리 교환**
창문 인트로(유지) → **① 막막함(`.msn`, `id="home"`)** → **② 네 개의 문(`.doors`)** → **③ 열다섯 가지 → 세 가지(`.sortsec`)** → **④ 숫자(`.nums`)** → **⑤ 마무리 CTA(`.closing`)**. 2026-08-19 오너 지시 *"챌린지 섹션을 '혼자 준비하는 시간이 막막하지 않도록' 다음으로, '혼자하면 막막'은 그다음으로"* — 다섯 박자가 **감정 → 문(챌린지가 첫 답) → 정리 → 근거 → 마무리** 로 바뀌었다(구 2026-07-30 순서는 감정 → 정리 → 문). 칩 그룹 제목=문 이름 일치 규칙은 유지 — 문이 이제 칩 정리 **위**에 있다.
- **① 막막함**: 라벨 Why MONC + "혼자 준비하는 시간이 막막하지 않도록." + 리드 + **질문 카드**(`.worry` — "그래서 저는 지금 뭘 준비해야 하나요?"). **⚠️ `id="home"`이 이 섹션에 있다** — footer '서비스 소개' 링크와 `hero_reached` 계측이 이 앵커를 보므로 **인트로 직후 첫 섹션에서 옮기지 말 것**. (구 하단 CTA 바 '몬크 더 알아보기'의 착지점이기도 했으나 그 상태는 2026-07-30 폐기.)
- **② 열다섯 가지 → 세 가지**("혼자하면 막막하지만, 몬크와 함께하면 **간결합니다**." — 2026-07-30 오너 문구): 칩 **15개**(자소서(FSC·LCC) · 답변(차별화·소재발굴) · 보이스(목소리) · 스피치(말 맛) · 이미지 연출(헤어&메이크업) · 기업정보(단순 정보) · 기업분석(기업문화 이해) · 카메라연출(구도·조명) · 필수기출 · 롤플레잉 · 영어인터뷰 · 멘탈관리 · 비언어 표현 · 스펙관리 · 이력관리 — **목록·괄호 설명·나열 순서 전부 오너 확정**, 같은 날 10개에서 확장)가 하나씩 체크된 뒤, **같은 칩이 제자리에서 세 그룹으로 날아가 붙는다**(FLIP). `data-g` 1=승준노트 2=챌린지 3=특강 → **③의 문 순서와 같다**. 그룹 구성(오너 확정): **승준노트** 자소서·기업정보·필수기출·답변 / **챌린지** 보이스·스피치·이미지 연출·비언어 표현·카메라연출·롤플레잉 / **특강** 기업분석·영어인터뷰·멘탈관리·스펙관리·이력관리. ⚠️ **`data-o` 는 그룹 안에서의 자리**다 — 나열 순서와 그룹 순서가 서로 달라서 따로 들고 있어야 한다(없으면 그룹 안 순서가 나열 순서를 따라간다). ⚠️ **높이 재기(`reserve`)와 실제 이동(`place`)이 같은 순서(`toGroups()`)를 써야 한다** — 부제가 있는 칩과 없는 칩의 높이가 달라, 순서가 다르면 줄마다 높이가 달라져 미리 잰 값이 어긋난다. ⚠️ 부제(`.sc-t em`)는 **12px 하한**이고 `.sc-t` 의 `line-height:1.35` 가 핵심이다 — body 의 1.7 을 물려받으면 두 줄 칩이 64px 까지 부풀어 목록이 화면 하나를 통째로 차지한다(실측). ⚠️ 칩 나열 순서는 일부러 섞여 있다(그룹 순으로 늘어놓으면 '정리되는 순간'이 안 보인다). ⚠️ 그룹 제목은 문 이름과 **글자 그대로 같아야** 방금 정리한 세 덩어리가 아래 세 문과 같은 것으로 읽힌다. ⚠️ **한 번만 실행**(`io.disconnect`) — 되감기를 넣으면 스크롤을 오르내릴 때마다 글이 다시 움직인다. ⚠️ **전체 1.5초 한도(오너 확정)는 칩 개수와 한 쌍이다** — 10개 시절 값(STEP 70·HOLD 240·FLY 450·FLY_STEP 22)을 15개에 그대로 쓰면 2.07초로 한도를 넘는다. 현재 44/180/430/12 = 약 1.45초. **칩을 더 늘리면 이 값들을 줄일 것.** ⚠️ reduced-motion·구형에선 이동 없이 결과 화면만 성립한다(요점은 '칩이 움직였다'가 아니라 '열 개가 세 덩어리가 됐다'). ⚠️ `.sort-pool[hidden]{display:none}` 가드 필수 — `display:grid`가 UA `[hidden]`을 이긴다(`.door[hidden]`과 같은 함정).
  - **⚠️⚠️ 자리 미리 잡기(`reserve()`)를 지우지 말 것 — 없으면 스크롤 중 화면이 튄다(2026-07-30 오너 신고 "밑으로 내릴 때 화면이 튀는 것 같다").** 칩 한 덩어리와 세 그룹 카드는 높이가 다르다(칩 10개 시절 실측 375px 262→524 · 1280px 252→524 · 320px 262→524. **15개로 늘린 뒤: 375px 예약 704 · 1280px 예약 712**). 그래서 칩이 그룹으로 바뀌는 **한 프레임에 아래 내용 전부가 262~272px 아래로 순간이동**했다. 원인은 애니메이션 성능이 아니라 **문서 높이가 갑자기 늘어난 것**이다(칩 이동은 `transform` 이라 레이아웃을 안 건드린다 — 여기를 헷갈리면 엉뚱하게 애니메이션을 줄이게 된다). 지금은 두 상태 중 큰 높이를 `wrap.style.minHeight` 로 미리 잡아 정렬 전·후 높이가 같다(**실측 이동량 0px** — 문 섹션 위치·문서 높이가 정렬 전후로 동일).
    - ⚠️ 남는 자리는 칩 **아래**에 둔다. 가운데·아래 정렬로 바꾸지 말 것 — 관측 대상이 `wrap`(=525px)이라 0.35 임계에 걸릴 때 칩이 화면에 거의 안 들어와 있게 된다.
    - ⚠️ `min-height` 는 **CSS 가 아니라 JS 로** 준다 — JS 가 동작하지 않는 환경에선 칩이 그룹으로 바뀌지 않으므로 CSS 에 박으면 그 자리가 영영 빈 칸이다.
    - ⚠️ 높이를 재려고 칩을 잠깐 옮겼다 되돌리는데 **전부 같은 태스크 안**이라야 한다(비동기로 쪼개면 정렬된 모습이 한 프레임 번쩍인다).
    - ⚠️ **글꼴 도착(`document.fonts.ready`)·리사이즈 때 다시 재야 한다** — SUIT 이 늦게 오면 그룹 제목 줄 높이가, 리사이즈 때는 칩 열 수(2열↔3열)가 바뀐다.
    - ⚠️ **`run()` 첫 줄에서도 한 번 더 잰다**(`playing = true` 보다 먼저 — reserve 는 이동 중이면 건너뛴다). 리사이즈 이벤트를 놓쳤거나 글꼴이 방금 바뀌면 잡아 둔 자리가 몇 px 모자랄 수 있는데, 여기서 바로잡으면 어긋남이 '정렬되는 순간'이 아니라 '체크 시작 전'에 흡수된다.
    - 대가로 정렬 전 1.5초 동안 칩 아래 빈 자리가 보인다(**15개 기준 실측 375px 약 150px · 1280px 약 177px**).
  - **⚠️ ≥1024px 에서는 세 그룹 카드를 가로로 놓는다(2026-07-30 오너 승인 · `.sort-groups{display:grid; repeat(3,1fr)}`).** 이유는 모양보다 **빈 자리**다: 칩 목록은 3열이라 5줄에서 끝나는데 카드를 세로로 쌓으면 두 배 넘게 길어져 예약 높이가 712px, 빈 자리가 366px 이었다. 가로로 놓으면 가장 큰 카드 하나 높이면 되므로 **예약 523px · 빈 자리 177px** 로 준다(실측). ⚠️ `.sgrp + .sgrp { margin-top }` 을 되살리지 말 것(가로 배치에선 gap 이 맡는다). ⚠️ 카드가 213px 로 좁아지므로 **카드 안 칩은 한 줄에 하나** — 이 선언은 `.sort-pool, .sgrp-list` 3열 선언 **뒤**에 와야 이긴다(특이도가 같아 순서로 결정된다). ⚠️ 높이는 기본 stretch 로 세 카드가 같아야 '한 세트'로 읽힌다(`align-items:start` 로 바꾸면 아래가 들쭉날쭉해진다). **모바일(<1024px)은 종전대로 세로로 쌓는다** — 가로로 놓으면 카드가 100px 대가 된다. 이건 버그가 아니라 '움직임 0'과 맞바꾼 것이다 — 없애려면 다시 튀는 수밖에 없다(대안은 높이를 부드럽게 늘리는 것인데, 그래도 아래 내용은 움직인다).
- **③ ~~세 개의 문 = 타임라인~~ → 네 개의 문(2026-08-06 개편 — 맨 아래 '네 개의 문' 절이 현행)**: 구 구성은 **매일(승준노트→`briefing.html`) → 2주(챌린지→`challenges.html`) → 면접 직전(특강→`lectures.html`)** 이었다. 아래 사진·칩·스크림 규칙은 새 판에서도 그대로 유효하다. 각 문 위에 `.tl-when` 라벨 + 앞 카드와 잇는 짧은 세로선(`.tl-step + .tl-step::before`)이 붙어 나열이 순서가 된다. **⚠️ 문 순서가 구 스케치(챌린지→승준노트→특강)와 다르다** — 되돌리면 ②의 그룹 순서·타임라인 라벨과 전부 어긋난다. 사진 카드가 좌우로 번갈아 붙는 리듬은 유지(2번째만 `rev`). **⚠️ 카드 자리는 반드시 사진**(텍스트 목록·미니 UI 덩어리는 오너 2회 기각). 사진 3장은 **2026-07-30 오너 제공 최종본으로 교체 완료**(`images/door-briefing.webp` 책상·노트 / `door-challenge.webp` 유니폼 정면 / `door-lecture.webp` 실제 강의실 — 구 `hero-sky-sunset`·`hero-expression`·`instructor-kwon` 재사용 샘플은 폐기). **⚠️ 사진별 `object-position` 가로 초점(`.door-1` 66% · `.door-3` 72%)을 지우지 말 것** — 모바일 카드가 132×220~244 라 cover 가 원본 가로의 **34~45% 만** 남기므로, `center` 로 두면 승준노트 인물이 반으로 잘리고 특강은 강사가 사라져 'TV 화면'으로 읽힌다(실측). 챌린지는 정면 인물이 이미 가운데라 center 유지. 사진을 또 바꾸면 이 두 값을 다시 잡을 것. ⚠️ 특강은 `#doorLecture`(=`.tl-step`)가 `hidden` 시작, 공개 특강이 있을 때만 JS가 켠다 — **C안을 고른 이유 중 하나가 이것**으로, 문이 하나 빠져도 '매일 → 2주'만으로 순서가 성립한다. `.tl-step[hidden]`·`.door[hidden]` 가드 둘 다 유지. 문 칩은 **세 개 모두 실데이터**(승준노트 = 오늘 news_articles count, 챌린지 = 모집중 챌린지 수, 특강 = 가장 임박한 신청중 특강 D-n) — 숫자를 꾸며내지 않는다. 2026-07-30 까지 챌린지 칩만 `DAY 7/14` 라는 지어낸 값이었다(제거 — 아래 항목 참조).
  - **⚠️⚠️ 사진 위 라벨(`.dr-chip`)에 상자를 씌우지 말 것 — 배경·테두리·`border-radius` 전부(2026-07-30 오너 "알약이 아니라 드럼통 같다").** 원인은 문구가 아니라 폭이다: 사진 칸이 375px에서 **실측 132px**이라 한 줄에 한글 7자뿐인데, 12~13자 문구가 두 줄로 풀리면서 `border-radius:999px`(반원)가 **122×53px(2.3:1)짜리 둥근 통**이 됐다. **알약은 한 줄일 때만 알약이다.** 지금은 상자를 없애고 **스크림(`.dr-card::after` 그라디언트) + `— ` 대시 + 흰 글자**다(목업 4안 중 C안 — `outputs/door-chip-mockup.html`이 결정 기록, 기각안 A 짧은 알약·B 아래 띠·D 숫자 태그도 그 파일에 보존). 상자가 없으면 두 줄이 돼도 그냥 두 줄이라 **문구 길이를 관리할 필요 자체가 없어진다.**
    - ⚠️ 스크림은 `pointer-events:none` 필수(`.dr-card`가 `<a>`라 덮으면 클릭이 동작하지 않는다) · 칩은 `z-index:2`(둘 다 카드의 자식) · 폭은 `width`가 아니라 `left+right`(둘째 줄이 카드 밖으로 안 나가게).
    - ⚠️ 대시·숫자 강조는 **`--action-on-dark`**(밝은 하늘색)다 — 네이비(`--accent`)는 어두운 스크림 위에서 1.4:1로 묻힌다(팔레트 규칙 '다크 면 위 강조').
    - ⚠️ `text-shadow`를 지우지 말 것 — 윗줄은 스크림이 옅어지는 구간에 걸친다.
    - **⚠️⚠️ 스크림 세기 `.94/.66` 은 대비 조절값이다 — 사진이 어둡다고 낮추지 말 것(2026-07-30 사진 교체 때 재실측).** 구 `.84/.56` 에서 **하늘색 강조(대시 `— `·`<b>숫자</b>`)가 모바일 4.11~4.49 / 데스크톱 3.64~3.92 로 9대 원칙 4번(4.5:1) 미달**이었다. 새 사진 셋 중 둘이 하단이 흰 책상·흰 유니폼이라 더 빠듯해졌지만 **구 사진도 이미 4.32 로 미달**이라 교체가 만든 회귀가 아니다 — 구 주석의 '5.3' 은 흰 글자 수치를 강조색 수치로 착각해 적어 둔 것. 현재 실측(최악 픽셀·그림자 제외): **하늘색 5.14~6.06 / 흰 글자 8.93~10.53**.
    - ⚠️ **데스크톱이 모바일보다 항상 나쁘다**(크롭이 약해 밝은 영역이 더 많이 글자 밑에 온다) — 한쪽만 재고 넘어가지 말 것. 잴 때는 `object-position` 가로 초점도 반영해야 한다(초점을 옮기면 글자 밑 픽셀이 바뀐다).
    - ⚠️ '아래만 진하고 위로 빨리 걷히는' 급경사(`.94/.60@24%` 등)는 **역효과**(실측 3.13) — 칩이 2줄까지 늘어나고 그 윗줄이 옅은 구간에 걸린다. 40% 정지점을 앞으로 당기지 말 것.
    - ⚠️ 강조색을 밝히는 쪽(`#C9DCF7`, 5.21)도 통하지만 **채택하지 않았다** — 팔레트에 없는 색을 이 자리에만 만드는 셈이라 '새 색을 자리마다 늘리지 말 것'에 걸린다. `--action-on-dark` 대신 스크림으로 맞춘다.
    - ⚠️ **D안(숫자 태그)을 나중에 다시 꺼낸다면 챌린지 칸의 진짜 숫자부터 정할 것.** 구 'DAY 7/14' 는 **아무것도 세지 않는 지어낸 값이라 2026-07-30 제거**했고, 지금은 `#doorChallengeChip` 이 `loadChallengeStatuses()` 로 **'지금 모집중 N개'**(실데이터)를 채운다. 기본값은 `4가지 챌린지`(상품 사실 — 챌린지를 추가하면 같이 볼 것). ⚠️ **모집중 0개면 기본 문구를 유지한다** — '모집중 0개'는 사실이지만 홈에서 가장 먼저 눈에 걸리는 자리에 "지금 살 게 없다"를 띄우는 셈이고, 마감 상태는 챌린지 허브 카드 칩이 이미 정확히 말한다. 세 칩 모두 실데이터이니 **숫자를 다시 꾸며 넣지 말 것.**
    - ⚠️ **펄스를 여기에 달지 말 것** — nav 의 주력 항목 강조(현재 '챌린지', `.nav-feature`)는 **색·굵기뿐**이다. 깜빡이는 점(bfPulse)은 2026-08-03 에 삭제했다(모집 0인 기간에도 계속 깜빡여 거짓 신호가 된다 — nav.css 주석). nav 에는 이제 깜빡임이 없으므로, 홈 카드에 점을 달면 nav 강조가 그 옆에서 배경이 된다(사이트에 깜빡임이 아예 없는 건 아니다 — 연구실 `.live` 와 특강 스켈레톤이 쓴다. 둘 다 '지금 진행 중'을 말하는 자리라 성격이 다르다). 움직임이 필요하면 깜빡임이 아니라 숫자 카운트업(한 번 보고 끝난다).
  - **⚠️ 문 설명줄(`.dr-ds`)·칩 문구의 `&nbsp;`를 지우지 말 것(2026-07-30).** 이 칸은 375px에서 **실측 155px**뿐이라 `<br>`로 잡은 두 줄이 네 줄로 풀린다. 그때 nbsp가 없으면 '답변 / 소재까지'·'필요한 / 내용을'처럼 한 덩어리가 갈라지고 낱말 하나가 혼자 한 줄에 남는다. 데스크톱(글 칸 440px)에선 nbsp가 있어도 그대로 두 줄이라 손해가 없다. **문구를 길게 고치면 375px에서 어디가 갈라지는지 다시 볼 것.**
  - ⚠️ **폭의 주인은 `.tl-step`**(문이 타임라인 칸 안으로 들어갔다). `.door`에도 `max-width`가 남아 있으면 좁은 쪽이 이겨 큰 화면에서 카드가 안 커진다 — `.door`는 `max-width:100%`로 칸을 채우기만 한다.
  - ⚠️ 타임라인 라벨·연결선은 카드 **바깥 가운데**다. 왼쪽 레일(세로줄+점) 방식은 375px에서 카드 폭을 26px 차지해 사진이 눈에 띄게 좁아진다(실측).
- **④ 숫자 — 몬크가 쌓아온 것(2026-07-30 오너 확정)**: 라벨 The Numbers → 제목 "어렵고 고민되는 승무원 준비 / 방향을 알려드리겠습니다." → **타일 4개 2×2**(`#moncStats`: 11년 몬크 데이터 · 30,000+명 함께했던 승준생 · 3,500+명 몬크 수강생 · 10,000+개 합격 자소서 데이터) → 마무리 "여러분들의 승무원 준비 여정 / 몬크가 함께 합니다." 숫자·문구 전부 오너 확정 값(출처 대응은 오너 몫).
  - **⚠️⚠️ 구 '업계 현실' 3타일(2만 준비생 · 350여 명 합격 · 0.18% 가능성)과 브릿지("이 숫자를 바꾸는 건 준비한 시간입니다.")는 오너 지시로 삭제** — *"기존 승무원 준비 확률은 빼버려"*. **되살리지 말 것.** 같은 날 아침엔 '현실 3개 → 브릿지 → 몬크 4개' 두 묶음이었는데, 오너가 그 구도(다른 곳의 숫자로 불안을 자극한 뒤 우리 숫자를 제시하는 방식) 자체를 접었다. 지금 이 섹션은 **우리가 쌓아온 것만** 말한다.
  - **⚠️ 그래서 이 섹션엔 액센트 타일이 없다** — `.num.is-hot` CSS도 함께 삭제했다(유일하게 쓰던 0.18% 타일이 사라져 미사용 코드가 됐다). 남은 넷 중 하나를 골라 색을 칠하지 말 것: 넷 다 같은 급의 사실이고, 홈의 액센트 세 자리(라벨·숫자 하나·링크) 중 '숫자 하나'가 비었을 뿐이다.
  - **⚠️⚠️ 2×2 고정 — 데스크톱에서 4열로 펴지 말 것.** 720px 컨테이너를 4열로 나누면 타일 안쪽이 131.5px 인데 여섯 자리 숫자+단위가 38px 활자로 그보다 넓다(구 '30,000+여 명' 실측 159.3px = 숫자 105.7 + '+' 22.9 + 단위 30.7). 들어가게 하려면 26px 까지 내려야 하는데 그러면 숫자가 이 섹션의 중심 요소로 읽히지 않는다. 2열이면 안쪽 309px 라 38px 그대로 넉넉하다(원칙 10). **단위에서 '여'를 뺀 뒤로는 320px 에서도 22px 로 안 깨진다** — 구 ≤360px 축소 블록은 그래서 삭제했다.
  - **⚠️ 카운트업 연출(2026-07-30 오너 "있어보이게, 너무 짧지 않게")**: 2.0초 + easeOutQuint + **타일마다 0.17초 지연**. 구 1.1초 easeOutCubic 동시 시작은 눈이 좇기 전에 끝나 '바뀌었다'로 보였다. 단위(년·명·개·+)는 **숫자가 멈춘 뒤에** 뜬다(`.is-done`) — 숫자가 올라가는 동안 '0명' 같은 조합을 감추는 장치이자 종료 표시. **⚠️ 단위를 숨기는 CSS 는 `.is-armed`(JS가 붙인다) 안에서만 동작한다** — JS가 동작하지 않는 환경에서 단위가 영영 안 보이면 안 된다. 1,000 이상은 콤마(`toLocaleString`), `tabular-nums` 라 자릿수가 늘어도 폭이 안 출렁인다.
  - 카운트업은 `[data-countgrid]` 가 붙은 그리드마다 각각 관측한다(묶음별 1회) — 타일을 추가하려면 그 안에 넣기만 하면 된다.
  - **'+'는 윗첨자 위치(`.num strong .np`, 숫자의 60% 크기 · `vertical-align: .55em`)**. 2026-08-06 오너 지시로 `.3em`(숫자 중간 높이)에서 올렸다 — 이 값에서 '+'가 숫자 윗변과 거의 같은 줄에 걸린다(375px 실측: 숫자 top 3637.7 / '+' top 3637.2). 더 올리면 숫자 위로 떠 단위와 무관해 보인다. **타일 높이는 안 변한다**(strong 높이 375px 38.3px · 1280px 64.6px 로 전후 동일) — 값을 더 키우면 줄상자가 늘어 숫자와 설명 사이가 벌어지므로 바꿀 때 높이를 같이 잰다. 320·375·1280px 에서 줄바꿈 없음(실측).
- **⑤ 마무리 CTA**: "국내 최초 온라인 승준 플랫폼, **몬크ON** 시작하기" + **버튼 2개**(왼쪽 '온라인 챌린지 시작하기'→`challenges.html` / 오른쪽 '나만의 승준로그 채우기'→`briefing.html`) + '이미 회원이라면 로그인'. 2026-07-30 오너 지시로 구 수미상관 문장·단일 버튼(→apply.html)에서 교체.
  - **⚠️ 버튼 둘의 무게가 다르다**: 왼쪽=돈 내고 시작하는 상품(흰 알약), 오른쪽=무료로 매일 채우는 도구(테두리 버튼). 둘 다 흰 알약으로 만들면 어디를 눌러야 할지 알 수 없다. ⚠️ `.closing-btn`의 **투명 1.5px 테두리를 지우지 말 것** — 고스트 쪽에만 테두리가 있으면 나란히 놓였을 때 높이가 3px 어긋난다(실측).
  - 375px에선 세로로 쌓고 480px부터 나란히 — 실측상 알약 하나가 195px이라 나란히 두려면 400px이 필요한데 375px 화면의 콘텐츠 폭은 335px뿐이다.
  - ⚠️ 다크 배경라 '몬크ON' 강조는 **`--action-on-dark`(밝은 하늘 블루)** 다. 네이비(`--accent`)는 이 배경에서 묻힌다.
  - ⚠️ 오너 지시로 목업의 **MONC PROMISE 3단 목록(01 오늘 할 일을 찾고 / 02 직접 연습하고 / 03 다시 나아가도록)은 넣지 않는다** — 다시 넣지 말 것.
- **⚠️⚠️ 홈 팔레트 = 딥 네이비 on 웜 페이퍼(2026-07-29 오너 확정 · 목업 `outputs/home-color-mockup.html` B안이 결정 기록, 기각안 A·C도 그 파일에 보존)**: 오너 지시 *"폰트에 색을 너무 많이 추가한 것 같다 / 오렌지는 탈피 / 첫 이미지처럼 깔끔·단정하게"*. **단일 소스는 `tokens.css` 하나다** — `--action`·`--accent`·`--accent-ink` 가 전부 `#1B3A6B` 한 값이다(여러 단계로 나뉘어 있으면 자리마다 다른 채도가 섞여 다시 산만해진다). `--accent-dark` 만 한 단계 어두운 `#142C52` 로 남는다(큰 볼드 텍스트·테두리·포커스링). ⚠️ 두 곳에 나뉘어 있으니 찾을 때 헷갈리지 말 것 — `--action` 계열은 앞쪽 기본 `:root` 의 'Action Color' 절, `--accent` 계열은 **맨 아래 '팔레트 오버라이드' `:root` 블록**이다. ⚠️ **`index.css` 에 `:root` 팔레트 재선언을 만들지 말 것** — 2026-08-06 커밋 302c2c0 에서 걷어냈다(홈만 잉크·경계 값이 갈려 있었다). 지금 `index.css` 의 `:root` 엔 `--serif` 하나뿐이다. 다크 배경 `--bg-dark`는 갈색에서 잉크 네이비 `#1C2A3A`로. **배경은 2026-08-05 에 순백으로 한 번 더 갈아탔다**(`--bg #FFFFFF` / `--bg2 #F4F7FC`, 잉크·회색도 쿨 — 오너 *"누리끼리해서 사기가 저하된다 / 완전히 백색, 시원시원하게"*). 웜 페이퍼로 되돌리지 말 것 — 상세는 design-principles.md '현행 팔레트'.
  - **⚠️ 히어로 사진도 2026-08-05 에 같이 다시 만들었다**(`images/hero-window-mob.webp`·`-web.webp`). 첫 화면은 사진이 전면을 덮어서 **배경 토큰을 아무리 희게 해도 안 바뀌기 때문**이다 — 벽이 크림톤(R−B≈+15)이라 배경만 희게 하면 홈 첫 화면만 누리끼리하게 남는다.
    - **`.hero-shot` 에 CSS 필터를 거는 방법은 기각**했다. 벽을 중화할 만큼 채도를 내리면 **창밖 하늘의 파랑까지 같이 빠진다**(실측: `saturate(.55)` 로 벽 R−B 17→9, 하늘 −85→−49). 하늘은 로고가 앉는 자리라 손해가 크다.
    - **대신 픽셀을 직접 고쳤다.** 규칙은 한 줄이다 — `w = R − B` 가 **양수인 픽셀만**(=웜한 벽) 중화하고, 음수인 픽셀(=창밖 파랑)은 건드리지 않는다. `R −= 0.62w · G −= 0.06w · B += 0.85w`, 그다음 밝기 190 이상에서만 흰색 쪽으로 34% 당긴다(어두운 베젤·창틀 그림자 보호). 실측상 이 사진은 **93%가 밝기 224 이상**이라 벽 전체가 이 규칙에 걸리고, 하늘(밝기 160~192, R−B≈−55)은 3~6%뿐이라 그대로 남는다.
    - 결과: 벽 R−B **+15~17 → −5~6**(쿨 쪽으로 살짝), 밝기 **223 → 231~234**. 치수(853×1844 · 1717×916)는 그대로라 `fitWindow` 픽셀 상수는 안 건드려도 된다. 파일도 67→49KB · 60→38KB 로 줄었다.
    - **재작업 도구**: PIL·ImageMagick·cwebp 가 없어서 **크로미움 캔버스**로 처리했다(`getImageData` → 픽셀 변환 → `toDataURL('image/webp', 0.9)`). 400% 확대로 하늘 밴딩 확인 완료(재인코딩 열화 없음).
  - **`.hero-fade` — 사진 하단과 다음 섹션의 이음매**(2026-08-05, 오너 지시). 사진 하단 벽(밝기 ~231)과 아래 `.msn`(~246) 사이에 가로선 하나가 남았다. 구 웜 페이퍼에선 배경도 94% 라 이어져 보였는데 순백 전환으로 드러난 자리다. 사진을 더 밝혀서 없애는 방법은 **창문 주변 은은한 글로우가 납작해져서 기각**하고 CSS 로 처리했다(사진 원본을 안 건드린다). 실측: 경계 단차 **0**(y 방향으로 235,238,242 → 244,247,252 까지 단조 증가 후 평탄).
    - ⚠️ **도착색은 `--bg` 가 아니라 `--bg2`** 다 — 히어로 바로 아래 `.msn` 이 `--bg2` 를 깐다. **`.msn` 배경을 바꾸면 여기도 같이 볼 것**(안 그러면 이음매가 되돌아온다).
    - ⚠️ **마크업 위치가 규칙의 일부다**: `<picture>` 바로 뒤 · `.hero-mid` 앞. z-index 없이 **DOM 순서로만** 로고·스크롤 신호 아래에 깔린다. 뒤로 옮기면 로고 위를 덮고, z-index 로 해결하려 들면 `.hero-mid` 안 조립 애니메이션까지 새 쌓임 맥락에 갇힌다.
    - ⚠️ 높이 **104px** 는 스크롤 신호(`.hero-cue`, 아래에서 156px 를 쓴다)보다 낮게 잡은 값이다 — 더 키우면 신호 글자 뒤가 통째로 단색 판이 되어 사진에서 떨어져 보인다.
  - **⚠️ 액센트는 세 자리만 — 라벨 · 숫자 3개 중 '가능성' 하나 · 링크.** 개편 전엔 오렌지 하나가 홈 스토리 영역에만 9자리(라벨 대시·라벨·숫자·"가능성"·문 링크 3개·칩·"옵니다"·버튼)에 쓰여 강조가 너무 많아 아무것도 강조되지 않았다. **새 색을 자리마다 늘리지 말 것.** "가능성" 강조는 색이 아니라 **밑줄**(`box-shadow: inset`)이다 — 색으로 칠하면 라벨·링크와 같은 색이 문장 한가운데 또 나온다.
  - **⚠️ `--action-ink`를 다크로 되돌리지 말 것** — 구 `#2A1206`은 '오렌지 배경 위 다크 글씨'로 대비를 맞추려던 값이다. 네이비 배경엔 흰 글씨(10:1). 반대로 **마무리 CTA 버튼은 흰 알약 + 네이비 글씨**여야 한다(다크 네이비 배경 위 네이비 버튼은 1.2:1로 안 보인다).
  - **2026-07-29 같은 날 전 사이트로 확장 완료**(오너 지시) — 팔레트는 전부 `tokens.css` 한 곳에 있고, `index.css` 의 `:root` 엔 `--serif` 하나만 남는다(2026-08-06 커밋 302c2c0 에서 재선언 블록 삭제). ⚠️ **`--ink` 는 2026-08-05 에 `tokens.css` 전역으로 올라갔다**(홈 제목만 네이비이던 것을 전 페이지로 넓힘) — `index.css` 에서 다시 선언하지 말 것. 홈만 다른 값이 필요하면 `:root` 가 아니라 그 요소 규칙에 적는다. 인트로 태그라인의 M·O·N·C 강조는 `.hi-l.is-mark`(색은 `var(--accent)`). 전 사이트 규칙은 위 'Design system' 항목 참조.
  - **⚠️ 승준노트 문(`.door.rev`)은 글도 오른쪽 정렬**(2026-07-29 오너 지적 "승준노트이 변칙인데 왜 텍스트는 왼쪽 정렬이냐"). 사진이 왼쪽·글이 오른쪽인 카드라, 왼쪽 정렬로 두면 글 블록 오른쪽에 빈 띠가 남아 카드가 한쪽으로 쏠려 보인다. 라벨 대시(`::before`)와 링크 화살표는 inline 요소라 자동으로 따라간다.
  - **⚠️ 미션 섹션은 밝은 종이(구 다크 배경) · 세 개의 문은 테두리 있는 흰 카드**(오너 요청). 문 카드가 박스가 되면서 **구 '모바일 사진 블리드 + 카드마다 다른 높이·글 시작점' 구도는 폐지** — 블리드로 되돌리지 말 것(세 개가 같은 급의 '문'으로 묶여 보이는 것이 요청의 목적). 다크 배경는 **마무리 CTA 한 곳뿐**.
  - **⚠️ 구 `nav.nav-transparent`(인트로 구간 투명 nav + 흰 글씨)는 CSS·JS 모두 삭제.** 인트로가 밝은 종이색이 된 뒤로 흰 글씨가 배경에 묻혔다(대비 약 1.1:1). 되살리려면 인트로 배경부터 어둡게 돌려야 한다.
- **⚠️⚠️ CSS 주석 안에서 클래스 이름을 슬래시로 나열하지 말 것 — 별표 뒤에 슬래시가 오면 주석이 그 자리에서 닫힌다.** 2026-07-29 스토리 개편 때 섹션 주석에 `hero-scene`·`hs-`·`ch-` 계열을 슬래시로 이어 적으며 별표+슬래시가 생겼고, 주석이 조기 종료되면서 뒤따르던 구분선 종료 기호가 문법 오류가 되어 **파서가 바로 다음 규칙인 `.msn { … }`을 통째로 삼켰다.** 결과: 미션 섹션에 배경이 안 깔린 채 제목·브릿지만 다크용 밝은 글씨로 남아 **베이지 배경에 흰 글씨**가 됐다(오너 신고 "배경에 흰색 글씨라 안 보인다"). 규칙 하나가 조용히 사라지는 종류의 오타라 눈으로는 안 잡힌다 — **CSS를 크게 손댔으면 주석 짝(`/*`/`*/`) 균형을 한 번 세어 볼 것.**
- **⚠️ `footer` 스타일도 같은 개편에서 통째로 날아가 있었다**(2026-07-29 복구). 규칙이 하나도 없어 링크가 **브라우저 기본 파란 밑줄 `#0000EE`**로 떴다 — 사이트 어디에도 없는 색이라 홈 맨 아래에서만 튀었다. 지우지 말 것.
- **⚠️ 데스크톱(≥1024px) — 스토리 컬럼 확대(2026-07-29 오너 "웹인데 따닥따닥")**: 구조는 그대로 두고 폭·활자만 큰 화면 급으로 올린다(미션 560→720px, 문 640→900px, 문 제목 20→28px·설명 15→17px). 개편 직후엔 브레이크포인트가 720px 하나뿐이라 **1440px에서도 375px 모바일 값이 그대로 떴다** — `clamp()`가 미션·마무리 제목에만 걸려 있어 문 섹션은 아예 커지지 않았다. ⚠️ **문 3개를 3열 카드로 펼치지 말 것(오너 기각)** — 좌우 교차 리듬이 이 섹션의 구도이고, 3열이면 challenges.html 카드 목록과 구분이 사라진다. ⚠️ `.door`의 `max-width`는 `min(900px, calc(100% - 96px))` — 고정값이면 브레이크포인트가 시작되는 1024px에서 좌우 여백이 거의 안 남는다(실측). ⚠️ 문 카드 사진은 `position:absolute`여야 한다 — 카드가 글 높이에 맞춰 늘어나는 구조(`align-items:stretch`)라 img가 흐름에 있으면 자기 높이로 카드를 밀어버린다.
- **홈에서 삭제된 것(2026-07-29 오너 확정 "깔끔하게")**: 구 #pillars(세 개의 문 카드) · 히어로 챌린지 카드 목록(→challenges.html 이사) · Before&After(증거는 각 상세가 담당) · #briefing-home · #lectures-home · 연구진 스트립 · 커뮤니티(후기는 reviews.html) · **파인더(#advisor — 존치 계측 중이었으나 개편에 포함해 삭제, 오너 승인)** · 블라인드 퀴즈(→blind-quiz.js, 보신각·스피닝 상세를 거쳐 2026-07-30 challenges.html 하단에 정착) · 성장기록 · 구 .cta-box. index.html 2979→1354줄, index.css 2006→673줄.
- **계측**: `pillar_challenge/lecture/briefing`(문 3개, pointerdown)은 유지 — 구 pillars와 이벤트명이 같아 기준선이 이어진다. `briefing_go`/`lecture_go`/`advisor_*`/`hero_ab_*` 리스너는 대상 소멸로 제거. `hero_reached`는 이제 '미션 섹션 도달'을 뜻한다(앵커 동일 — 인트로 이탈률 지표로서 연속성 유지).
  - **2026-08-19 추가(홈 시작 버튼 위치 결정용 — 사용자 피드백 "시작하기까지 스크롤이 너무 길다")**: ① `closing_reached` — 첫 방문 세션이 마무리 CTA(`.closing`)까지 내려간 순간(intro_view·hero_reached 와 같은 IIFE·같은 조건·1회). ② 마무리 버튼 2개를 `data-pillar` → **`data-closing`** 으로 분리해 `closing_challenge`/`closing_briefing` 으로 센다 — **그 전 `pillar_challenge`/`pillar_briefing` 숫자에는 맨 아래 버튼 클릭이 섞여 있다**(문 카드만의 수치가 아니었다). 홈에서 챌린지로 가려는 클릭 총량은 pillar_x + closing_x 로 합산.
- **nav(2026-07-29)**: '커뮤니티' 메뉴 삭제(섹션 소멸 — researchers.html의 `index.html#community` 링크도 함께 제거). ⚠️ 같은 날 넣었던 드롭다운 첫 항목 '챌린지 한눈에'(→challenges.html)는 **2026-07-31 오너 지시로 뺐다** — 목록 맨 위에 다시 넣지 말 것. 지금은 상위 버튼 '챌린지' 자체가 허브로 간다(`nav.js` 의 `CHALLENGE_HUB` + `data-hub`).

### 히어로 — 창문 정적 배경 + MONC 조립 (`#heroIntro` · 2026-07-29 전면 교체)
오너가 새 배경 이미지(창+하늘 **합본**)를 주면서 히어로를 갈아엎었다. **조립 안무 자체는 구 인트로 그대로**이고, 바뀐 것은 **구동 방식(스크롤 → 시간)**과 **뒷부분(창 통과 줌 삭제)**이다. 오너 지시: *"원래 애니메이션을 쓰되, 스크롤하면 작동하는 게 아니라 입장하면 자동으로 발동되게."*

- **구성**: `<section class="hero-intro">` = `<picture>` 배경 한 장 + 정중앙 `.hi-stack`(문장 `.hi-phrase` ↔ 로고 `.hi-target`) + 한글 줄 `.hi-kor` + `#heroCue`(스크롤 신호). 높이 **100svh 한 장**.
- **안무(2026-08-19 2차 압축 — 오너 "훨씬 빠르게" 후 "문장 정지는 0.4초로 놔두고 조립 속도를 지금으로 유지")**: 0~0.4초 문장 정지(읽는 시간 — **줄이지 말 것, 오너 확정**) → 0.4~0.95초 **조립(0.55초)** → 0.90초 한글(페이드 0.3) → 1.20초 스크롤 신호(페이드 0.4). 총 **1.70초**, **로드 직후 rAF 1회**. (1차 압축 2026-07-30: 정지 0.4/조립 0.95/총 2.45 · 최초 0.6/1.8/3.6)
  - **⚠️⚠️ 시간만 줄이지 말고 이징을 함께 볼 것 — '느리다'의 진짜 원인은 곡선이었다.** 구 `easeInOutCubic`(대칭)은 정지 상태에서 **속도 0으로 출발**해 앞의 20%를 매우 느리게 지나가고(1.8초 중 360ms 동안 3% 이동) 끝에서도 같은 만큼 느려서, 실제 이동이 가운데 짧은 구간에 몰려 있었다. 대칭 곡선을 그대로 두고 시간만 줄이면 그 가운데만 빨라져 **'빠르게 지나갔는데도 여전히 느리게 느껴진다'** 가 된다. 지금은 **`easeOutCubic`** — 바로 출발해 길게 미끄러져 착지한다(첫 100ms 에 28%, 절반 시점에 88%, 남은 12%를 길게 흘림). 도착 속도가 0 이라 '툭' 끝나지도 않는다. **대칭 이징으로 되돌리지 말 것.**
  - **⚠️ 소문자 페이드의 기준은 시간이 아니라 이동량(`e`)이다** — `cl(e / 0.64)`. 시간 기준(구 `u / 0.55`)으로 두면 이징을 바꿀 때마다 관계가 어긋나, 앞이 빠른 곡선에서는 **대문자가 이미 날아간 뒤에도 소문자가 제자리에 불투명하게 남아 겹친다.** 0.64 는 구 동작에서 소문자가 사라지던 시점의 이동량(대문자가 64% 갔을 때)이라 보이는 결과는 같다.
  - **⚠️ 프레임마다 같은 값을 다시 쓰지 않는다**(`lastFade`·`lastDone` 캐시). 소문자 17개 opacity·transform 과 `.hi-phrase`(자식 21개) `visibility` 를 매 프레임 쓰던 것이 조립 구간에서 가장 비싼 자리였다. **⚠️ `measure()` 에서 `lastFade` 를 반드시 버릴 것** — 거기서 transform 을 전부 지우므로, 캐시를 남기면 폰트 도착·리사이즈로 재측정된 뒤 '값이 같다'며 다시 쓰지 않아 **소문자가 원래 크기로 되살아난다**(실측으로 확인한 자리).
  - **실측 검증(375px · rAF 를 직접 펌프해 프레임 단위로 재생)**: 조립 시작 420ms · 진행 25% 500ms / 50% 600ms / 90% 920ms / 99% 1160ms(easeOutCubic 이론값 일치) · 소문자 소멸 680ms · 로고 교체 1360ms · 한글 1320→1760ms · 신호 1820→2300ms · 마지막 프레임 2460ms · 최종 배율 0.596. **⚠️ 프리뷰 탭이 백그라운드면 `requestAnimationFrame` 이 아예 안 돌아 애니메이션이 시작조차 안 한다** — 검증할 땐 iframe 에 띄우고 rAF 를 큐로 바꿔치기해 타임스탬프를 직접 넣을 것(라이브 관찰보다 정확하다). `getComputedStyle(...).opacity` 는 CSS transition 이 걸린 요소(`.hero-cue`·`.hi-kor`)에서 동기 루프 중 값이 따라오지 않아 **오독을 만든다 — 인라인 `style.opacity` 를 볼 것.**
- **⚠️ 스크롤 신호(`#heroCue`) = D안(2026-07-30 오너가 목업 4안 중 선택 · `outputs/hero-scroll-cue-mockup.html` 이 결정 기록, 기각안 A 캡슐·B 갈매기·C 알약배지도 그 파일에 보존)**: 한글 라벨 "아래로 스크롤"(12.5px/800/자간 .08em) → 레일(2×46px, 빛 한 줄기가 1.9초 주기로 아래로 흐름) → 화살표(빛이 끝에 닿는 순간 밝아지며 따라 내려감). **구 안(11.5px 'SCROLL' + 1px×16px 세로선)은 활자가 원칙 1번(12px) 미달, 대비 4.15:1 로 원칙 4번(4.5:1) 미달**이었고 결정적으로 **등장 후 멈춰 있었다**(전부 실측). 지금은 잉크 네이비로 대비 10.4:1.
  - **⚠️ '내려라'는 신호는 크기가 아니라 움직임이 만든다** — 활자·대비만 올리고 애니메이션을 빼면 같은 문제로 돌아간다. 레일과 화살표의 **주기가 같아야**(둘 다 1.9초) '흘러내려 도착했다'로 읽힌다. 따로 두면 두 개가 각자 움직이는 것으로 보인다.
  - **⚠️ 스크롤이 24px 넘으면 사라지고, 맨 위로 돌아오면 다시 뜬다**(오너 지시). 안 숨기면 다음 섹션까지 화살표가 따라다니며 이미 내리고 있는 사람에게 계속 "내려라"라고 말한다. **숨김은 `.is-gone` 클래스 + `opacity:0 !important`** — 타임라인 `frame()`이 인라인 `style.opacity`를 매 프레임 덮어쓰므로 인라인으로 숨기면 재생 중에 바로 되살아난다(저자 `!important`가 인라인을 이긴다).
  - ⚠️ 세 조각(라벨·레일·화살표)이 한 벌이다 — 레일만 두면 장식으로 보이고, 화살표만 두면 지금 디자인과 안 이어진다. reduced-motion 이면 흐름 없이 '도착한 상태'로 고정한다(크기·대비 개선은 그대로 남는다).
- **⚠️⚠️ 문장은 두 줄이고, 첫 화면에 **다 보여야** 한다(2026-07-30 오너 "첫 화면이 글자가 이딴식으로 시작하는 게 어딨냐, 화면 안에 봐야 할 거 아니야" — 구 한 줄은 375px 에서 화면의 241%라 `nt Of New` 만 보였다).** `Moment Of` / `New Career` 두 줄(`.hi-line`)이고, `clamp(48px,16vw,128px)` 위에 **`fitPhrase()`가 화면 폭(-32px)에 맞춰 더 줄인다** — 기기 폭이 제각각이라 clamp 만으론 '화면 안'이 보장되지 않는다. **⚠️ 한 줄로 되돌리지 말 것**: 20자라 화면에 맞추는 순간 34px 이 되어 로고(40px)보다 작아지고, '줄어들며 창으로 들어오는' 연출이 **커지는** 방향으로 뒤집힌다(실측). 조립 배율 0.55~0.65.
- **⚠️⚠️ 로고 크기·자리는 CSS 가 아니라 JS(`fitWindow`)가 개구부에서 역산한다 — 퍼센트로 되돌리지 말 것.** 개구부의 화면 대비 폭이 **세로형 38.1% · 가로형 15.7%**로 완전히 달라 같은 `vw` 값이 두 경우에 전혀 다른 결과를 낸다. 게다가 배경이 `object-fit: cover` 라 화면 비율에 따라 이미지가 잘리면서 개구부의 폭도 **세로 위치도** 또 달라진다(실측: 1440×900 에서 창이 224px 이 아니라 **264px**). 그래서 cover 스케일을 직접 계산해 **개구부 사각형을 박스 좌표로 환산**한 뒤 ① 로고 폭이 그 폭의 `FIT`(0.90)이 되도록 font-size 를 역산하고 ② `.hero-mid`를 그 중심에 앉힌다(`top` px + `translate`). 실측 채움률 **90.0%**, 중심 오차 **0.0px**(320·393·430·768·1024·1440·1920 전 폭). CSS 의 `clamp(28px, 10vw, 52px)`·`top:49.4%/50.7%`는 **JS 가 동작하지 않을 때의 폴백일 뿐**이다.
- **⚠️⚠️ `OPEN` 상수는 '유리(개구부)' 좌표다 — 바깥 창틀이 아니다(2026-07-30 오너 "글자가 창틀 밖으로 삐져나오고 위치도 중앙이 아니다").** 구 세로형 값 `0.230~0.769`는 유리가 아니라 **창틀 바깥 테두리**를 잰 것이어서, 로고가 개구부의 **127%**(실측)로 커져 M 과 C 가 창틀 위로 올라탔다. 가로형 값만 우연히 유리였다. 현재 값 — 세로형 `l .3118 / r .6928 / t .3384 / b .6490`, 가로형 `l .4222 / r .5789 / t .2750 / b .7385`. **배경 이미지를 교체하면 네 값을 '유리 기준으로' 다시 잴 것**(창틀을 재면 같은 사고가 반복된다).
- **⚠️⚠️ 세로 기준은 '로고 + 한글'을 합친 **덩어리 중심**이고, 거기서 개구부 높이의 `LIFT`(4%)만큼 더 올린다(2026-07-30 오너 "아직도 창문 아래쪽에 있잖아").** 한글 줄이 흐름 밖(`top:100%`)이라 `.hero-mid` 높이에 안 잡혀서, **로고만** 정중앙에 두면 그 아래 한글 42px 이 통째로 추가로 붙어 덩어리가 아래로 쏠린다 — 오너가 두 번 지적한 지점이 정확히 이것이다. LIFT 는 시각 보정(기하학적 정중앙은 눈에 낮게 읽히고, 위가 짙은 하늘·아래가 흰 구름이라 아래가 더 비어 보인다). 네 자리(0 / 덩어리중앙 / +4% / +7%)를 **실제로 렌더해 비교**한 결과이고, 값을 바꾸려면 같은 방식으로 비교할 것.
- **⚠️⚠️ 한글 줄은 로고와 **양끝을 맞춘다**(2026-07-30 오너 "서브타이틀 메인타이틀 양쪽 글자에 맞춰라, 혼자 툭 튀어나오잖아"). `fitKor()`가 자간을 계산해 넣는다** — 로고 폭이 창 크기에 따라 달라지므로 CSS 고정 자간으로는 절대 안 맞는다. 순서: ① 한 줄이 로고보다 좁으면 자간을 벌려 채운다 ② 조금 넘치면 **글씨를 줄여** 한 줄로 맞춘다(하한 12px — 9대 원칙) ③ 그래도 넘치면 **두 줄로 접고 줄마다** 편다(모바일: 한글 207px vs 로고 135px 이라 항상 두 줄). ⚠️ ②를 빼면 로고가 한글보다 아주 조금 좁은 폭(실측 1024px)에서 반토막 난 줄을 억지로 늘려 **자간이 18px(≈1em)** 까지 벌어진다.
  - **⚠️ 맞추는 기준은 상자가 아니라 '보이는 글자(잉크)'다.** M·C 는 사이드베어링이 넓고 한글은 좁아, `getBoundingClientRect` 끼리 맞추면 한글이 양쪽으로 **3~4px 씩 삐져나온다**(실측). 그래서 캔버스 `measureText`의 `actualBoundingBoxLeft/Right`로 첫 글자·끝 글자의 베어링을 빼고 잰다(`bearings()`/`inkEdges()`). 구형 브라우저에 확장이 없으면 0 으로 떨어져 상자 기준으로 degrade.
  - **⚠️ 마지막 글자 뒤에 붙는 자간은 `margin-right: -자간`으로 도로 뺀다.** 안 빼면 그 빈칸까지 폭에 잡혀 가운데정렬이 왼쪽으로 밀린다. 그래서 `.hi-kl` 은 `inline-block` 이어야 한다.
- **⚠️ 로고-한글 간격은 로고 크기에 **비례**한다(`GAP` 0.55em · `gapFor()`, 2026-07-30 오너 "모바일에서는 메인과 서브 행간이 더 줄어들어야 할 것 같다").** 구 CSS 고정 `margin-top:18px` 은 로고가 작아지는 모바일에서만 헐거워졌다 — 실측 시각 간격 **모바일 0.94em vs 웹 0.62em**. 지금은 세 폭 모두 **0.50em**(모바일 21px). ⚠️ margin 값 ≠ 보이는 간격이다 — 두 줄 상자 안의 빈 공간(로고 글자 아래 ≈0.30em, 한글 줄 위 ≈0.5×글자크기)을 빼고 넣는다.
- **⚠️ 문장과 로고 크기는 한 쌍이다.** 로고만 창에 맞춰 키우고 문장을 그대로 두면 배율이 0.97 이 되어 **크기가 안 변하는 것처럼 보인다**(실제로 한 번 그렇게 됐다). 한쪽을 바꾸면 배율을 다시 볼 것(현재 0.47(모바일) ~ 0.45(웹)).
- **⚠️ 이미지 로드 전에는 `naturalWidth`가 0이라 창을 못 잰다** → `load` 이벤트에서 `refit()`을 다시 부른다. 이 재호출을 빼면 폴백 크기(작은 로고)로 고정된다. **화면 회전은 `<picture>` 소스까지 세로형↔가로형으로 갈아치우므로**(개구부 상수도 함께 바뀐다) `resize`·`load` 둘 다 재생이 끝난 뒤에도 `fitWindow()`를 돌려야 한다 — 재생 중에만 재측정하면 회전 후 로고가 엉뚱한 크기·자리로 남는다.
- **⚠️ 문장 배치는 `left:50%` + `translateX(-50%)` + `width:max-content`.** `left/right:0` + `text-align:center` 는 내용이 컨테이너보다 넓어지는 순간 왼쪽 기준이 되어 **오른쪽으로만 삐져나간다**(구 한 줄 시절 실측으로 확인한 사례).
- **⚠️ 조립 후 로고가 화면에 남는다.** 구 인트로의 뒷부분(로고 전진 확대 → 창 통과 → 페이드아웃)은 **되살릴 수 없다** — 새 배경은 창과 하늘이 한 장에 합쳐져 있어 확대하면 그림 전체가 커질 뿐이다. 되살리려면 하늘/창틀 2겹 이미지부터 필요하다.
- **⚠️⚠️ 도착점은 `.hi-target`을 실측해서 얻는다 — 계산으로 만들지 말 것.** `.hi-target`은 `visibility:hidden`이지만 **자리는 차지하는 실물**이고, JS가 각 글자의 `getBoundingClientRect` 중심을 도착 좌표로 읽는다. 지우거나 `display:none`으로 바꾸면 조립이 엉뚱한 데로 간다.
- **⚠️ 자간은 `letter-spacing`이 아니라 `margin`으로 준다**(`.hi-target span { margin: 0 .035em }`). letter-spacing 은 글자 박스를 부풀려 박스 중심이 실제 글자 중심과 어긋난다 — 구 인트로에서 서브라인 정렬이 삐뚤어졌던 원인이 이것이다.
- **⚠️ 조립이 끝나면 문장을 숨기고 `.hi-target`을 보인다**(`u > 0.995`). transform 으로 2.28배 키운 글자는 래스터가 흐려진다 — 이 교체를 빼면 로고가 뿌옇게 남는다.
- **⚠️ 한글 줄은 흐름에서 빼 `position:absolute; top:100%`로 둔다** — 흐름에 두면 조립 중 커진 글자에 밀려 줄 위치가 출렁인다. **대신 `.hero-mid` 높이에 안 잡히므로 세로 중심을 잡을 때 그 높이를 직접 더해 줘야 한다**(위 '덩어리 중심' 항목 — 이걸 빠뜨린 게 오너가 두 번 지적한 '아래로 쏠림'의 원인이었다).
- **⚠️ `.hi-stack` 높이를 고정해 둔 이유** — 조립되며 글자가 커질 때 아래 한글 줄이 밀려 내려가는 걸 막는다.
- **⚠️ 웹폰트(SUIT)가 늦게 오면 글자 폭이 바뀐다** → `document.fonts.ready`에서 재측정한다(재생 중일 때만 — 도착 좌표가 필요한 구간이라).
- **⚠️ `prefers-reduced-motion`이면 조립을 건너뛰고 '완성된 로고' 상태로 둔다.** 아무것도 안 하면 문장만 남아 로고를 못 보게 된다.
- **⚠️ 타임라인 `total`은 마지막 페이드(`cueAt + 0.4` = 1.60)보다 커야 한다** — 모자라면 스크롤 신호가 중간 투명도에서 얼어붙는다(구 3.4로 뒀을 때 0.83에서 멈춘 실측). 타임라인을 또 줄이면 이 관계를 먼저 확인할 것(현재 1.70 > 1.60).
- **⚠️ 배경 이미지가 뜬 뒤 시작한다**(`load`/`error` + 1.2초 안전망). 안 그러면 느린 회선에서 글자만 떠 있는 화면이 보인다.
- **이미지**: `images/hero-window-web.webp`(1717×916 · 38KB) / `images/hero-window-mob.webp`(853×1844 · 49KB). 구 60KB/67KB 는 2026-08-05 순백 재작업 전 값이다(위 '히어로 사진도 2026-08-05 에 같이 다시 만들었다' 참조). **⚠️ 분기는 폭이 아니라 `(max-aspect-ratio: 1/1)`** — 태블릿 세로에서 가로형 이미지를 cover 하면 좌우가 심하게 잘려 창만 거대해진다. 모바일 이미지 비율 1:2.16이 375×812(1:2.165)와 거의 일치해 잘림이 없다. ⚠️ `<picture>` source 는 **로드 시점에 결정**된다 — 창을 리사이즈해도 안 바뀌므로 확인할 땐 새로고침할 것.
- **⚠️ 스크롤과 무관하다.** 러웨이(200/180vh)·sticky 핀·재방문 단축(`data-zoom-runway` 재작성)·`monc:zoomprogress` 이벤트가 전부 사라졌다. 인트로 도달률 38%의 원인이던 긴 스크롤 구간 자체가 없어진다(index.html 1353→약 730줄).
- **함께 삭제된 것**: `scroll-fx.js`의 `initZoomExit`(293줄) · `tokens.css`의 `[data-zoom-exit]`/`.zoom-exit-pin` 규칙 · 구 태그라인 IIFE(약 35KB, 줌·개구부 알파 스캔·fixed 승격 포함) · 창틀·하늘 이미지 2겹(89KB) · 페이드 레이어 4장. 구 스펙 문서(`docs/superpowers/specs/2026-07-10-hero-monc-tagline-design.md` 등)는 이제 **역사 기록**이다.
- **⚠️ `scroll-fx.js`는 2026-07-30에 `initReveal` 하나만 남기고 정리했다(138→39줄).** 남은 진입점은 `[data-reveal]` 뿐이고 **쓰는 곳은 챌린지 상세 4종**이다. 같이 뺀 것: `initStickyPanel`(`[data-sticky-panel]`을 쓰는 마크업이 사이트에 없었다) · `initCountUp`(`[data-count-up]`도 없었다 — 홈 숫자 타일은 단위 노출·지연 시작 때문에 `index.html`이 자체 카운트업을 갖고 있고 **그쪽이 정본**) · `window.MoncScrollFx` 내보내기(부르는 데가 없었다). **`index.html`의 `<script src="scroll-fx.js">`도 함께 제거** — 홈엔 `[data-reveal]`이 하나도 없어 받아만 놓고 아무 일도 안 했다.
- **⚠️⚠️ 하단 CTA 바는 히어로 구간에서 통째로 숨는다(`.is-intro`, 2026-07-30 오너 지시)** — 구 '몬크 더 알아보기' 상태(`.is-browse` + 광 스윕 + 화살표 까딱)는 **폐기**했다. 이유 둘: ① 바가 자리를 차지해 **스크롤 안내가 위로 밀려 창문을 침범**했다(오너 신고) ② 그 상태의 목적(Clarity — 인트로에서 유일한 버튼을 눌러 아무것도 안 보고 가격 페이지로 떨어지고 빠른 뒤로가기 19%)은 이제 **스크롤 신호가 대신한다**. 버튼을 아예 안 보여주면 잘못 누를 일 자체가 없어 원래 목적에 더 맞는다. `.mcta-browse`/`.mcta-apply`/`.mcta-arrow`·`mcta-shine`·`mcta-arrowbob` CSS 와 '#home 스크롤' 클릭 핸들러는 전부 삭제.
  - 판정 기준은 그대로 **히어로가 화면에 절반 넘게 남아 있는 동안**(`rect.bottom > innerHeight*0.5`). 첫 방문·재방문을 가르지 않는다(2026-07-22 오너 결정).
  - **⚠️ `visibility: hidden` 까지 끄는 이유** — `opacity: 0` 만으로는 **투명한 버튼이 그대로 눌리고** 키보드 포커스도 그 자리에 멈춘다.
  - **⚠️ 바를 되살리면 `.hero-cue` 의 `bottom: 34px` 도 같이 올릴 것** — 34px 은 '아래에 바가 없다'는 전제로 잡은 값이라 그대로 두면 겹친다(구 값 96px 이 그 자리를 피한 값이었다).
- **계측**(`intro_view`/`hero_reached`)과 `monc_intro_seen` 플래그는 유지. 다만 `monc_intro_seen`은 이제 **러웨이 단축이 아니라 첫 방문 판별에만** 쓰인다.
- **폐기된 중간안 — 로고 흩날림**: 같은 날 오너가 *"모래알처럼 한쪽으로 날아가는 느낌"*을 제안해 한 번 구현했다가(위로 스르륵 사라지고 SCROLL만 남는 안) *"날아가는 거 말고 원래 애니메이션을 쓰자"*로 되돌렸다. 결정 기록은 `outputs/hero-sand-mockup.html`. **되돌리지 말 것** — 그 안은 4초 뒤 히어로에 브랜드 메시지가 하나도 안 남는다는 문제가 있었다.

## 히어로 시작 버튼 — 단일 [시작하기] → 네 개의 문 (2026-08-19 2차 확정 · 현행)

> 근거: 첫 방문 95명 중 45명(47%)이 인트로에서 이탈(8/17~19 계측) — 첫 화면에 누를 것이 없었다.
> 경위(전부 같은 날): 1차 A안(신호 위 네이비 알약) 목업 선택 후 라이브 반영 → **오너 반려
> ("완전 별로") revert 11bdd29** → 2차 대형 서비스 실캡처 레퍼런스(클래스101·노션·애플·넷플릭스·
> 당근·에어비앤비)로 '가) 클래스101식 스택'(`[챌린지 시작하기]` 주버튼 + 갈래) 선택, `?cta=on`
> 5차 다듬어 공개(4c66ecb) → **공개 직후 오너 "조잡하다, 그냥 시작하기로 바꾸고 4개의 문에서
> 고르게"** → 단일 [시작하기] 를 같은 게이트 절차로 확인 후 확정.

- **현행 구성**: 한글 서브라인 아래 `.hi-cta` — **단일 `[시작하기]`**(`.hcta-main` · 14px/800 ·
  라운드 12 · 광 그라데이션) 하나뿐. 누르면 **`#doors`(네 개의 문)로 스크롤 착지** — 전역
  `html { scroll-behavior:smooth }` 가 태우고 `.doors { scroll-margin-top:60px }`(≥769px 78px —
  nav 높이 근거는 `.msn` 과 동일, nav 높이를 바꾸면 같이 볼 것)가 고정 nav 를 피한다.
  **별도 JS 없음**(footer→`#home` 앵커와 같은 문법 — 해시가 남는 것도 기존 동작과 동일).
- **왜 단일인가(되돌리지 말 것)**: 레퍼런스 실측(넷플릭스·듀오링고·클래스101 — 2026-08-19
  모바일 폭 재확인) 공통 문법이 **'히어로엔 동사 하나, 갈래 선택은 다음 화면'**. 스택의
  조잡함은 다듬기 문제가 아니라 구조 문제였다 — 유리 폭(375px 실측 143px)에 갈래 둘을 눌러
  담는 한 6차 축소(밑줄 글줄→분할 바→0.93배)로도 안 풀렸다. **갈래 분할 바(`.hcta-row`·
  `.hcta-chip`)·히어로 갈래 링크를 되살리지 말 것** — 갈래가 필요하면 그건 `#doors` 섹션의 일이다.
- **트레이드오프(승지)**: 챌린지 직행이 한 탭 늘었다(구 주버튼이 challenges 직행 · 문 클릭 1위
  42%). 문 1번이 전폭 챌린지 포스터라 위계는 유지되고, 문 클릭은 기존 `pillar_*` 가 잇는다.
- 계측: `hero_cta` 유지 — 의미가 '챌린지 직행'에서 **'#doors 착지'** 로 바뀌었다(1차 스택
  데이터는 공개 몇 시간분이라 단절 무시 가능). `hero_cta_tools`·`hero_cta_lab` 은 갈래 폐지로
  종료(구 `hero_cta_sub` 는 공개 전 교체라 데이터 없음).
- **공개 절차 기록**: 1차 반려(직행 배포)의 교훈으로 만든 `?cta=on` 게이트를 2차에도 그대로
  썼다(숨김 반영 → 오너 폰 실물 확인 → 공개·게이트 제거). **다음에도 히어로급 변경은 이 절차.**
- **⚠️ 버튼은 `.hi-kor`(absolute) 안에 둔다** — 흐름에 두면 로고가 창 중앙에서 밀린다(.hi-kor
  주석의 49.1% 실측과 같은 함정). 등장도 한글 줄과 한 몸(타임라인이 kor 인라인 opacity 제어) —
  별도 타임라인 항목을 만들지 말 것. 부모의 `white-space:nowrap` 과 JS 자간(fitKor)은
  `.hi-cta` 에서 풀어 둔다(`letter-spacing:0`). `.hero-mid` 가 `pointer-events:none` 이라
  버튼에 `pointer-events:auto` 를 준다 — 빼면 안 눌린다.
- **버튼 입체감(오너 "클래스101 느낌이 없다, 그냥 박스다")**: 새 색을 만들지 않고
  **토큰 네이비 위에 흰 광 그라데이션**(`background-image` 알파 화이트→알파 블랙)을 얹었다.
  평면 단색 상자로 되돌리지 말 것.
- **⚠️ 폭 상한 = 유리 실측 폭 `--glass-w`(오너 "창문 안에 이쁘게 들어가게 못하냐")**:
  구 280px 고정폭은 유리(모바일 ~150px)의 **두 배**라 창을 뚫고 나갔다. fitWindow 가 로고(FIT)와
  같은 실측으로 `--glass-w` 를 `.hero-mid` 에 넣고, 버튼은 **글자 폭 맞춤**(`width:max-content`
  + 유리 ×.98 상한 — 오너 "바를 좀 줄이면 되잖아"). fitWindow 의 korH 중앙 정렬이 버튼 높이까지
  포함해 덩어리 전체를 유리 중심에 앉힌다.
- **⚠️ 신호 위치도 실측 — `--cue-lift` + `FRAME_B`(같은 신고 "스크롤 문구가 창문에 있다")**:
  고정 `bottom:34px` 는 화면 비율 따라 창틀에 올라탔다(인앱처럼 세로 짧은 화면·낮은 데스크톱 창).
  창틀+그림자 하단은 사진 높이의 **세로형 70.4% · 가로형 80.7%**(캔버스 픽셀 실측 `FRAME_B`) —
  fitWindow 가 그 아래 +8px 로 `--cue-lift` 를 넣고(6~34px 범위), 모자라면 `cue-tight` 로 세 조각
  사이 여백만 30px 압축한다(한 벌 유지). 구 데스크톱 전용 media 핵은 이걸로 폐기. 고정 bottom
  으로 되돌리면 같은 신고가 돌아온다 — CSS 34px 는 JS 죽었을 때 폴백일 뿐.
- 실측(2026-08-19 2차): 375×812 버튼 85.5×44.3(유리 중심 오차 0 · 유리 143.1px 안) ·
  1280×730 버튼 동일(유리 214.4px 안) · 착지 시 문 라벨이 nav 아래(여유 3.4·10.5px) · 콘솔 0.
- **버튼 모션 — 둥실 플로팅+샤인 스윕(2026-08-19 3차 · 오너 "밋밋한거같은데")**: 움직이는
  목업 5안(플로팅·바운스·펄스·이륙 포함)을 보고 오너가 **1안+4안 조합 확정("1+4로 가자")** —
  기각안 보존: `outputs/hero-cta-motion-mockup.html`. 구현은 CSS 만: 플로팅은 `.hi-cta`
  (translateY -6px · 3.2s), 샤인은 `.hcta-main::before`(폭 34% 흰 띠 · 4s) — 두 주기를
  다르게 둬야 한 덩어리 루프로 안 읽힌다. 시작은 둘 다 delay 1.8s — 타임라인이 로드 직후
  시작하는 보통 경우에 '한글 착지(1.70) 후 움직임'이 되는 값이다. 타임라인 시작이 이미지
  게이트(최대 1.2s 폴백)로 늦으면 페이드 중에 이미 돌 수 있지만 페이드 0.3s 동안 변위가
  2px 이하라 무시한다 — 스크롤 신호(hcFlow·hcNudge)와 같은 문법(무한 모션은 로드부터 돌고,
  보임은 타임라인이 제어). '별도 타임라인 항목 금지'는 그대로 지켰다.
  - ⚠️ **플로팅을 `.hcta-main` 으로 옮기지 말 것** — 돌고 있는 애니메이션이 `:active` 의
    `transform: scale(.98)` 을 덮어 누름 반응이 죽는다(케스케이드에서 애니메이션 > 일반 선언).
    같은 이유로 버튼 자체에 transform 애니메이션을 새로 걸면 안 된다.
  - ⚠️ 샤인은 **transform 으로만** 움직인다 — 챌린지 카드 모집 띠 광 스윕(`.ch-band::after`)과
    같은 문법·같은 이유(left/width 는 매 프레임 페인트 — 트래픽 99% 모바일).
  - reduced-motion 은 둘 다 끈다 — 광 줄기는 멈추면 얼룩이라 `display:none`.
- **버튼 위치(2026-08-20 오너 "약간 아래쪽으로")**: 자막↔버튼 간격 16→32px(`.hi-cta`
  margin-top). 배치 목업 4안(A 현행 / B 아래로 / C 폭 타이틀 맞춤 / D 둘 다 —
  `outputs/hero-cta-layout-mockup.html`) 중 **B 확정("b로가자")**. 텍스트 덩어리(로고+자막)와
  버튼이 두 층으로 나뉘어 읽히는 게 목적.
  - **C(폭을 로고·자막에 맞춤)는 기각** — 네이비 면적이 로고보다 무거워져 위계(로고→자막→버튼)가
    뒤집히고, 컨테이너를 채운 버튼은 "버튼"보다 "바"로 읽힌다. 무엇보다 8/19 오너 확정
    "바를 좀 줄이면 되잖아"(유리 폭 채움→글자 폭 축소)의 역행 — 되살리려면 그 결정부터 뒤집을 것.
    폭 맞춤이 다시 필요해지면 D(아래로+폭)가 차선이었다(간격이 벌어지면 "명판"으로 읽혀 C보다 낫다).
  - JS 수정 없음 — fitWindow 의 korH(= kor offsetHeight + kor marginTop)에 버튼 margin 이
    offsetHeight 로 포함되어 재중앙 정렬된다: 로고 ~8px ↑ · 버튼 ~8px ↓, 간격만 16px 벌어짐
    (목업 B와 같은 거동 — 목업도 flex 중앙 정렬이라 동일했다).
  - 실측(2026-08-20 375×812): 자막↔버튼 간격 12.4→28.4px · 버튼 규격 85.5×44.3 불변(유리
    143.1px 안) · 플로팅 진폭 6.0px 유지 · 콘솔 0.

## challenges.html 개요(구 CLAUDE.md Pages 항목)

- `challenges.html` — **챌린지 허브(2026-07-29 신설, 홈 '챌린지 문'의 목적지).** 아래 '챌린지 카드 목록 — challenges.html' 참조. nav 에서는 상위 '챌린지' 버튼이 여기로 온다(데스크톱은 한 번, 모바일은 아코디언이 열린 뒤 한 번 더 — `nav.js` `data-hub`). 목록 첫 항목 '챌린지 한눈에'는 2026-07-31 오너 지시로 제거됐다.

### 챌린지 카드 목록 — `challenges.html` (2026-07-29 홈 #home에서 통째로 이사)
**챌린지 허브(신설)** — 홈 '챌린지 문'의 목적지. 구성: nav → 허브 히어로(홈 문과 같은 문장 "매일 습관을 만들 기회" + 상품 사실 한 줄) → 레터프레스 카드 4장 → **블라인드 퀴즈(2026-07-30 이사 · 위 '블라인드 퀴즈' 항목)** → 홈으로 돌아가기 → footer. **신청 CTA 없음**(카드가 곧 문, 신청 버튼은 각 상세가 담당 — 오너 확정 "챌린지만 구성되어 있는 게 좋다"). **전·후 증거도 없음**(각 상세가 이미 담당 — 허브에 B&A를 넣자는 안은 오너가 "깔끔하게"로 철회). 카드 CSS·재정렬 JS(`heroReorder` 이관)는 이 파일 인라인.
#### 카드 규격 — 2026-08-03 오너 확정 전면 개편(클래스101 위계 × 무신사 풀블리드 + 광 스윕 띠)

**결정 과정이 목업 4파일에 있다**(전부 outputs/ · 기각안 포함 보존): ① `challenge-card-impact-mockup.html` 1차 하단 띠·절취선 4안 — **기각**(오너 "아랫쪽에만 갖고 뭘 하잖아, 배치는 지금처럼 한 줄로") ② `challenge-card-layout-mockup.html` 2차 형태 4안 ③ `challenge-card-reference-mockup.html` 3차 — 클래스101·메가스터디·무신사·29CM·듀오링고 실물 채집(공통 발견 = **카드마다 '숫자 증거 한 줄'**), 오너 "위계는 101, 레이아웃은 무신사" ④ `challenge-card-band-mockup.html` 4차 — 모집 띠 동작 3안 중 **가안(광 스윕) 확정**("제일 잘보이고 깔끔") + 투명도 비교(85% 채택).

카드 구조(위→아래): **풀블리드 사진**(4:5 · 카드 위를 끝까지, 구 8px 종이 액자 폐지) → **`.ch-copy` 변화 문장이 제목**(15px 800 · '전 → 후' 구조 유지 · 후반 `em`은 틴트 밑줄 `inset 0 -7px var(--accent-tint)` + `box-decoration-break:clone`) → **`.ch-sub`**('보.신.각 · 2주 챌린지' — 이름은 메타로) → **`.ch-proof` 증거 줄** → `.ch-meta` 날짜 줄 → `.ch-st` 상태 줄. 구 `.ch-no`/`.ch-num`(01·Voice 킥커)·`.ch-name`(18px 이름)·`.ch-line`은 폐지 — `heroReorder()`의 번호 재부여도 함께 삭제(재정렬 자체는 유지).

- **카테고리 판(`.ch-cat`) — 전 카드, 사진 하단 고정(2026-08-26 오너 확정 C안).** 오너 진단 "뭔 챌린지인지를 모르니까 사람들이 중도 포기하는거 같아 — 큰 텍스트로 직관적으로 강조". 사진 하단 네이비 판(19px 800 흰 글자, `rgba(27,58,107,.85)` — 띠와 같은 α.85·같은 '면 위 활자' 예외) 위에 **보이스 / 영상면접 / 말투 · 어투 / 답변**(문구는 오너가 준 그대로 — 챌린지 이름이 아니라 훈련 대상). 시안 3벌 비교는 `outputs/challenge-card-category-mockup.html`(A 본문 20px 네이비 제목 · B 24px 초대형 — 둘 다 기각, C 확정). 마크업은 각 `.ch-shot` 안 정적 `<span>`(런타임 아님 — 상태와 무관한 상품 사실). 모집 중이면 위 띠·아래 판이 같이 뜬다(오너가 시안에서 보고 골랐다). 마감 카드도 판은 네이비 유지(흑백 필터는 img 에만 걸린다). 이미지 상단 오버레이, `rgba(27,58,107,.85)`(=`--accent`의 α.85) + 흰 활자, `::after` 광 스윕이 2.8초에 한 번 지나간다(`transform` 애니메이션 — left 로 바꾸면 레이아웃을 건드린다). 문구는 recruit.js 가 채운 `.ch-st` 텍스트('모집 중 · D-n')를 그대로 올리고 **하단 상태 줄은 `hidden` 으로 숨긴다**(중복 방지 — `.ch-st[hidden]{display:none}` 가드 필수, display:block 이 UA hidden 을 이긴다). 예정·마감은 하단 활자 그대로(전 카드에 띠를 붙이면 열린 카드가 안 튄다). reduced-motion 이면 스윕 정지.
  - ⚠️ **투명도 85% 는 실측값** — 띠 밑 최악(순백) 픽셀 기준 흰 글자 대비 100%=11.3 / 85%=7.3 / 75%=5.4:1, **65% 밑은 4.5:1 미달이라 금지**(4차 목업 번외 절이 실측 기록). **블러(backdrop-filter) 금지** — 특강 카드 유리 패널 폐지 전례.
  - ⚠️ **띠 있는 카드만 사진 초점 `50% 8%`**(`.ch-shot.has-band img`) — 기본 24% 그대로 두면 띠(35px)가 인물 머리를 가린다(목업 실측). 사진을 바꾸면 이 값을 다시 볼 것(door-* object-position 과 같은 성격).
- **증거 줄(`.ch-proof`) — 실데이터만, 숫자 꾸며 넣기 금지.** `reviews` 테이블에서 `visible=true` 를 `select('*')` 로 받아(컬럼 나열 시 kind·challenge 마이그레이션 미적용 환경에서 400 — reviews-list.html 과 같은 가드) `kind==='challenge'` 를 클라에서 거르고 `challenge` 값(한글명 '보신각'…, 카드의 `data-review-ch` 와 매칭)별로 세서 '수강생 후기 N개'를 채운다. **0건·조회 실패면 줄 자체를 hidden 유지**(`.ch-proof[hidden]{display:none}` 가드). 2026-08-03 실측: 보신각 55 · 영합각 28 · 스피닝 19 · 승자각 6.
- **레터프레스 종이 카드 유지**: 종이 그라디언트(#FCFDFF→#EBF1F8 — 2026-08-05 쿨 화이트 전환, 구 #FCF9F1→#F3EEE1) + 그림자 3겹(inset 1px / 2px / 18px — 하나라도 빼면 종이 질감이 사라진다. 색은 갈빛 `rgba(58,42,26,·)` 에서 쿨 잉크 `rgba(22,38,64,·)` 로). ⚠️ 카드에서 그라디언트를 빼고 순백으로 만들지 말 것 — 순백 배경과 같은 색이 되어 종이가 사라진다. ⚠️ **사진 위 '맨글자'·스크림·초록 알약은 여전히 금지** — 띠는 불투명 85% 네이비 면 위 활자라 오너가 확정한 예외다.
- ⚠️ hover는 `@media (hover:hover)` 안에(모바일 탭 잔류 강조 방지). ⚠️ `.ch-list`는 `minmax(0,1fr)` + `grid-auto-rows:1fr`(넘침·행높이 함정), 375px부터 2×2 · ≥860px 4열(2026-08-03 실측: 1280px 카드 211px · 행높이 420px 균일). ⚠️ `.ch-shot`은 `flex:0 0 auto` + 4:5, 라운드는 위 두 모서리만(카드 `overflow:hidden` 이 지킨다). ⚠️ 카드 `padding:0` — 되살리면 풀블리드가 깨진다.
- **상태 활자**: 모집 중 = 띠(위 항목), 마감·예정 = 사진 흑백(`.is-dim`, recruit.js가 부여) + '다음 기수 준비 중'('마감 · ' 접두 금지 — 카드 폭에서 두 줄이 된다). **카드 순서는 런타임**: `monc:recruitready` → `heroReorder()`가 모집 중→예정→마감 재정렬(마크업 고정값 금지).
- **모집 기간 줄(`.ch-meta`)**: recruit 데이터 도착 후 'N기 · 선착순 모집 8/24~9/6' 압축형. '모집' 라벨은 유지(원칙 15 — 없으면 챌린지 진행 기간으로 읽힌다). 마감 카드는 비워 둔다(지난 기간은 정보가 아니다). ⚠️ **통째 `white-space:nowrap` 금지(2026-08-26 오너 신고 "날짜 잘리는것도 고쳐줘")** — '5기 · 선착순 모집 8/24~9/6'(실측 144px)이 375px 카드 내용폭 131px 을 넘겨 끝이 잘렸다. 지금은 라벨·날짜를 `.mm` 두 덩어리(각자 nowrap)로 나눠 **그 사이에서만** 줄바꿈한다 — 좁은 화면 2줄·데스크톱 4열 1줄, 덩어리 구조가 네 카드 같아 하단 정렬 유지.

| 마크업 순서 | id | `data-review-ch` | 이미지 | 변화 문장(=`.ch-copy`) |
|---|---|---|---|---|
| 1 | `voice` | 보신각 | `hero-voice.webp` | 떨리는 목소리 → 신뢰가 실리는 목소리 |
| 2 | `expression` | 영합각 | `hero-expression.webp` | 굳은 표정 → 카메라 앞에서도 자연스럽게 |
| 3 | `spinning` | 스피닝 | `hero-spinning.webp` | 딱딱한 말투 → 귀에 감기는 말투 |
| 4 | `answer` | 승자각 | `hero-answer.webp` | 뻔한 답변 → 나만 할 수 있는 답변 |

⚠️ 위 순서는 **마크업 순서일 뿐 화면 순서가 아니다**(모집 상태로 재정렬됨). 변화 문장은 '전 → 후' 구조 유지 — 후반 강조는 굵기+틴트 밑줄이지 **색이 아니다**. 구 'B&A `.ba-arc`와 한 쌍' 규칙은 B&A 삭제로 소멸 — 이 문장의 단일 소스는 이제 challenges.html.

### 블라인드 퀴즈 (`blind-quiz.js` — 2026-07-30 챌린지 허브 하단으로 이사)
자기 주입 공용 컴포넌트(`nav.js`와 같은 패턴): **`challenges.html` 한 곳만** `<div id="blind-quiz-mount">`(카드 4장 뒤·'홈으로 돌아가기' 앞) + `<script src="blind-quiz.js" defer>`로 싣는다.
- **⚠️⚠️ 자리 이력: 홈 → (7/29) 보신각·스피닝 상세 → (7/30) 허브 하단.** 마지막 이동은 오너 지시 *"보신각, 스피닝에서 빼서 허브 하단에 넣어라 — 한 번씩 볼 수 있게"*. 7/29에 적어 둔 '허브에 넣으면 다크 체험이 카드 훑기를 끊는다'는 판단은 **오너가 뒤집었다** — 그 우려는 퀴즈를 카드 *위나 사이*에 둘 때의 것이고, 카드를 다 훑은 **뒤**에 두면 훑기를 끊지 않는다. 상세로 되돌리지 말 것.
- **⚠️ 두 곳 이상에 싣지 말 것** — 상세 두 곳에 있던 동안엔 같은 사람이 보신각·스피닝을 둘 다 열면 같은 체험을 두 번 만났다. 허브는 네 챌린지의 공통 입구라 '한 번씩'이 자연히 성립한다.
- **⚠️ `.bq-btn` hover 배경을 `--action-hover`(네이비)로 두지 말 것** — 글씨가 `--accent-ink`(네이비)라 마우스를 올리는 순간 네이비 위 네이비로 글자가 사라진다(2026-07-30 수정). 다크 배경 위 버튼은 '흰 알약 + 네이비 글씨'가 규칙이니 hover 도 흰 계열 안에서 한 단계만 누른다(`#E8EEF8`).
- **⚠️ 스타일·마크업·로직이 blind-quiz.js 한 파일 — 페이지에 복사본을 만들지 말 것.** 클래스는 전부 `bq-` 네임스페이스(상세 페이지의 `.section-label`/`.btn`과 충돌 방지 — 헤더 `.bq-label/.bq-title`, 버튼 `.bq-btn`). 동작은 종전 그대로: 5라운드(보신각 7쌍 중 3 + 스피닝 4쌍 중 2 랜덤), 실루엣 영상(`video/bq-candidate.mp4`)은 클립 재생 중일 때만(`syncScene`), `prefers-reduced-motion`이면 켄번즈·영상 자동재생 정지, '같은 사람' 반전은 1라운드 1회만.

### (삭제) Before&After 섹션 · 변화 문장 띠
홈 `#before-after`(.ba-* 전부)와 `.ba-arc` '한 쌍' 규칙은 2026-07-29 홈 개편으로 소멸. 전·후 증거(오디오 플레이어·영합각 영상)는 **각 챌린지 상세 페이지가 담당**(원래도 갖고 있었다 — 오너 "챌린지마다 다 들어가 있으니 굳이 밖으로 뺄 필요 없다"). 승자각 '고치기 전/후 답변' 글 카드 아이디어(실제 지원자 답변 2개 필요)는 재료 확보 시 상세에 추가하는 보류 과제로 유효.

## 네 개의 문 — 챌린지 포스터 승격 (2026-08-06 오너 확정 · 현행)

승준 코스·승준 도구 분리에 맞춰 문을 넷으로 늘렸다. 목업 5안(`outputs/home-four-doors-mockup.html`) 중 **A안(전폭 포스터)** 확정, 순서는 오너 원문 그대로: *"A안으로 가자, 순서는 전문 코치진 챌린지, 코스 정하기, 매일 기초 다지기, 면접 직전 필살기 만들기"*. (이 인용의 네 이름은 순서 지정이고, 화면 라벨·제목·설명 문구는 **2026-08-19 오너 확정으로 전면 교체**됐다 — 오너가 낸 2안 중 1안 기반 혼합. 순서·구조·칩은 그대로.)

| # | tl-when(2026-08-19 문구) | 문 | 형태 | 목적지 | 칩 |
|---|---|---|---|---|---|
| 1 | 2주 밀착 챌린지 | 챌린지 | **전폭 포스터**(`.door-poster`) | challenges.html | `#doorChallengeChip` 지금 모집중 N개(실데이터, 0이면 '4가지 챌린지') |
| 2 | 나만의 맞춤 로드맵 | 승준 코스 | 분할 카드(사진 오른쪽) | briefing.html | '코스 4개'(상품 사실) |
| 3 | 올인원 면접 툴킷 | 승준 도구 | 분할 카드 rev(사진 왼쪽) | tools.html | `#doorNewsChip` 오늘 뉴스 N건(실데이터) |
| 4 | 공채 대비 파이널 | 특강 | 분할 카드(hidden 시작 — 공개 특강 있을 때만) | lectures.html | `#doorLectureChip` 다음 특강 D-n |

- **2026-08-19 문구 교체 상세**(제목/설명 — 1차 긴 판을 오너가 "워딩이 너무 길어서 가독성이 떨어진다"로 반려, **짧은 판으로 재확정**. `&nbsp;` 는 고아 줄 방지, 375·750·1280 실측으로 잡은 자리라 지우지 말 것): ① 외운 답변 말고,`<br>`몸에 밴 태도로 합격 / 목소리부터 답변까지, 코치진과 하루 하나씩 ② 합격까지 가장 빠른`&nbsp;`길 / 첫 지원부터 실전까지, 연구진이 설계한 코스로 ③ 준비 시간은`&nbsp;`반으로 / 항공 뉴스, 소재 발굴, 합격생 데이터 첨삭까지 ④ 나만의 한 끗`&nbsp;`필살기 / 이번 공채 핵심을 연구진과 실시간 라이브로. "하루 하나씩"은 상품 사실이라 유지(오너 초안의 '하루 10분'은 지어낸 숫자라 교체).

- **챌린지 포스터가 유일한 강조 문법이다** — 사진이 카드 전체(모바일 340px·데스크톱 430px), 하단 그라데이션(.96/.72/0 62%) 위 흰 제목 + **흰 알약 CTA**(모양만 알약 — 카드 통째가 `<a>` 라 중첩 링크 금지). 다른 문까지 포스터로 바꾸면 강조가 죽는다.
- ⚠️ **다크 위 글자 함정 둘**(실측으로 밟음): ① 제목은 `.dp-title{color:#fff}` 명시 — tokens 의 `h3{color:var(--ink)}` 가 이긴다(apply 배너 전례) ② 라벨은 `!important` 필요 — tokens.css 팔레트 오버라이드가 `.section-label` 을 네이비 `!important` 로 강제한다. 대시(`::before`)도 같이 하늘색으로.
- **구 '준비의 타임라인'(매일→2주→직전) 서사는 폐지** — 오너가 챌린지를 1번으로 지정하며 순서의 뜻이 '시간'에서 '우선순위'로 바뀌었다. ⚠️ 그래서 **칩 그룹(1=승준 도구 2=챌린지 3=특강) 순서는 문 순서와 더 이상 같지 않다** — 그룹을 문 순서에 맞춰 뒤집지 말 것(data-g·data-o 가 전부 그 순서 기준). 2026-08-19부터 칩 정리(.sortsec)는 문 **뒤** 섹션이다.
- 칩 그룹 1 제목은 '승준노트'→'승준 도구'(문 이름과 글자 일치 규칙 유지). 클로징 고스트 CTA 는 '나만의 승준노트 채우기'→'내 승준 코스 정하기'.
- 번호 클래스(door-1/2/3)는 이름 클래스(door-course·door-tools·door-lecture)로 — **초점값은 사진에 딸린 값**이라 사진을 따라간다(door-briefing.webp 66% → 도구 문, door-lecture.webp 72% 유지).
- **코스 문 사진은 door-course.webp**(2026-08-19 오너 교체 — 코칭 화면을 함께 보는 스터디, 오너 제공 IMG_9091 을 sigma 7 블러 + 1400×1050 webp 26KB. 구 hero-answer.webp 재사용은 이날로 종료 — hero-answer 파일 자체는 다른 자리가 쓸 수 있어 남긴다). 초점은 기본값(center 30%). src `?v=20260819` — 갈면 값도 올릴 것.
- **챌린지 포스터 사진은 2026-08-19 오너 교체** — 참여자 화면 격자(블러) 1672×941 webp 57KB(데스크톱 898px의 약 1.86배 — 구 1000×563은 1.1배라 레티나 미달이었다). src 에 `?v=20260819`(인앱 웹뷰 캐시 대비 — 사진을 또 갈면 이 값도 올릴 것). **밝은 사진이라 `.dp-chip` 에 네이비 알약 배경(rgba(20,32,52,.58))을 추가** — 맨글자+그림자였던 구 방식은 어두운 사진 전제라 대비 미달. 다시 어두운 사진으로 갈아도 알약은 유지(어느 쪽에서도 안전).
- 실측(2026-08-06): 375px 포스터 335×340 · 제목 22px 흰색 · CTA 44px 흰 알약 · 라벨 #A8C7F0 · 넘침 0 / 1280px 포스터 900×430 · 제목 30px · 넘침 0 / 모집중 4개·오늘 뉴스 59건 실데이터 칩 정상.
